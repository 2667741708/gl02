# 220.12 8093 V4 预览守护操作记录

## 2026-08-02：Billboard tooltip 单写入者稳定悬停

- 缺陷：密集点位附近发生 tooltip jitter / hover flapping；原始鼠标跟随、平滑补丁和局部坐标补丁竞争同一 `.cad-furnace-tooltip`。
- 修复：新增 `assets/bf3d-tooltip-stable-hover-8093.js`，使用视口固定坐标、30/46px 迟滞、12px 切换优势量和 140ms 驻留；拖拽相机时隐藏，不恢复点位聚焦。
- 部署入口：`tools/remote_deploy_8093_stable_tooltip_hover.py --hover-payload <uploaded-js>`；部署器必须保护 8094 页面、共享 adapter 和 8094 相机哈希。
- 回滚：从 `backups\8093_stable_tooltip_hover_20260802\<timestamp>` 只恢复 8093 页面和专用 hover 资源。
- 冷启动兼容：安装失败时不得提前登记 viewer，必须继续重试；模块早于 Babel 单写入者标志时也必须在安装阶段继续等待；远端 viewer 不暴露 `viewer.THREE`，Vector3 从相机位置克隆。
- 2026-08-02 最终状态：6 项合同测试通过；已用 `tools/remote_guarded_redeploy_8093_tooltip.ps1` 执行“暂停 `BFV4PreviewProxy8093` → 重新部署 → 恢复守卫”，8768 全程保持运行。最终 8093/8768 均为 `Running` 且监听、HTTP 200，8094/共享文件 SHA-256 未变化。当前运行时同时提供稳定悬停与 121 点 `1.22×` 适度放大，SHA-256 为 `E74DFF787522585BA80C44323C744CA9D1B6BE6CD849107EDCE83F35ED0B52F7`；回退目录：`backups\8093_stable_tooltip_hover_20260802\20260802_200808`。
- 交互专项边界：两次新的 Chromium 冷启动均因多个 8093 静态资源返回 `ERR_EMPTY_RESPONSE/ERR_CONNECTION_RESET` 而未进入 viewer，当前不得宣称完整浏览器交互验收通过；守卫/端口/单资源 HTTP 和资产哈希验证已通过。

> **现行口径（2026-07-26）**：11434 只允许一个 27B 大语言模型驻留，
> `OLLAMA_MAX_LOADED_MODELS=1`；8094 只从 `/api/ps` 选择批准且已驻留的
> 27B，不回退 `/api/tags`，不接受调用方指定旧 30.5B。本文更早日期段落中的
> “当前”、30.5B 运行状态和双驻留结论均是对应核查时点的历史事实，不是现行
> 操作指令。完整策略与回滚边界见
> [22012 Ollama 单 27B 修复说明](22012_Ollama_27B_30B自动拉起链路_20260726.md)
> 和本文件末尾的 `OPS-22012-OLLAMA-27B-ONLY-20260726` 记录。

## 2026-07-26 13:59 8094 无回复 PID/GPU 只读复核

- 追踪编号：`OPS-22012-8094-QA-PID-GPU-AUDIT-20260726`。按用户要求只读取
  PID、父子进程、端口连接和 GPU 状态；没有发送问答请求，没有启停、重启、
  卸载或切换模型。
- Ollama 对外监听正常：`ollama serve` PID `13620` 监听 `[::]:11434`。
  8092/8093/8094 监听 PID 分别为 `12444/14172/5560`，父进程均存在；
  先前记录的孤儿 runner PID `976` 已不在进程表中。
- 与用户观察不同，13:58～13:59 的两次 `/api/ps` 均显示两个模型驻留：
  27.8B `chiqiong-blast-furnace:latest` 对应 runner PID `6972`，
  30.5B `bf-diagnosis-runtime:v1` 对应 runner PID `18644`。二者都由当前
  `ollama serve` PID `13620` 直接创建，不是孤儿进程；30.5B runner 创建于
  13:52:27，说明本轮核查前已有其它请求把它重新加载。
- `nvidia-smi` 显示 PID `6972/18644` 分别占用约 `20084/20288 MiB`，
  L20 总显存 `46068 MiB`、已用 `40408 MiB`、剩余 `5345 MiB`。连续三次
  1 秒采样的 GPU/显存控制器利用率均为 `0%/0%`，温度 `46°C`；两个 runner
  当时都没有执行推理。
- 关键连接证据：8094 PID `5560` 与客户端 `10.30.200.18` 保持一个
  `Established` 连接，另有一个 `FinWait2`；但没有 PID `5560` 到
  `127.0.0.1:11434` 的活动连接。Ollama 侧只有 PID `13620` 到 27B runner
  内部端口 `63222` 的连接。这说明页面无回复卡在 8094 代理进入 Ollama
  之前，优先怀疑 `prepare_qa_chat` 前置数据、RAG/MCP 或其它 I/O 等待，
  不是 GPU 正在慢速生成，也不是当前 Ollama 监听 PID 消失。
- 双大模型共占约 `40.4 GiB` 是明显的显存风险，后续若请求需要 embedding、
  新上下文缓存或再次换模，仍可能出现显存不足；但本次快照没有 GPU 忙或
  正在 OOM 的直接信号，不能把“当前卡住”简单归因为 GPU 满载。
- 证据：
  [PID/GPU 只读探针](../tools/probe_22012_pid_gpu_only.ps1)、
  [13:59 完整快照](../logs/22012_pid_gpu_only_20260726_r2.json)。
  若获准继续排障，下一步应只读检查 PID `5560` 的请求日志/线程等待位置，
  确认具体卡在数据库、RAG 还是 MCP；若获准恢复，再仅处理 8094 和非目标
  30.5B runner，不应影响正式 8093。

## 2026-07-26 8094 问答无响应与孤儿代理定点恢复

- 现象：`http://10.30.220.12:8094/` 的智能问答持续无回复；8094 TCP 可连接，但 HTTP 请求会挂起。
- 根因：常规隔离重启停止 `\BlastFurnaceServices\V3AutoPreviewProxy8094` 后，旧 8094 Python 监听进程没有随父进程退出，导致端口始终无法释放；失败保护重新启动计划任务后，新代理无法绑定端口，形成多个 V4 预览代理并存。取证时旧监听 PID 为 `13772`，另有父进程已消失的 PID `7828`；正式 8093 PID `14172` 仍监听 8093，必须保护。
- 恢复：先运行 [restart_22012_8094_preview.ps1](../tools/restart_22012_8094_preview.ps1) 并确认其因“8094 未释放”安全失败；随后使用 [recover_22012_8094_orphans.py](../tools/recover_22012_8094_orphans.py) 停止仅 8094 的计划任务，按进程命令、父进程和监听端口三重条件移除 PID `7828/13772/16952`，保留 8093 PID `14172`，再启动一个新的 8094 托管实例。
- 恢复结果：首次恢复于 2026-07-26 13:35:21 建立 PID `12012`。真实 QA 冒烟在客户端超时后仍占用单线程代理，因此再次定点恢复；最终计划任务于 13:38:38 启动，13:39:18 建立唯一 8094 PID `5560`，父进程 PID `15648` 为受管 `run_22012_8094_preview.ps1`。最终进程表只剩正式 8093 PID `14172` 和 8094 PID `5560`；8094 `/api/ollama/status` HTTP 200，响应为 `proxy_ok=true`、`ollama_ok=true`、`model_ok=true`。证据见 [首次恢复输出](../logs/8094_recovery_r2.out.log)与[最终进程表](../logs/8094_final_process_map.out.log)。
- 剩余问题：首次恢复后对 `/api/qa/chat` 的真实 SSE 冒烟仍在 30 秒内没有响应头；同时直连 `10.30.220.12:11434/api/chat` 4 秒无首包、`/api/ps` 2 秒超时，说明剩余阻塞已不在 8094 进程生命周期，而在共享 Ollama 11434 或其推理队列。证据见 [问答冒烟](../logs/8094_qa_smoke.out.log)。为避免再次占用最终实例，第二次恢复后只验证首页/状态接口，不再发送长问答。本次未重启 Ollama，因为 11434 同时服务 8093 等生产入口，需单独授权。
- 隔离边界：未修改 HTML、数据库、8768、8770、8093 或 Ollama；远端临时恢复脚本执行后自删。

## 2026-07-16 炉况展示名称改为“热制度上行 / 热制度下行”

- 需求：现场页面不再显示“炉热”“炉凉”；统一显示为“热制度上行”“热制度下行”。
- 实现：在 8093 正式页的最终渲染前设置展示映射 `hot→热制度上行`、`cold→热制度下行`，并统一处理诊断排行、趋势图例、建议、证据、告警及问答的可见文本；front2 同步加入同一渲染层映射。
- 边界：只改变前端展示，保留数据库/8768/API/历史快照的 `hot/cold` 键、诊断得分、规则和阈值，避免破坏历史数据与诊断契约。
- 发布：使用 [remote_deploy_8093_core_metric_clip_fix.ps1](../tools/remote_deploy_8093_core_metric_clip_fix.ps1) 暂存上传、备份并仅重启 `BFV4PreviewProxy8093`。远端回退副本为 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_core_metric_clip_20260716_115913`；发布后 HTML SHA256 为 `183EBFE88CE2F985163F313CC8532CD6E2CB9E66455E0490ECA160B39D3C9219`，HTTP 200。
- 验收：使用 [verify_8093_thermal_diagnosis_display.py](../tools/verify_8093_thermal_diagnosis_display.py) 检查真实 8768 数据、旧名称不可见、新名称可见、控制台错误为 0 且无横向溢出。Chromium 9 个固定视口均通过；Firefox、WebKit、Edge 各 4 个代表视口均通过；Chromium 五桌面尺寸 × 五业务路由矩阵全部挂载、无横向溢出，1440×900 总览的一次性资源响应告警已用独立复核通过。报告在 `logs/8093_thermal_diagnosis_display_qa/`。
- 隔离：发布前后 `BFV4PreviewProxy8093=Running`、8093 监听；`BFV4PreviewWs8768=Running`、8768 持续监听，未停止数据桥。

## 2026-07-16 参数优化页移除“建议 / 已接入”状态卡

- 需求：移除参数优化页左侧“当前炉况”中的“建议 / 已接入（或等待中）”状态卡，避免把建议引擎接入状态作为现场炉况信息展示。
- 实现：[frontend_dashboard_v3.server.html:L10416](../高炉前端数据/frontend_dashboard_v3.server.html#L10416) 仅保留“炉况等级、运行状态、异常信号”三项；完整建议引擎仍在中部决策中心及候选处置区使用，未改变 8768 契约、规则或建议生成。
- 远端发布：先上传暂存文件、备份原页面，再仅重启 `BFV4PreviewProxy8093`。回退副本为 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_remove_suggestion_20260716_113112`；当前 HTML SHA256 为 `A69FE16A838CAA306585A49AC5653D8A889662DB18FF5C388192D0B27B289FFC`。
- 验收：Chromium `1366×768` 打开 `http://10.30.220.12:8093/?ws_port=8768#optimization`，三项状态分别为“炉况等级、运行状态、异常信号”；“建议 / 已接入”不存在，页面/控制台错误为 0、无横向溢出。截图：`logs/8093_remove_suggestion_qa/optimization_1366x768.png`。
- 隔离：`BFV4PreviewProxy8093=Running`；8768 未停止。

## 2026-07-15 V4 新版页面独立 8094 预览

- 目的：在不替换 8093 正式预览 HTML 的前提下，供现场查看本机新版页面（含最新引擎文案及参数优化布局）。
- 独立页面：本机 `高炉前端数据/frontend_dashboard_v3.server.html` 已上传为远端 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.8094_preview.server.html`；当前两端 SHA256 均为 `ED39A438AEAF1D44F378B5F381566FC931716539E646636C312324B5C6DC3968`。
- 运行机制：复用已有 `\BlastFurnaceServices\V3AutoPreviewProxy8094` 的常驻托管任务，仅将其动作切换为 V4 专用启动脚本 `tools/run_22012_8094_preview.ps1`；该脚本固定 `BF_PROXY_PORT=8094` 和 `BF_INDEX_FILE=frontend_dashboard_v3.8094_preview.server.html`。原任务 XML 已备份到 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\logs\V3AutoPreviewProxy8094.before_v4_8094_preview.xml`，可用于回退旧 8094 预览。
- 验证：8094 监听 `0.0.0.0:8094`，本机 HTTP 返回 `200`；预览 URL 为 `http://10.30.220.12:8094/?ws_port=8768#optimization`。
- 隔离核验：`BFV4PreviewProxy8093=Running`、8093 保持监听，8093 原 HTML SHA256 仍为 `BD3B4AF81910A6DFC2052C6956FD44551CA89A7377DC73806386B4DCACE3E88A`；本次未替换 8093 HTML，未停止 8768。
- 黑屏修复：浏览器控制台定位到 `bfBaselineDeviationOption()` 的 `rows.map()` 结尾少一个闭合 `)`，Inline Babel 在编译阶段报 `Unexpected token`，故主应用未挂载；已改为正确的 `rows.map(...))` 结构。另清理了旧的 8094 Python 监听进程，避免两个进程同时复用端口并随机返回空响应。Chromium `1366×768` 实测主应用已渲染、参数优化驾驶舱存在、页面脚本错误为 `0`；截图为 `logs/8094_preview_black_screen_diagnosis/fixed_1366x768.png`。

## 2026-07-15 核心变量曲线详情与相关性分析

- 总览“核心指标”每条迷你曲线均改为可聚焦、可点击的只读入口。点击后打开变量详情：可缩放时间趋势、精确时间点数值表、当前/最小/最大/均值/窗口变化，以及 30 分钟、2 小时、8 小时窗口切换。
- 详情内可选择任一其他核心变量；前端用 8768 已下发历史缓冲区的同一分钟有效样本计算 Pearson 相关系数并绘制散点图。该分析不新增接口、不写数据库；页面明确提示“相关不等于因果”。
- 实现标记：`REQ-8093-CORE-SPARK-DETAIL-CORRELATION-20260715`；专项回归：[verify_8093_core_spark_detail.py](../tools/verify_8093_core_spark_detail.py)。本地 Chromium 9 个固定视口、Firefox/WebKit/Edge 各 4 个代表视口，以及 Chromium 五桌面尺寸 × 五业务路由 25 组合均通过，报告目录为 `logs/8093_core_spark_detail_qa/`。
- 发布记录：已替换远端前端 HTML 并仅重启 `BFV4PreviewProxy8093`；`BFV4PreviewWs8768` 未停止，发布前后 8093/8768 均保持监听。备份为 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_core_metric_clip_20260715_081149`；发布后 SHA256 为 `BD3B4AF81910A6DFC2052C6956FD44551CA89A7377DC73806386B4DCACE3E88A`，HTTP 返回 200，9 个发布标记均存在。
- 远端实际数据冒烟：`http://10.30.220.12:8093/?ws_port=8768#overview` 在 Chromium `1366×768` 成功打开“综合顶压趋势与变量关联”弹层、选择 `DP_total`，详情趋势、相关性散点及无横向溢出断言均通过。远端首次加载内联编译较慢，专项检查的入口等待上限已调整为 120 秒，避免将冷启动误判为交互故障。

## 2026-07-15 首页核心变量末行裁切修复

- 现象：总览左侧“核心指标”第一页在中低高度桌面视口会截掉部分工艺分组的末行；现场截图中缺少的是“顶压D、富氧流量、喷煤设定、2#铁口温度参考”。第二页也存在相同的潜在裁切机制。
- 根因：`.core-page-v7-content` 采用纵向 flex，原 `.core-metric-group` 只按 `--rows`（指标行数）分配高度，但每个分组还包含一行固定高度标题；同时分组设置 `overflow:hidden`。短分组得到的高度不足，末行因此被裁掉。1280/1366/1440/1546 的紧凑断点还存在最小行高与可用高度不匹配的问题。
- 页面修复：[frontend_dashboard_v3.server.html](../高炉前端数据/frontend_dashboard_v3.server.html) 使用标记 `BUG-8093-CORE-METRIC-GROUP-CLIP-20260715`，将分组权重改为 `flex:calc(var(--rows) + 1) 1 0`，把标题行计入分配；同时对 `max-height:900px`、`max-width:1366px/max-height:760px` 和 `max-height:680px` 收紧标签、标题、间距及最小行高，短视口隐藏次要说明行，保留两页全部 28 项核心变量。
- 补丁源同步：[patch_22012_8093_core_metric_groups.py](../tools/patch_22012_8093_core_metric_groups.py) 已使用相同 CSS，避免后续重新打补丁时回退旧裁切规则。
- 自动回归：[verify_8093_core_metric_no_clip.py](../tools/verify_8093_core_metric_no_clip.py) 会切换两页并逐组核对行数、末行名称、隐藏状态和 DOM 边界，同时检查正式头部、页面横向溢出、页面错误和控制台错误。
- 本地验收：Chromium 覆盖 9 个固定视口（1280×720、1366×768、1440×900、1546×864、1920×1080、1024×768、768×1024、390×844、375×667），两页 7 组 28 项均无裁切；Firefox、WebKit、Edge 覆盖 1920×1080、1366×768、768×1024、390×844，全部通过。另用 [capture_8093_five_pages_matrix.py](../tools/capture_8093_five_pages_matrix.py) 完成 Chromium 五桌面尺寸 × 总览/炉况诊断/参数优化建议/趋势分析/智能问答，共 25 组合，失败 0。
- 本地报告：`logs/8093_core_metric_no_clip_qa/manifest_chromium_all.json`、`manifest_firefox_representative.json`、`manifest_webkit_representative.json`、`manifest_msedge_representative.json`、`five_pages_local/manifest.json`。
- 远端核对：部署前发现 220.12 目标文件已经与本地修复版完全一致，SHA256 均为 `96DB653415DAA99F007F470F97F92E2D149BB935998F944C63D569885D245B85`；远端 HTTP 200 内容与本地 HTTP 内容一致，并包含修复标记和新权重。因此未重复覆盖、未生成无意义备份、未重启服务。`BFV4PreviewProxy8093` 保持 `Running`，8093 与 8768 均持续监听。
- 远端实测：`http://10.30.220.12:8093/` 的 Chromium 9 视口与 Edge 4 代表视口全部通过，报告位于 `logs/8093_core_metric_no_clip_qa/remote/`。

复测命令：

```powershell
python tools\verify_8093_core_metric_no_clip.py --url "http://127.0.0.1:8093/?ws_port=8768#overview" --browser chromium --matrix all
python tools\verify_8093_core_metric_no_clip.py --url "http://10.30.220.12:8093/?ws_port=8768#overview" --browser chromium --matrix all --out-dir "logs\8093_core_metric_no_clip_qa\remote"
```

## 2026-07-15 核心指标 IQR 状态阈值更新

- 五档口径统一为：`z < -1.5` 极低、`-1.5 <= z < -1` 偏低、`-1 <= z <= 1` 正常、`1 < z <= 1.5` 偏高、`z > 1.5` 极高。
- 同步修改总览核心指标 `iqrStatus()` 与诊断页基线对比 `zBand()`；页面标记为 `REQ-CORE-IQR-BANDS-NEG1-POS1-20260715`。
- 远端文件：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html`；备份：`frontend_dashboard_v3.server.html.bak_core_iqr_thresholds_20260715_013910`。
- 仅重启 `BFV4PreviewProxy8093`，未停止 `BFV4PreviewWs8768`。验收结果：8093/8768 均监听，远端本机 HTTP 200，文件与 HTTP 四项阈值标记检查全部通过，远端文件 SHA256 为 `1184533C39D5D132E12117F55B8CE82FE70B72FE360C7CC5FE28876F46E36DE8`。
- Chromium 实测截图：`logs/core_iqr_threshold_8093_20260715/overview_1366x768.png`、`logs/core_iqr_threshold_8093_20260715/overview_768x1024.png`。

## 2026-07-11 问答 SSE 流式输出加固

- 问答文字链路固定为 `POST /api/qa/chat` 的 SSE；`BFV4PreviewWs8768`/`8768` 只负责炉况实时数据和曲线，不承担问答文字流。
- 8093 页面新增标记 `OPS-8093-QA-SSE-ROBUST-V1`，对 SSE 分块、UTF-8 半包、`start`/`delta`/`final`/`error` 事件和非 SSE 回退统一处理。
- 部署后仅重启 `BFV4PreviewProxy8093`，未停止 `BFV4PreviewWs8768`；HTTP 页面标记、8093 监听和服务状态均已复核。
- 修复脚本：[patch_22012_8093_qa_sse.py](../tools/patch_22012_8093_qa_sse.py)。
- 验证命令：`python PT/test_22012_ollama_qa.ps1 -HostAddress 10.30.220.12 -Model bf-diagnosis-runtime:v1 -Question "请只回复：流式链路正常。"`，并用浏览器 DevTools 确认响应 `Content-Type: text/event-stream`、事件顺序为 `start -> delta* -> final -> done`。

## 当前结论

- `http://10.30.220.12:8093/` 由 Windows 服务 `BFV4PreviewProxy8093` 守护。
- 服务显示名为 `Blast Furnace V4 Preview Proxy 8093`，启动类型为 `Automatic`。
- 8093 当前实际监听进程是 `C:\Program Files\Python311\python.exe`，入口为：
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\智能助手\backend\ollama_proxy_server.py`
- 8093 对应前端文件为：
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html`
- 同组 WebSocket/数据桥接服务 `BFV4PreviewWs8768` 不应因为刷新 8093 页面而一起停止。

## 2026-07-14 8093 模型供应切换口径

- 当前远端状态接口 `GET http://10.30.220.12:8093/api/ollama/status` 返回 `target_model=bf-diagnosis-runtime:v1`、`model_ok=true`。这说明 8093 当前问答代理实际供应模型是 `bf-diagnosis-runtime:v1`，不是 27B 的 `chiqiong-blast-furnace:latest`。
- 远端 Ollama `GET http://10.30.220.12:11434/api/tags` 已确认存在 `chiqiong-blast-furnace:latest`，模型详情为 `parameter_size=27.8B`、`quantization_level=Q4_K_M`；同机也存在 `chiqiong-blast-furnace:latest_s`，digest 相同。
- 8093 模型供应不写在前端 HTML 中；实际由远端服务配置 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\tools\service_configs\22012_BFV4PreviewProxy8093.json` 的 `env.BF_LLM_MODEL` 和 `env.OLLAMA_BASE_URL` 控制。2026-07-14 只读探测结果：`BF_LLM_MODEL=bf-diagnosis-runtime:v1`、`OLLAMA_BASE_URL=http://127.0.0.1:11434`、`BF_PUBLIC_MODEL_NAME=炽穹·高炉炼铁大模型`。
- 切换为 27B 炽穹模型时只允许改 `BFV4PreviewProxy8093` 的服务配置并重启该服务；不要停止 `BFV4PreviewWs8768`，不要替换 8093 HTML，不要改数据库或 8777/8096 等其它服务。
- 本地已提供只读探测脚本：[probe_8093_model_service.ps1](../tools/probe_8093_model_service.ps1)、[probe_8093_model_config.ps1](../tools/probe_8093_model_config.ps1)，以及受控切换脚本：[switch_8093_model_to_chiqiong27b.ps1](../tools/switch_8093_model_to_chiqiong27b.ps1)。切换脚本会先用 `/api/show` 验证 27B 模型存在，备份 JSON，再设置 `BF_LLM_MODEL=chiqiong-blast-furnace:latest` 与 `OLLAMA_BASE_URL=http://127.0.0.1:11434`，最后只重启 `BFV4PreviewProxy8093` 并校验 `/api/ollama/status`。
- 2026-07-14 二次修复：首次切换失败的根因是服务配置 `env.BF_LLM_MODEL` 已改为 27B，但 `arguments` 仍包含 `--model bf-diagnosis-runtime:v1 --public-model bf-diagnosis-runtime:v1`，并且旧的 8093 子进程未被普通 `Restart-Service` 清掉。已将 [switch_8093_model_to_chiqiong27b.ps1](../tools/switch_8093_model_to_chiqiong27b.ps1) 修正为同时更新 `arguments`、停止 `BFV4PreviewProxy8093`、只清理命令行含 `V4_8093_PREVIEW` 的 8093 监听进程，再启动服务。当前运行态父进程命令行为 `start_v3_8092_python.py ... --model chiqiong-blast-furnace:latest --public-model 炽穹·高炉炼铁大模型 --port 8093`。
- 注意：`/api/ollama/status` 的 `target_model` 字段由后端返回 `BF_PUBLIC_MODEL_NAME`，当前显示 `炽穹·高炉炼铁大模型` 是公开名；底层实际 Ollama 模型需以父进程 `--model chiqiong-blast-furnace:latest`、服务配置 `arguments/env.BF_LLM_MODEL` 和状态接口 `model_ok=true` 共同验收。

执行切换命令：

```powershell
python tools\remote_22012_exec.py --allow-agents-password --workdir "F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW" --timeout 240 --script "tools\switch_8093_model_to_chiqiong27b.ps1"
```

切换后复核：

```powershell
curl.exe --max-time 20 -s "http://10.30.220.12:8093/api/ollama/status"
curl.exe --max-time 20 -s "http://10.30.220.12:11434/api/tags"
```

成功信号：8093 状态接口返回 `ok=true`、`proxy_ok=true`、`ollama_ok=true`、`model_ok=true`，`target_model=炽穹·高炉炼铁大模型`，且运行父进程或服务配置中存在 `--model chiqiong-blast-furnace:latest`。若切换后异常，用切换脚本输出的 `BackupPath` 恢复 `22012_BFV4PreviewProxy8093.json`，再只重启 `BFV4PreviewProxy8093`。

## 2026-07-14 8093 MCP 问答链路复核

本次复核用户反馈的“8093 大模型多次没有成功调用 MCP 查询数据库再回答”。结论需分工具类型看：

- 端口层：本机到 `10.30.220.12` 的 `8093`、`8768`、`8092`、`11434`、`22` 均可连；复核过程中曾出现 `8093/8768` 短时 TCP 不通，随后恢复，远端服务视角仍为 `BFV4PreviewProxy8093=Running`、`BFV4PreviewWs8768=Running` 且本机监听存在。
- 知识库状态：`GET http://10.30.220.12:8093/api/qa/knowledge/pgvector/status` 返回 `ok=true`、`chunk_count=5521`、`embedding_count=5521`、`index=hnsw`。
- 最新值查询：`POST /api/qa/chat`，问题“请调用MCP工具查询T_top当前最新值，并说明数据时间。”，返回 `mcp_tool_calling=true`、`mcp_prefetch.used=true`、`mcp_tool_trace=[get_latest_gl02_value]`，说明 PostgreSQL 最新值 MCP 可用。
- 统计查询：问题“T_top最近30分钟平均值、最大值、最小值是多少？请根据数据库回答。”，返回 `mcp_prefetch.kind=statistics`，但 `mcp_tool_trace=[]`。这是确定性预取 `qa_mcp_prefetch` 已查库并注入上下文，不一定产生通用 `tool_calls` trace，不能据此判断未查库。
- 炉况快照：问题“当前炉况怎么样？请调用MCP查询数据库后回答。”，返回 `mcp_tool_trace=[get_latest_furnace_snapshot]`，说明炉况快照 MCP 可用。
- 趋势图工具异常：问题“请调用MCP工具画出最近1小时炉顶温度和顶压的趋势对比图，使用归一化对比，并告诉我图片路径。”，SSE 事件顺序为 `start -> tool_result(query_gl02_statistics) -> tool_start(plot_gl02_trends)`，之后超过 220 秒没有返回 `tool_result`；非流式请求 160-300 秒超时。远端直接 import 调用 `bf_data_mcp_server.py` 的 `plot_gl02_trends()`，`database` 与默认 `auto` 均可在约 0.25-1.24 秒内生成图片；但通过 MCP stdio `session.call_tool("plot_gl02_trends", ...)` 会超过 90 秒不返回。根因定位为 `plot_gl02_trends` 在 MCP stdio 协议执行路径卡住，不是 PostgreSQL、pSpace 兜底或绘图函数本体不可用。

当前影响面：

- 数据类问答不是整体失效；最新值、统计预取和炉况快照可用。
- 页面上“是否调用 MCP”不能只看回答文本；需要看 `mcp_prefetch`、`mcp_tool_calling`、`mcp_tool_trace` 与 SSE 的 `tool_start/tool_result`。
- 明确失败的是“画图/趋势图”类 MCP 工具调用：前端会看到长时间生成中或请求超时。

已修复：

- `plot_gl02_trends` 已在 `高炉前端数据\智能助手\mcp\bf_data_mcp_server.py` 中改为默认通过隔离绘图 worker 子进程生成 PNG。MCP stdio 主服务只负责接收工具调用和返回轻量 JSON，避免 matplotlib 字体扫描、缺字告警或渲染状态堵塞 MCP 协议进程。可用开关：`BF_MCP_PLOT_SUBPROCESS=0` 可回退到进程内绘图；`BF_MCP_PLOT_TIMEOUT_SECONDS` 控制 worker 超时，默认 45 秒。
- 8093 代理 `高炉前端数据\智能助手\backend\ollama_proxy_server.py` 已增加 `append_mcp_chart_links()` 兜底：若 `mcp_tool_trace` 中有 `/data/mcp_charts/*.png`，但模型最终回答没有保留正确链接，则自动追加 `![MCP趋势图](...)`，避免模型把 `mcp_charts` 翻译成“数据库查询_charts”后页面无法自动渲图。
- 远端备份：`bf_data_mcp_server.py.bak_plot_stdio_fix_20260714_210034`、`bf_data_mcp_server.py.bak_plot_stdio_fix_20260714_211024`、`ollama_proxy_server.py.bak_chart_link_fix_20260714_211921`。
- 部署边界：只重启 `BFV4PreviewProxy8093`；`BFV4PreviewWs8768` 全程保持运行。
- 验收：MCP stdio `session.call_tool("plot_gl02_trends", ...)` 已返回 `ok=true`、`image_url=/data/mcp_charts/gl02_trend_20260714_211103_13798266.png`；8093 `/api/qa/chat` 真实问答返回 `mcp_tool_trace[0].tool=plot_gl02_trends`、`ok=true`、`image_url=/data/mcp_charts/gl02_trend_20260714_212043_77095ff8.png`，最终答案包含正确图片 Markdown。

## 2026-07-14 8093 MCP 口语调用准确率第一阶段实验

- 需求：支持现场口语表达直接触发 MCP 数据查询，例如“告诉我一下炉顶的温度”“给我说一下炉顶的压力”，不要求用户说出 `T_top`、`P_top` 或“调用 MCP”。
- 修改范围：
  - `高炉前端数据\智能助手\backend\ollama_proxy_server.py`：新增 `normalize_spoken_question()`、`SPOKEN_MCP_VARIABLE_ALIASES`，扩展 `QA_MCP_INTENT_KEYWORDS`、`qa_mcp_variable()`、`qa_mcp_prefetch()`、`qa_mcp_should_use_tools()`。
  - `高炉前端数据\智能助手\mcp\bf_data_mcp_server.py`：为变量检索、最新值、统计、趋势图工具补充口语适用场景说明。
- 部署边界：只重启 `BFV4PreviewProxy8093`；`BFV4PreviewWs8768` 未停止。
- 远端最终备份：`ollama_proxy_server.py.bak_spoken_mcp_aliases_20260714_214636`、`bf_data_mcp_server.py.bak_spoken_mcp_aliases_20260714_214636`。
- 本地验证：`python -m py_compile` 通过；15 条口语解析样例命中预期变量；假 MCP 数据模块验证 `latest/statistics` 预取路径通过。
- 8093 真实接口复测：
  - “告诉我一下炉顶的温度”返回 `mcp_tool_calling=true`、`mcp_prefetch.used=true`、`mcp_prefetch.kind=latest`、`mcp_prefetch.variable=T_top`。
  - “给我说一下炉顶的压力”返回 `mcp_tool_calling=true`、`mcp_prefetch.used=true`、`mcp_prefetch.kind=latest`、`mcp_prefetch.variable=P_top`。
  - “最近半小时炉顶压力有没有波动”返回 `mcp_tool_calling=true`、`mcp_prefetch.used=true`、`mcp_prefetch.kind=statistics`、`mcp_prefetch.variable=P_top`。
- 验收提醒：最新值/统计类问题常由 `qa_mcp_prefetch()` 直接查库并注入上下文，可能没有 `mcp_tool_trace`；判断是否已查 MCP 数据，应同时看 `mcp_prefetch`、`mcp_tool_calling`、`mcp_tool_trace` 和最终回答中的数据时间。

## 2026-07-14 诊断页基线标题修补

- 需求：8093 诊断页右下“基线对比”面板只保留标题“基线对比”，去除括号说明；颜色规则不改，仍由 `zBand()` 根据当前值相对历史基线的 `z` 偏离判定。
- 执行脚本：[remote_patch_8093_baseline_title.ps1](../tools/remote_patch_8093_baseline_title.ps1)。
- 操作边界：只重启 `BFV4PreviewProxy8093`，不停止 `BFV4PreviewWs8768`。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_baseline_title_20260714_175837`。
- 部署后文件 SHA256：`650F4F80968A984720C7431C1642197B82FE0E3632E3D92267607DA28394BAE7`。
- 验证结果：`BFV4PreviewProxy8093=Running`，`8093=true`，`8768=true`，远端本机 HTTP `200`，`HttpHasNewTitle=true`，`HttpHasOldTitle=false`。
- 本机外部复核：访问 `http://10.30.220.12:8093/?t=baseline-title-local-check-20260714#diagnosis`，返回 `HasNewTitle=true`、`HasOldTitle=false`。

## 2026-07-11 参数优化工作台替换

- 操作边界：只停止 `BFV4PreviewProxy8093`，不停止 `BFV4PreviewWs8768`。
- 标准流程：校验待部署页关键标记 → 停止 8093 服务并确认端口释放 → 确认 8768 仍监听 → 时间戳备份 → 替换 HTML → 启动 8093 → 验证端口、HTTP 200 与页面标记。
- 自动化入口：[remote_deploy_8093_optimization_workbench.ps1:TODO-LINES](../tools/remote_deploy_8093_optimization_workbench.ps1)。
- 首次部署备份：`frontend_dashboard_v3.server.html.bak_optimization_workbench_20260711_114146`；部署后服务为 `Running`，8093/8768 均监听。
- 平板宽度修复后二次受控部署备份：`frontend_dashboard_v3.server.html.bak_optimization_workbench_20260711_120349`；最终 HTML SHA256：`11040816BB3B8307123AF28778F26C9DA71C1D67551AB10832B3701B98262F85`，HTTP 200。
- 2026-07-11 1280×720 桌面断点修复：将主页面 `max-width:1280px` 临界断点收紧为 `1279px`，避免 1280px 错进平板单列。远端备份 `frontend_dashboard_v3.server.html.bak_optimization_workbench_20260711_163355`，部署后 `BFV4PreviewProxy8093=Running`、8093/8768 均监听、HTTP 200，最终 HTML SHA256 为 `05CD7DE62A5C09B7A2254ABEDFC360A736D002F68B2BF131FD6F52623D070F9F`。
- 2026-07-11 当前建议详情裁切修复：在 1280–1500 的紧凑桌面档，优先保留“判断依据／预期作用／现场确认”完整正文，隐藏次级“本轮复查”以释放高度；远端备份 `frontend_dashboard_v3.server.html.bak_optimization_workbench_20260711_164101`，最终 HTML SHA256 为 `5DAC47DD61A19235BD1B2ADFC316443CB2C497DD850A33AECCEE3C1327C15657`。

## 受控刷新 8093 代理

### 2026-07-14 诊断得分单列布局同步

- 本地页面已同步到8093暂存文件，并按受控部署流程替换远端页面；远端自动生成了带时间戳的备份。
- 仅重启 `BFV4PreviewProxy8093`，未停止或修改 `BFV4PreviewWs8768`。
- HTTP `200` 验证通过，远端返回内容与本地页面 SHA256 一致：`8395890846E07BEDAA76B72A8BB95BF83006FB6319F8E4C99B0765CD356E65CE`。
- 页面标记验证通过：`REQ-DIAG-SCORE-SPLIT-20260714`、`正常顺行炉况`、`异常炉况`、单列7项异常网格及白色字体样式均存在。
- 期间8093服务首次启动返回时序性失败，随后复核服务已为 `Running`，HTTP可访问；8768保持运行。

只重启 8093 代理，不动 8768、8777、8096 等其它 V4 守护：

```powershell
Restart-Service -Name 'BFV4PreviewProxy8093' -Force
Start-Sleep -Seconds 6
Get-Service -Name 'BFV4PreviewProxy8093'
Get-NetTCPConnection -LocalPort 8093 -State Listen
```

## 临时停止 8093 代理

只在排障窗口内使用。停止后 `http://10.30.220.12:8093/` 会不可访问：

```powershell
Stop-Service -Name 'BFV4PreviewProxy8093' -Force
Get-Service -Name 'BFV4PreviewProxy8093'
```

恢复：

```powershell
Start-Service -Name 'BFV4PreviewProxy8093'
Start-Sleep -Seconds 6
Get-Service -Name 'BFV4PreviewProxy8093'
```

## 验证新页面是否生效

从本机验证时，缓存参数必须放在 hash 前面：

```powershell
curl.exe --max-time 45 -s "http://10.30.220.12:8093/?t=manual-refresh#overview"
```

当前 2026-07-09 验收关键字：

- `CORE_METRIC_GROUPS`
- `core-grouped-metrics`
- `OPS-8093-FURNACE-LAYER-CALLOUTS`
- `furnace-layer-callouts`
- `顶压 / 热制度 / 送风 / 压差`
- `furnace-stage-3d .furnace-callout-3d`

注意：只看到 `CORE_METRIC_GROUPS` / `core-grouped-metrics` 还不够。2026-07-09 复核发现，8093 返回页曾经同时存在分组版 `MetricRows` 和更靠后的旧版 `MetricRows=function({buf}){...compact-core-metrics...}`，浏览器最终执行后者，导致左侧仍显示旧 28 行紧凑核心指标。必须确认最后生效的 `MetricRows` 是分组版。

远端文件级验证：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File "F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\tools\verify_8093_v4_file_enabled.ps1"
```

合格信号：

- `EffectiveCoreGroups` 为 `true`
- `LastGroupedMetricRowsIndex` 大于 `LastCompactMetricRowsIndex`
- `HasFurnaceLayerCallouts` 为 `true`
- HTTP 返回内容哈希与刚部署的目标文件一致

## 2026-07-09 核心变量分组部署记录

本次确认：`frontend_dashboard_v3.server.html` 文件本身没有单独的系统守护或文件锁；被 Windows 自动守护的是 8093 代理服务 `BFV4PreviewProxy8093`。按用户授权执行时，采用“停止 8093 服务 -> 修改 HTML -> 启动 8093 服务”的顺序；`BFV4PreviewWs8768` 数据桥保持运行。

执行入口：

```powershell
python tools\remote_22012_exec.py --allow-agents-password --workdir "F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW" --timeout 300 --upload "tools\patch_22012_8093_core_metric_groups.py=F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\tools\patch_22012_8093_core_metric_groups.py" --upload "tools\verify_8093_v4_file_enabled.ps1=F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\tools\verify_8093_v4_file_enabled.ps1" --script "tools\remote_deploy_8093_core_groups.ps1"
```

远端备份：

- `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_120345`
- `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_121131`

最终部署结果：

- 8093 文件 SHA256：`A4020DCD91ED1202B53BE52E71632D8D656D6FD0ABBDB436A178704207811B08`
- 8093 HTTP SHA256：`B1A58557AE955C3AE8AD758E50D414448EBA85E0BBFD32DC192C2BCD036D4831`
- `BFV4PreviewProxy8093`：`Running`
- 8093 监听 PID：`6536`
- 8768 监听：`true`
- 文件 `EffectiveCoreGroups=true`，`LastCompactMetricRowsIndex=403243`，`LastGroupedMetricRowsIndex=406116`
- HTTP `EffectiveCoreGroups=true`，`HttpLastCompactMetricRowsIndex=425276`，`HttpLastGroupedMetricRowsIndex=428149`

注意：首次部署后 Playwright 发现总览页运行时报错 `baselineMap is not defined`，原因是远端 V4 页面不存在本地分组行组件依赖的 `baselineMap/quantileStatus/scaledQ`。最终补丁改为 `OPS-8093-CORE-GROUPS-FINAL-SAFE`，覆盖 `CoreMetricRow` 与 `MetricRows`，只依赖远端已有的 `latest/rollingIqrDeviation/iqrStatus/displayNum/displayUnit/sparkOption`，避免该运行时错误。

补充说明：`baselineMap/quantileStatus/scaledQ` 只属于一版中间态核心指标行 UI 辅助逻辑，不是 8768 WebSocket、PostgreSQL 数据、诊断结果或趋势曲线的数据来源。当前稳定页面改用 `rollingIqrDeviation/iqrStatus` 做左侧行状态，影响范围只限左侧核心指标行的状态标签算法；诊断、趋势、问答上下文和七工艺分层实时值不受影响。远端 HTML 因多次追加热修补，可能仍保留早期未执行的 `baselineMap(...)` 字符串；浏览器最终执行的是更靠后的 safe override。

## 2026-07-09 核心变量小趋势线调优

用户反馈左侧核心变量小趋势线趋势不明显、线条略粗。复核当前实现后确认：原始 `sparkOption(b,id)` 使用 `lineOption(b,[id],60)`，即最近 60 个 1 分钟点，线宽继承通用默认 `2.4px`。在 11-12px 高的小图中，60 分钟窗口会压缩短时斜率，`2.4px` 线宽也会让细微波动显得发糊。

本次远端补丁：

- 标记：`OPS-8093-CORE-SPARK-30MIN-THIN-V2`
- 小趋势窗口：`CORE_SPARK_MINUTES=30`
- 小趋势线宽：`CORE_SPARK_LINE_WIDTH=1.45`
- 小趋势槽位：桌面约 `60px`，1366 宽度下约 `52px`
- 备份文件：
  - `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_122726`
  - `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_123037`
- 最终文件 SHA256：`64FEE7F3D8479EA0B297ED2B82D4DA19399FAEC54A5C99476EFDCBB98484A264`
- 最终 HTTP SHA256：`F1A0EB45EE99F94C9CA2EF003B73A61690A53D93DD99749137D620275F00D9D0`
- `BFV4PreviewProxy8093`：`Running`
- 8093 监听 PID：`12820`
- 8768 监听：`true`

若浏览器仍显示旧页面，优先使用 `Ctrl + F5` 或打开：

`http://10.30.220.12:8093/?t=20260709-coregroups#overview`

## 注意事项

- 不要把“退出系统守护”理解为关闭全部 V4 服务。通常只需要重启 `BFV4PreviewProxy8093`。
- 8093 是 V4 预览服务，8092 是另一套 V3/基线服务，二者读取的目录不同。
- 修改 8093 页面后，先验证 HTTP 返回内容，再判断是否需要重启服务。

## 2026-07-12 知识库答案偏差与问答延迟核查

本次对 8093 最近会话 `qa_1783740664716_49fed650` 做了只读复核。该会话最近 6 轮从用户提问到助手落库的间隔为 10.68–15.11 秒（“管道行程”一轮为 15.11 秒），与页面约 10 秒后才看到回复的现象一致。

### 检索证据

- `GET /api/qa/knowledge/pgvector/status` 返回 `embedding_count=56`、`chunk_count=56`，`vector_enabled=true`；但 HNSW 未创建，原因是 `column does not have dimensions`，当前是小规模向量顺序扫描，不是索引检索。
- 更新前事故文档 `bf_accident_20260711` 只有 4 个约 900 字符的 chunk。题目和答案被跨题目合并：`chunk_003` 末尾只有“十四、下密打不开的原因？”题目，答案位于 `chunk_004` 开头；因此该问题的关键词检索只返回 `chunk_003`，模型没有拿到对应答案，只能用通用工艺常识补写，形成截图中的气源、电磁阀、PLC 等未在原文出现的内容。
- 更新前“炉顶不能上料”检索的 `chunk_001` 同时包含吹管烧穿、炉顶不能上料、铁口过浅、停水等多个问答；混合检索还会把相邻的“炉顶蓬料” `chunk_002` 排到前面，所以回答会混入蓬料确认、350/450℃、料流阀等另一题内容。
- 更新前“调压阀组”混合检索会同时命中 `chunk_002` 和“煤气快切阀”所在的 `chunk_004`，模型会做压缩式归纳，容易漏掉四个阀的完整职责和 TRT 故障时的顺序。

### 当前生成链路

`POST /api/qa/chat` 进入 [prepare_qa_chat](../高炉前端数据/智能助手/backend/ollama_proxy_server.py#L5039)，在 [qa_search_knowledge](../高炉前端数据/智能助手/backend/ollama_proxy_server.py#L3485) 中按默认 `hybrid`、`top_k=6` 检索；[search_knowledge](../高炉前端数据/智能助手/backend/bf_knowledge_rag.py#L629) 合并关键词和 pgvector 候选后由 [rank_rows](../高炉前端数据/智能助手/backend/bf_knowledge_rag.py#L772) 排序，再由 [evidence_pack_text](../高炉前端数据/智能助手/backend/bf_knowledge_rag.py#L848) 拼入系统提示词。提示词要求“优先依据证据”，但没有要求逐题逐条引用或在证据缺失时停止生成，因此跨题大块和缺答案块会被模型自由概括。

### 延迟与流式表现

[handle_qa_chat_stream](../高炉前端数据/智能助手/backend/ollama_proxy_server.py#L5237) 先完整执行 `prepare_qa_chat`，然后才发送 HTTP/SSE 响应和 `start` 事件（同文件约 L5244–L5259）。这意味着数据库快照、8 小时趋势摘要、项目上下文、混合检索和可能的 MCP 预查期间，前端收不到任何流式事件。单独测得知识检索通常约 0.1–1.0 秒，首次向量调用约 2–6 秒；模型当前已由 `ollama ps` 确认 100% GPU 常驻，最近会话的 10–15 秒主要还包含较大的上下文预填充和最多 900 token 的生成。

### 2026-07-12 一题一块重建并同步 8093

- [import_accident_docx_native.py](../tools/import_accident_docx_native.py) 已改为按题目/答案成对解析，并强制校验 15 条；本机原生 PostgreSQL 16 重建结果为 `documents=1`、`chunks=15`、`embeddings=15`。
- 8093 同步前已生成可逆备份：`C:\Windows\Temp\rag_before_remove_accident_20260712_4chunks.dump`；远端旧 4 块已删除，新 15 块及 15 个向量已导入。[restore_accident_kb_remote.ps1](../tools/restore_accident_kb_remote.ps1) 已先清理当前事故文档再恢复备份，避免主键冲突。
- 15 条题目的关键词检索全部命中对应 chunk_001–chunk_015；混合检索 15/15 的第一名也均为对应题目 chunk。特别是“调压阀组”现在第一名为 `chunk_008`，“下密打不开”第一名为 `chunk_014`，“煤气快切阀”第一名为 `chunk_015`。
- 重新切块只修复证据粒度和召回定位；SSE 首事件延迟、模型自由补充和 pgvector HNSW 未建索引仍属于后续独立优化项。

## 2026-07-09 核心指标密度与小趋势 V3

用户复核后指出左侧核心指标仍有留白、小趋势线不够明显。本次远端补丁继续保持最近 30 分钟窗口，但把小趋势改为每个变量按自身最近窗口自动收紧 y 轴范围，并扩大趋势槽位：

- 标记：`OPS-8093-CORE-DENSITY-SPARK-V3`
- 小趋势窗口：`CORE_SPARK_MINUTES=30`
- 小趋势线宽：`CORE_SPARK_LINE_WIDTH=1.55`
- 曲线策略：`sparkOption()` 不再直接复用通用大图 y 轴，而是按当前变量最近 30 点计算 `lo/hi/minSpan/pad`，提高小图中细微波动可见度。
- 左侧核心行：隐藏单位列和偏离列，保留序号、名称、数值、状态、小趋势线。
- 1366 宽度下左侧面板从约 `391.5px` 收到约 `364.5px`；第一条小趋势槽位从约 `52px` 增至约 `72px`。
- 1920 宽度下第一条小趋势槽位约 `86px`。
- 中间炉体画布宽度约为炉体面板宽度的 `50%`，减少标注层周围空白。
- 备份文件：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_125142`
- 最终文件 SHA256：`5A350B593BA97088E27EF1B0F0A86002A1967EE22895F54A9864A280A6B38788`
- 最终 HTTP SHA256：`4B80177F21C3AD44D886CF7DE459AC1E8F8F69F4B9A80AE11DC491613D4F18C4`
- `BFV4PreviewProxy8093`：`Running`
- 8093 监听 PID：`15916`
- 8768 监听：`true`

Playwright 验收：

- URL：`http://10.30.220.12:8093/?t=playwright-density-spark-v3-20260709-1253#overview`
- `1366x768`：`CORE_SPARK_MINUTES=30`，`CORE_SPARK_LINE_WIDTH=1.55`，第一条小趋势数据点 `30`，线宽 `1.55`，`smooth=false`，趋势槽 `72x14px`，控制台错误 `0`，页面错误 `0`，面板溢出 `0`，横向溢出 `0`。
- `1920x1080`：第一条小趋势数据点 `30`，趋势槽 `86x15px`，控制台错误 `0`，页面错误 `0`，面板溢出 `0`，横向溢出 `0`。
- 截图：`logs/8093_coregroups_playwright/overview_1366x768_density_spark_v3.png`、`logs/8093_coregroups_playwright/overview_1920x1080_density_spark_v3.png`

## 2026-07-09 炉体七工艺分层标注

用户参考“高炉现场模型”图，希望总览炉体区域也按七类工艺分层展示实时值。该七类与本项目固定的核心 28 变量工艺分层一致，口径如下：

- 煤气顶压：`P_top`、`P_top_gas_A`、`P_top_gas_B`、`P_top_gas_C`、`P_top_gas_D`
- 热制度：`GasUtil`、`TFT`、`T_blast`、`T_top_A`、`T_top_B`、`T_top_C`、`T_top_D`
- 送风供氧：`Q_blast`、`P_blast_cold`、`P_blast`、`O2_rate`、`Q_O2`
- 压差透气：`PI`、`DP_upper`、`DP_lower`、`DP_total`
- 料线：`L`、`L_south`、`L_north`
- 喷煤：`PCI_rate`、`PCI_set`
- 出铁口温度：`T_taphole_1`、`T_taphole_2`

实现位置：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html](../高炉前端数据/frontend_dashboard_v3.server.html)
- 远端热修补脚本：[tools/patch_22012_8093_core_metric_groups.py](../tools/patch_22012_8093_core_metric_groups.py)
- 文件验证脚本：[tools/verify_8093_v4_file_enabled.ps1](../tools/verify_8093_v4_file_enabled.ps1)
- 远端部署脚本：[tools/remote_deploy_8093_core_groups.ps1](../tools/remote_deploy_8093_core_groups.ps1)

本次远端补丁：

- 标记：`OPS-8093-FURNACE-LAYER-CALLOUTS`
- 组件：`FURNACE_LAYER_GROUPS`、`FurnaceLayerCallouts`、`FurnaceLayerCard`
- 行为：七个标注卡片绑定 `latest(buf,id)` 实时值，使用 `displayNum/displayUnit` 保持煤气利用率百分数和单位一致；不新增静态假数据。
- 视觉：三维炉体收在中轴区域，七类卡片分布在左右两侧，黄色引线连接炉体区域。
- 备份文件：
  - `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_123845`
  - `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_124254`
- 最终文件 SHA256：`0692F18764BEA4561BD88042E028E5438BBBB5943C799BC7BE4A94477D424305`
- 最终 HTTP SHA256：`55C5F7B105CCCE5EA6DCD8A43B0F4D8DCEAF1DE16927499E74F57D1D503A9CD0`
- `BFV4PreviewProxy8093`：`Running`
- 8093 监听 PID：`3960`
- 8768 监听：`true`

浏览器验收信号：

- URL：`http://10.30.220.12:8093/?t=playwright-layer-callouts-final-20260709-1244#overview`
- `1920x1080` 与 `1366x768` 均为 `.furnace-layer-card=7`
- 行数为 `5 / 4 / 2 / 3 / 7 / 5 / 2`
- `.core-grouped-metrics=1`，`.compact-core-metrics=0`
- CAD 模型状态 `loaded`，传感器节点 `115`，映射节点 `115`
- 控制台错误 `0`，页面错误 `0`，标注卡片溢出 `0`，面板溢出 `0`
- 截图：`logs/8093_coregroups_playwright/overview_1920x1080_furnace_layer_callouts_final.png`、`logs/8093_coregroups_playwright/overview_1366x768_furnace_layer_callouts_final.png`

## 2026-07-09 诊断正常态颜色极性修正

用户指出右上角“自动诊断值守”中 `正常顺行` 不应显示为红色。项目颜色极性调整为：主诊断为 `normal` / `正常顺行` / `正常运行` 时固定绿色；`暂无明显并发` 固定绿色；只有 `cold/hot/lowline/edge/center/channel/column` 等异常炉况及其中文标签允许使用黄/红。

实现位置：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html](../高炉前端数据/frontend_dashboard_v3.server.html)
- 远端热修补脚本：[tools/patch_22012_8093_core_metric_groups.py](../tools/patch_22012_8093_core_metric_groups.py)
- 文件验证脚本：[tools/verify_8093_v4_file_enabled.ps1](../tools/verify_8093_v4_file_enabled.ps1)
- 远端部署脚本：[tools/remote_deploy_8093_core_groups.ps1](../tools/remote_deploy_8093_core_groups.ps1)

本次远端补丁：

- 标记：`OPS-8093-DIAGNOSIS-NORMAL-GREEN`
- 覆盖组件：`DiagnosisSummary`、`DiagnosisConclusion`
- 浮层兜底：对 `#bf-auto-monitor` 做 DOM 颜色巡检；当前浮层实际字段为 `炉况标签 normal / score` 时，`normal / score` 也会被标为绿色。
- 备份文件：
  - `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_130056`
  - `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_130609`
- 最终文件 SHA256：`3046DCA404358BCB91B159E530CBE84D9A46F5A34548AAFDA8112B4DA6ED7083`
- 最终 HTTP SHA256：`6B018D911F69384FF6B569A3A7355F4EF0EE743C64D063103070A3F250769B02`
- `BFV4PreviewProxy8093`：`Running`
- 8093 监听 PID：`4156`
- 8768 监听：`true`

验证结果：

- HTTP：`http://10.30.220.12:8093/?t=final-diagnosis-normal-green-hash-20260709#overview` 返回 `200`，包含 `OPS-8093-DIAGNOSIS-NORMAL-GREEN` 与 `^normal(?:\b|\s*\/)`。
- Playwright：`http://10.30.220.12:8093/?t=playwright-diagnosis-normal-green-v2-20260709#overview`
- 总览右侧 `正常顺行` 计算色：`rgb(56, 228, 122)`
- 总览右侧 `暂无明显并发` 计算色：`rgb(56, 228, 122)`
- 自动值守浮层当前 `normal / 30.62` 计算色：`rgb(56, 228, 122)`
- 截图：`logs/8093_coregroups_playwright/overview_1366x768_diagnosis_normal_green_v2.png`

操作备注：第二次升级时 `Start-Service` 返回过一次启动失败，但随后复核 `BFV4PreviewProxy8093` 为 `Running`，8093 端口已由 Python 进程监听。该现象判断为 NSSM 启动返回时序问题，非最终服务失败；后续以服务状态、端口监听和 HTTP 200 为准。

## 2026-07-09 炉体工艺标注点位锚定 V2

用户指出七个工艺分层标注高度不准确，希望缩小高炉本体，并将 `料线`、`出铁口温度` 等标注贴近真实点位。本次将原先“左右按高度均匀摆卡片”的样式升级为“卡片 + SVG 引线 + 炉体点位锚点”。

实现位置：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html](../高炉前端数据/frontend_dashboard_v3.server.html)
- 远端热修补脚本：[tools/patch_22012_8093_core_metric_groups.py](../tools/patch_22012_8093_core_metric_groups.py)
- 文件验证脚本：[tools/verify_8093_v4_file_enabled.ps1](../tools/verify_8093_v4_file_enabled.ps1)
- 远端部署脚本：[tools/remote_deploy_8093_core_groups.ps1](../tools/remote_deploy_8093_core_groups.ps1)

本次远端补丁：

- 标记：`OPS-8093-FURNACE-POINT-ANCHORS-V2`
- 炉体缩放：`.cad-furnace-viewer` 收到舞台宽度约 `30%~32%`，高度约 `82%~84%`，避免高炉本体过大挤压点位标注。
- 点位口径：
  - `料线` 锚点放在炉顶料线/探尺区域。
  - `出铁口温度` 放到底部炉缸区域，使用两个锚点并显示 `1#`、`2#`。
  - `煤气顶压`、`热制度`、`压差透气`、`送风供氧`、`喷煤` 分别锚定到炉顶、炉身热制度区、炉身压差区、风口/送风区、喷煤/风口下部区域。
- 备份文件：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_131309`
- 最终文件 SHA256：`F9E4EC9A9EB43DF12CC4D83E422820FA122C2935290871BF957E43F9F1DF4FAC`
- 最终 HTTP SHA256：`E027A185CB4194D35651782F3FA44058750C1E9507FB9F9363487D6AD06DACA7`
- `BFV4PreviewProxy8093`：`Running`
- 8093 监听 PID：`18224`
- 8768 监听：`true`

Playwright 验收：

- URL：`http://10.30.220.12:8093/?t=playwright-furnace-point-anchors-v2-1366x768-20260709#overview`
- `1366x768`：`.furnace-layer-card=7`，`line=8`，`pin=8`，锚点标签 `1#`、`2#` 存在；CAD 状态 `loaded`，传感器节点 `115`；炉体画布宽度占舞台 `0.319`，高度占舞台 `0.817`；卡片重叠 `0`，越界 `0`，横向溢出 `false`。
- `1920x1080`：`.furnace-layer-card=7`，`line=8`，`pin=8`，锚点标签 `1#`、`2#` 存在；炉体画布宽度占舞台 `0.299`，高度占舞台 `0.838`；卡片重叠 `0`，越界 `0`，横向溢出 `false`。
- 截图：`logs/8093_coregroups_playwright/overview_1366x768_furnace_point_anchors_v2.png`、`logs/8093_coregroups_playwright/overview_1920x1080_furnace_point_anchors_v2.png`

## 2026-07-09 炉体工艺标注跟随模型 V3

用户进一步指出 V2 仍然偏“硬标注”：标注浮层看起来贴近点位，但没有真正跟随 Three.js 高炉本体。V3 将七个数据浮层升级为“模型对象投影跟随”：从 `window.__BF_CAD_FURNACE_VIEWER.hitObjects/sensorObjects` 读取实际传感器对象，用当前相机把 `object.position` 投影到页面坐标，并在 `requestAnimationFrame` 循环中同步 SVG 引线、黄色锚点、`1#`/`2#` 标签和卡片纵向位置。

实现位置：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html:L1675-L1712](../高炉前端数据/frontend_dashboard_v3.server.html#L1675-L1712)
- 远端热修补脚本：[tools/patch_22012_8093_core_metric_groups.py:L243-L284](../tools/patch_22012_8093_core_metric_groups.py#L243-L284)
- 文件验证脚本：[tools/verify_8093_v4_file_enabled.ps1:L43-L44](../tools/verify_8093_v4_file_enabled.ps1#L43-L44)
- 远端部署脚本：[tools/remote_deploy_8093_core_groups.ps1:L92-L172](../tools/remote_deploy_8093_core_groups.ps1#L92-L172)

本次远端补丁：

- 标记：`OPS-8093-FURNACE-FOLLOW-ANCHORS-V3`
- 同步函数：`window.__BF_FURNACE_FOLLOW_ANCHORS_SYNC__`
- 点位来源：
  - `料线` 使用 `L / L_south / L_north`
  - `出铁口温度` 使用 `T_taphole_1 / T_taphole_2`，分别显示 `1#`、`2#`
  - `煤气顶压` 使用 `P_top / P_top_gas_A-D`
  - `压差透气` 使用 `PI / DP_upper / DP_lower / DP_total`
  - `送风供氧` 使用 `Q_blast / P_blast_cold / P_blast / O2_rate / Q_O2`
  - `喷煤` 使用 `PCI_rate / PCI_set`
  - `热制度` 使用炉身温度层传感器 `T_body_L14_A/C/E/G` 作为炉身热制度锚点
- 卡片仍保留左右两侧可读布局，但卡片纵向、引线端点和黄色锚点会跟随模型投影变化；旋转或拖拽高炉本体时会重新计算。
- 同步修复：`OPS-8093-CAD-TOOLTIP-SAFE-FALLBACK`，避免当前远端页面在鼠标拖拽/悬停时因 `cadSensorTooltip` 未定义抛错并残留空蓝色提示框。
- 最终文件 SHA256：`5E19B1466DAF9ED7592534E47EBBBB8DC9BA09E920F6B37196C2F90C86693C9B`
- 最终 HTTP SHA256：`95B0626488A98DFDA8C9CAA7787C120B8E6ED0D440443010F59EA55C26C5C884`
- `BFV4PreviewProxy8093`：`Running`
- 8093 监听 PID：`5632`
- 8768 监听：`true`

Playwright 验收：

- URL：`http://10.30.220.12:8093/?t=playwright-furnace-follow-anchors-v3-tooltipfix-20260709#overview`
- 页面标记：`OPS-8093-FURNACE-FOLLOW-ANCHORS-V3=true`，`OPS-8093-CAD-TOOLTIP-SAFE-FALLBACK=true`
- `cadStatus=loaded`，`.furnace-layer-card=7`，`line=8`，`pin=8`，锚点标签 `1#`、`2#` 存在。
- 拖拽模型前后，`料线` 引线与卡片位置发生同步变化：引线 `y1 35.3 -> 50.2`，卡片 `top 8px -> 21.7307px`，证明标注跟随模型投影而不是固定背景坐标。
- 控制台严重错误 `0`；横向溢出 `false`；tooltip 由空框修正为真实点位内容。
- 截图：`logs/8093_coregroups_playwright/overview_1366x768_furnace_follow_anchors_v3_tooltipfix.png`

## 2026-07-09 核心指标两列均衡布局 V4

用户希望把 28 个核心指标改为更均衡的两列展示，减少面板空旷感。最终口径为：左列 `煤气顶压 / 送风供氧 / 喷煤 / 出铁口温度` 共 14 项，右列 `热制度 / 料线 / 压差透气` 共 14 项；字体和趋势小图同步放大。

实现位置：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html:L1723-L1785](../高炉前端数据/frontend_dashboard_v3.server.html#L1723-L1785)
- 远端热修补脚本：[tools/patch_22012_8093_core_metric_groups.py:L291-L353](../tools/patch_22012_8093_core_metric_groups.py#L291-L353)
- 文件验证脚本：[tools/verify_8093_v4_file_enabled.ps1:L21-L51](../tools/verify_8093_v4_file_enabled.ps1#L21-L51)
- 远端部署脚本：[tools/remote_deploy_8093_core_groups.ps1:L33-L193](../tools/remote_deploy_8093_core_groups.ps1#L33-L193)

本次本地补丁：

- 标记：`OPS-8093-CORE-TWO-COLUMN-V4`、`OPS-8093-CORE-TWO-COLUMN-V4-BALANCED-14-14`
- 左列：`煤气顶压` 5 项、`送风供氧` 5 项、`喷煤` 2 项、`出铁口温度` 2 项，合计 14 项。
- 右列：`热制度` 7 项、`料线` 3 项、`压差透气` 4 项，合计 14 项。
- 说明：仅改变核心指标展示布局，不改变 8768 WebSocket 数据源、诊断逻辑、趋势数据和炉体总览浮层。

远端状态：

- 2026-07-09 16:13 已随 `remote_deploy_8093_core_groups.ps1` 部署到 `10.30.220.12:8093`。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_161300`
- `BFV4PreviewProxy8093`：`Running`，8093 监听 PID：`14800`；8768 保持监听。
- 文件/HTTP 验证：`FileHasCoreTwoColumnV4=true`，`HttpHasCoreTwoColumnV4=true`，`FileHasBalancedCoreColumns=true`，`HttpHasBalancedCoreColumns=true`。
- 文件 SHA256：`DE67D4B855EC50903CBE9D8566AA1D87497D253E4A7FAAA85BB6E765BA2D6CF7`
- HTTP SHA256：`DA0310B00167F38343451C7C41DD065A8AB9278CB3B86142E9FCF619D5FE7F16`
- Playwright 验收：`colCount=2`；左列 `rowCount=14`，分组为 `煤气顶压 / 送风供氧 / 喷煤 / 出铁口温度`；右列 `rowCount=14`，分组为 `热制度 / 料线 / 压差透气`；`horizontalOverflow=false`。
- 截图：[overview_1366x768_core_balanced_14x14.png](../logs/8093_coregroups_playwright/overview_1366x768_core_balanced_14x14.png)
- 结果 JSON：[core_balanced_14x14_result.json](../logs/8093_coregroups_playwright/core_balanced_14x14_result.json)

## 2026-07-09 核心指标两列大字号填充 V5

用户截图反馈核心指标面板下方仍有大片空白，且字体和数字不够显眼。本次在 V4 两列均衡分组基础上增加 V5 填充样式：两列继续保持左 14 项、右 14 项，但核心指标面板宽度加大，状态列在总览紧凑视图中隐藏，把空间让给变量名、实时数值和趋势线；各工艺分组按变量数量占用高度，减少底部空白。

实现位置：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html:L1788-L1868](../高炉前端数据/frontend_dashboard_v3.server.html#L1788-L1868)
- 远端热修补脚本：[tools/patch_22012_8093_core_metric_groups.py:L63-L64](../tools/patch_22012_8093_core_metric_groups.py#L63-L64)、[tools/patch_22012_8093_core_metric_groups.py:L361-L443](../tools/patch_22012_8093_core_metric_groups.py#L361-L443)、[tools/patch_22012_8093_core_metric_groups.py:L617-L626](../tools/patch_22012_8093_core_metric_groups.py#L617-L626)
- 文件验证脚本：[tools/verify_8093_v4_file_enabled.ps1:L51](../tools/verify_8093_v4_file_enabled.ps1#L51)
- 远端部署脚本：[tools/remote_deploy_8093_core_groups.ps1:L104-L201](../tools/remote_deploy_8093_core_groups.ps1#L104-L201)

远端状态：

- 标记：`OPS-8093-CORE-TWO-COLUMN-V5-FILL-LARGE`
- 2026-07-09 16:40 已随 `remote_deploy_8093_core_groups.ps1` 部署到 `10.30.220.12:8093`。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_164019`
- `BFV4PreviewProxy8093`：`Running`，8093 监听 PID：`10648`；8768 保持监听。
- 文件 SHA256：`EF7290510193B5EBCA0B87A7237AB2BACB956C72B5DD48C3CF7F8CABA04BE801`
- HTTP SHA256：`FE8AE6920F10E315361BDA50F0027C7AD050474EDED0DE0456FD69007C85F457`

Playwright 验收：

- URL：`http://10.30.220.12:8093/?t=playwright-core-v5-large-applied-20260709#overview`
- 结果：`styleV5.exists=true`，`styleV5.hasLargeSpecific=true`，`styleV5.hasValue159=true`，`markerV5=true`，`balancedMarker=true`。
- 两列：左列 `煤气顶压 / 送风供氧 / 喷煤 / 出铁口温度`，`rowCount=14`；右列 `热制度 / 料线 / 压差透气`，`rowCount=14`；总计 `28` 项。
- 视觉量测：变量名字号 `13.8px`，数值字号 `15.9px`，编号字号 `12.9px`，状态列 `display=none`，趋势线 `78px × 18px`，平均行高约 `27.82px`。
- 空白与溢出：`gridToNote=6px`，`noteToBodyBottom=6px`，`horizontalOverflow=false`。
- 截图：[overview_1366x768_core_v5_large_applied.png](../logs/8093_coregroups_playwright/overview_1366x768_core_v5_large_applied.png)
- 结果 JSON：[core_v5_large_applied_result.json](../logs/8093_coregroups_playwright/core_v5_large_applied_result.json)

## 2026-07-09 核心指标数值列对齐 V6

用户确认核心指标数值列希望采用“左对齐、等宽数字、固定列宽、趋势线向右留 4-6px 间距”。本次在 V5 大字号填充基础上增加 V6 数值列排版覆盖，不改变 28 变量分组、诊断逻辑、8768 数据桥或趋势数据来源。

实现位置：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html:L1869-L1892](../高炉前端数据/frontend_dashboard_v3.server.html#L1869-L1892)
- 远端热修补脚本：[tools/patch_22012_8093_core_metric_groups.py:L65-L66](../tools/patch_22012_8093_core_metric_groups.py#L65-L66)、[tools/patch_22012_8093_core_metric_groups.py:L446-L469](../tools/patch_22012_8093_core_metric_groups.py#L446-L469)、[tools/patch_22012_8093_core_metric_groups.py:L552-L553](../tools/patch_22012_8093_core_metric_groups.py#L552-L553)
- 文件验证脚本：[tools/verify_8093_v4_file_enabled.ps1:L52-L53](../tools/verify_8093_v4_file_enabled.ps1#L52-L53)
- 远端部署脚本：[tools/remote_deploy_8093_core_groups.ps1:L104-L203](../tools/remote_deploy_8093_core_groups.ps1#L104-L203)

远端状态：

- 标记：`OPS-8093-CORE-VALUE-ALIGN-V6`
- 2026-07-09 17:17 已随 `remote_deploy_8093_core_groups.ps1` 部署到 `10.30.220.12:8093`。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_171732`
- `BFV4PreviewProxy8093`：`Running`，8093 监听 PID：`8320`；8768 保持监听。
- 文件 SHA256：`8AE68DD308331A5C997DEC2782CF6C93B674567C45E45631BD20CCEAC117E98C`
- HTTP SHA256：`792C4BCAA0C92B67072384E404EA3AF0BE2F73B99B8D1D3DC04B7A725F6DD735`
- 文件/HTTP 验证：`FileHasCoreValueAlignV6=true`，`HttpHasCoreValueAlignV6=true`。

Playwright 验收：

- URL：`http://10.30.220.12:8093/?t=playwright-core-v6-value-align-20260709#overview`
- 结果：`markerV6=true`，`styleV6.exists=true`，`styleV6.hasTabularNums=true`，`styleV6.hasMarginLeft6=true`。
- 运行时 CSS：`valueTextAlign=left`，`valueJustifySelf=start`，`valueFontVariantNumeric=tabular-nums`，`valueFontFamily` 包含 `Cascadia Mono / Consolas / monospace`。
- 列宽与间距：1366x768 下 `rowGridTemplateColumns="24px 68px 92px 84px"`，数值列宽 `92px`，趋势线 `78px × 18px`，趋势线 `margin-left=6px`，数值列右侧到趋势线左侧实际间隔 `10px`。
- 布局状态：左右列均 `rowCount=14`，`horizontalOverflow=false`，`oldOptimizationNoteVisible=false`。
- 截图：[overview_1366x768_core_v6_value_align.png](../logs/8093_coregroups_playwright/overview_1366x768_core_v6_value_align.png)
- 结果 JSON：[core_v6_value_align_result.json](../logs/8093_coregroups_playwright/core_v6_value_align_result.json)

### 2026-07-10 V6 数值字体切换为 Times New Roman

用户在 D-DIN 与 Roman 字体预览后选择 `Times New Roman`。本次仅改变核心指标数值列字体，继续保留 V6 的左对齐、固定列宽、`tabular-nums`、趋势线左侧 6px 间距和两列 14/14 分组，不改变数据源、诊断、趋势线取数或 8768 数据桥。

实现位置：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html:L1869-L1892](../高炉前端数据/frontend_dashboard_v3.server.html#L1869-L1892)
- 远端热修补脚本：[tools/patch_22012_8093_core_metric_groups.py:L65-L66](../tools/patch_22012_8093_core_metric_groups.py#L65-L66)、[tools/patch_22012_8093_core_metric_groups.py:L446-L469](../tools/patch_22012_8093_core_metric_groups.py#L446-L469)
- 文件验证脚本：[tools/verify_8093_v4_file_enabled.ps1:L52-L54](../tools/verify_8093_v4_file_enabled.ps1#L52-L54)
- 远端部署脚本：[tools/remote_deploy_8093_core_groups.ps1:L104-L203](../tools/remote_deploy_8093_core_groups.ps1#L104-L203)

远端状态：

- 2026-07-10 13:44 已随 `remote_deploy_8093_core_groups.ps1` 部署到 `10.30.220.12:8093`。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260710_134443`
- `BFV4PreviewProxy8093`：`Running`，8093 监听 PID：`1272`；8768 保持监听。
- 文件 SHA256：`C0D46F50E277802A7093C626F2A58EDD449DC00BB8DF196C43869F72B289DBCA`
- HTTP SHA256：`026F2320CB2B45466D5E31975C04428D9F910678C5D9F7C6E6B7593CE1387D5B`
- 文件/HTTP 验证：`HasCoreValueTimesNewRoman=true`，`FileHasCoreValueAlignV6=true`，`HttpHasCoreValueAlignV6=true`。

Playwright 验收：

- URL：`http://10.30.220.12:8093/?t=playwright-core-v6-times-new-roman-20260710#overview`
- 结果：`markerV6=true`，`styleHasTimes=true`。
- 运行时 CSS：`valueFontFamily="Times New Roman", Times, serif`，`valueTextAlign=left`，`valueJustifySelf=start`，`valueFontVariantNumeric=tabular-nums`。
- 列宽与间距：数值列宽 `92px`，趋势线 `margin-left=6px`，数值列右侧到趋势线左侧实际间隔 `10px`。
- 布局状态：左右列均 `rowCount=14`，`horizontalOverflow=false`，`oldOptimizationNoteVisible=false`。
- 截图：[overview_1366x768_core_v6_times_new_roman.png](../logs/8093_coregroups_playwright/overview_1366x768_core_v6_times_new_roman.png)
- 结果 JSON：[core_v6_times_new_roman_result.json](../logs/8093_coregroups_playwright/core_v6_times_new_roman_result.json)

### 2026-07-10 核心变量两页展示与 D-DIN 恢复 V7

用户反馈两列 14/14 版中，小趋势图并非没有生成，而是每列的 `.core-metric-column-v4` 设置了 `overflow:hidden`；行内趋势图从数值列右侧开始，超出单列边界后被裁掉。同时 `CoreMetricRow` 仍调用 `sparkOptionForIds()`，此前的 `sparkOption()` 细线覆盖并未作用到核心变量行。

本次改为两页展示，每页 14 项，单页使用核心指标面板的完整宽度，避免名称和曲线与另一列争夺空间：

- 第 1 页：煤气顶压 5、送风供氧 5、喷煤 2、出铁口温度 2。
- 第 2 页：热制度 7、料线 3、压差透气 4。
- 每行趋势改为 `coreSparkOptionV7()` 独立绘制最近 30 分钟有效数据，线宽 `1.4px`；空缺分钟不补零。
- 数值字体恢复为 `D-DIN`，保留左对齐、`tabular-nums` 与固定数值列宽；中文名称继续使用宋体。
- V7 初次部署后发现旧版高优先级 CSS 覆盖了新字号和曲线宽度，追加 `OPS-8093-CORE-TWO-PAGE-V7-SPECIFICITY-FIX` 后，V7 的字号、曲线宽度、数值左对齐和 1366 紧凑分页标签均被显式锁定。

实现位置：

- 本地页面：[frontend_dashboard_v3.server.html:L1890](../高炉前端数据/frontend_dashboard_v3.server.html#L1890)
- 远端热修补脚本：[patch_22012_8093_core_metric_groups.py:L67-L70](../tools/patch_22012_8093_core_metric_groups.py#L67-L70)
- 文件验证脚本：[verify_8093_v4_file_enabled.ps1:L57-L62](../tools/verify_8093_v4_file_enabled.ps1#L57-L62)
- 远端部署脚本：[remote_deploy_8093_core_groups.ps1:L104](../tools/remote_deploy_8093_core_groups.ps1#L104)

远端部署状态：

- 已于 2026-07-10 15:25 受控停止、更新并重启 `BFV4PreviewProxy8093`；8768 数据桥始终保持监听。
- 最新备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260710_152506`
- 文件 SHA256：`3134E698C21F217648A766FD8C5910A4E712450292FBB58861679ACD2EF99120`
- HTTP SHA256：`3D0D458C3332780810A070932F8DC76518B7346B8BD7A14914CE8C7549E6F688`
- 服务状态：`Running`；8093 PID `8140`；8768 监听为 `true`。

Playwright 验收：

- URL：`http://10.30.220.12:8093/?t=core-v7-final-compact-20260710#overview`
- 两页均为 `14` 行，所有点位名称和两个分页标签均未溢出；无 JavaScript/React 页面错误，无横向滚动。
- 1366x768 下数值字体为 `D-DIN`、字号 `17px`、左对齐；每条曲线有 `15-30` 个有效点，线宽 `1.4px`。
- 截图：[page 1](../logs/8093_coregroups_playwright/overview_1366x768_core_v7_final_checked_page1.png)、[page 2](../logs/8093_coregroups_playwright/overview_1366x768_core_v7_final_checked_page2.png)
- 结果 JSON：[core_v7_final_checked_result.json](../logs/8093_coregroups_playwright/core_v7_final_checked_result.json)

### 2026-07-10 总览趋势大图 V8

用户要求总览中的“趋势分析 / 事件时间线”保留为清晰的大图，而不是右下角的小卡片。本次把总览调整为“核心指标左侧全高、炉况总览与当前诊断在上、趋势大图横跨中右两列在下”的工作台布局；诊断、依据和建议仍保留，事件时间线移动到大图右侧辅助阅读。

- 标记：`OPS-8093-OVERVIEW-LARGE-TREND-V8`
- 主图数据：总压差、综合顶压、透气性指数、综合顶温；保留最近 120 分钟历史、预测区及悬浮数值。
- 1366x768：趋势面板 `883×273px`，主图 `654×230px`。
- 1920x1080：趋势面板 `1243×374px`，主图 `951×328px`。
- 四条实时曲线各有 `120` 个历史点；无 JavaScript/React 错误、无横向溢出。

实现位置：

- 本地页面：[frontend_dashboard_v3.server.html:L1947](../高炉前端数据/frontend_dashboard_v3.server.html#L1947)
- 远端热修补脚本：[patch_22012_8093_core_metric_groups.py:L71-L72](../tools/patch_22012_8093_core_metric_groups.py#L71-L72)、[V8 覆盖:L195-L236](../tools/patch_22012_8093_core_metric_groups.py#L195-L236)
- 文件验证：[verify_8093_v4_file_enabled.ps1:L63-L64](../tools/verify_8093_v4_file_enabled.ps1#L63-L64)
- 远端部署校验：[remote_deploy_8093_core_groups.ps1:L107-L109](../tools/remote_deploy_8093_core_groups.ps1#L107-L109)、[HTTP 校验:L167-L169](../tools/remote_deploy_8093_core_groups.ps1#L167-L169)

远端状态：2026-07-10 15:48 已执行“停止 8093 -> 更新 -> 启动 8093”；`BFV4PreviewProxy8093=Running`，8093 PID `2920`，8768 保持监听。远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260710_154839`。文件 SHA256 为 `49FAD4FF71F84292BE067D687FC33C9FBF2780A71136B68EA39B487EAA758884`，HTTP SHA256 为 `E9225FCFC2A50B82DD8113AEFA1F28BC946267A7261A24CE370D2A4843ECFAF8`。

浏览器验收：

- URL：`http://10.30.220.12:8093/?t=overview-large-trend-v8-final-20260710#overview`
- 截图：[1366x768](../logs/8093_coregroups_playwright/overview_1366x768_large_trend_v8.png)、[1920x1080](../logs/8093_coregroups_playwright/overview_1920x1080_large_trend_v8.png)
- 结果 JSON：[overview_large_trend_v8_result.json](../logs/8093_coregroups_playwright/overview_large_trend_v8_result.json)

## 2026-07-09 参数优化建议注释移除

用户要求移除参数优化建议页中“正常炉况不生成满屏操作建议，只保留默认运行和关键观察项。”这句说明。当前本地 HTML 已从 `OptimizationTab` 渲染中删除该 `focus-principle` 文案，并增加 `OPS-8093-OPTIMIZATION-NOTE-REMOVED` 兜底样式；远端热修补脚本会在部署时删除同一 JSX 片段，并验证 HTTP 返回中不再包含该文案。

实现位置：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html:L694-L694](../高炉前端数据/frontend_dashboard_v3.server.html#L694-L694)、[高炉前端数据/frontend_dashboard_v3.server.html:L1723-L1724](../高炉前端数据/frontend_dashboard_v3.server.html#L1723-L1724)
- 远端热修补脚本：[tools/patch_22012_8093_core_metric_groups.py:L63-L63](../tools/patch_22012_8093_core_metric_groups.py#L63-L63)、[tools/patch_22012_8093_core_metric_groups.py:L368-L440](../tools/patch_22012_8093_core_metric_groups.py#L368-L440)
- 文件验证脚本：[tools/verify_8093_v4_file_enabled.ps1:L52-L53](../tools/verify_8093_v4_file_enabled.ps1#L52-L53)
- 远端部署脚本：[tools/remote_deploy_8093_core_groups.ps1:L107-L109](../tools/remote_deploy_8093_core_groups.ps1#L107-L109)、[tools/remote_deploy_8093_core_groups.ps1:L164-L166](../tools/remote_deploy_8093_core_groups.ps1#L164-L166)

远端状态：2026-07-09 16:13 已随 `remote_deploy_8093_core_groups.ps1` 部署到 `10.30.220.12:8093`；文件/HTTP 验证均为 `HasOptimizationNoteRemoved=true`。Playwright 验收显示 `bodyHasOldNote=false`、`sourceHasOldNote=false`、`focusPrincipleVisible=false`。后续验证脚本已将两列布局的中文源码匹配改为 ASCII 标记 `OPS-8093-CORE-TWO-COLUMN-V4-BALANCED-14-14`，避免远端 Windows PowerShell 5 按 ANSI 解码 UTF-8 脚本时产生 `Select-String` 参数绑定噪音。

## 2026-07-09 CAD 炉体两侧灰色竖块修复

用户截图指出总览页中“高炉本体被旁边两侧灰色区域遮住”。复核后确认根因不是 GLB 模型，而是前序 `OPS-8093-FURNACE-FOLLOW-ANCHORS-V3` 为给左右工艺卡片让位，将 `.cad-furnace-viewer` 压缩到舞台约 `30%~32%` 宽；同时早期 `.cad-furnace-viewer` 样式带半透明背景，外层 `.layered-cad-stage` 仍为 `overflow:hidden`，因此形成竖向灰块和裁切感。

本次修复：

- 标记：`OPS-8093-CAD-GHOST-BANDS-FIX`
- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html:L1715-L1722](../高炉前端数据/frontend_dashboard_v3.server.html#L1715-L1722)
- 远端热修补脚本：[tools/patch_22012_8093_core_metric_groups.py:L356-L363](../tools/patch_22012_8093_core_metric_groups.py#L356-L363)
- 文件验证脚本：[tools/verify_8093_v4_file_enabled.ps1:L43-L45](../tools/verify_8093_v4_file_enabled.ps1#L43-L45)
- 远端部署脚本：[tools/remote_deploy_8093_core_groups.ps1:L98-L101](../tools/remote_deploy_8093_core_groups.ps1#L98-L101)、[HTTP 校验:L149-L152](../tools/remote_deploy_8093_core_groups.ps1#L149-L152)
- 样式口径：`.cad-furnace-viewer` 改为透明背景，Three.js renderer 运行时固定 `clearAlpha=0`，舞台允许 `overflow:visible`，画布宽度恢复到舞台约 `52%`，并通过更高优先级选择器与延迟重放覆盖后续 V3 跟随标注样式。
- 同步修复旧 tooltip 兜底：将 `var cadSensorTooltip=function...` 改为 `window.cadSensorTooltip=function...`，避免已存在 `function cadSensorTooltip` 时 Babel 报 `Identifier 'cadSensorTooltip' has already been declared`。

本地浏览器验收：

- 临时预览 URL：`http://127.0.0.1:8093/frontend_dashboard_v3.server.html?t=ghostbands-local-static-20260709d#overview`
- Playwright 结果：`marker=true`，`stageOverflow=visible`，`viewerBackgroundColor=rgba(0, 0, 0, 0)`，`viewerBackgroundImage=none`，`canvasBackgroundColor=rgba(0, 0, 0, 0)`，`rendererClearAlpha=0`，`viewerWidthRatio=0.518`，`sensorCount=115`，`cardCount=7`，`lineCount=8`，`pinCount=8`，`horizontalOverflow=false`，`overflowCards=0`。
- 截图：[overview_1366x768_cad_ghost_bands_fix_local_static_8093.png](../logs/8093_coregroups_playwright/overview_1366x768_cad_ghost_bands_fix_local_static_8093.png)
- 结果 JSON：[cad_ghost_bands_fix_local_static_8093_result.json](../logs/8093_coregroups_playwright/cad_ghost_bands_fix_local_static_8093_result.json)

远端 220.12 部署状态：

- 2026-07-09 16:10 已通过 `tools\remote_22012_exec.py` 上传并执行 `remote_deploy_8093_core_groups.ps1`。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260709_161029`
- 文件 SHA256：`BE74603844726E8D47344777C36F01655185E3BFD710F03759E745978D4915F2`
- HTTP SHA256：`41A34DFBFF9B481997C8D057D410825B25C9E29DBEB40B541F6BC2FD2AE8208A`
- 初次部署后状态：`BFV4PreviewProxy8093=Running`，8093 监听 PID：`7704`，8768 监听：`true`。
- 二次脚本同步验证：`HasCadGhostBandsFix=true`，`HasRemovedOptimizationNoteText=true`，`EffectiveCoreGroups=true`，8093 监听 PID：`14800`。

远端浏览器验收：

- URL：`http://10.30.220.12:8093/?t=ghostbands-remote-20260709-1615#overview`
- Playwright 结果：`marker=true`，`stageOverflow=visible`，`viewerBackgroundColor=rgba(0, 0, 0, 0)`，`viewerBackgroundImage=none`，`canvasBackgroundColor=rgba(0, 0, 0, 0)`，`rendererClearAlpha=0`，`viewerWidthRatio=0.518`，`sensorCount=115`，`hitCount=115`，`cardCount=7`，`lineCount=8`，`pinCount=8`，`horizontalOverflow=false`，`overflowCards=0`。
- 截图：[overview_1366x768_cad_ghost_bands_fix_remote_8093.png](../logs/8093_coregroups_playwright/overview_1366x768_cad_ghost_bands_fix_remote_8093.png)
- 结果 JSON：[cad_ghost_bands_fix_remote_8093_result.json](../logs/8093_coregroups_playwright/cad_ghost_bands_fix_remote_8093_result.json)

## 2026-07-09 趋势页图例翻页箭头颜色核查

用户反馈趋势分析页 ECharts 图例左右翻页箭头的可用/不可用颜色逻辑相反：期望白色表示可点击，灰色表示不可点击。

本次浏览器实测 URL：

- `http://10.30.220.12:8093/?t=legend-white-active-20260709#trend`

Chrome/CDP 读取 8093 运行时 ECharts option 后确认，四个趋势图仍在使用默认值：

- `legend.pageIconColor="#2f4554"`：实际可点击箭头颜色偏暗灰。
- `legend.pageIconInactiveColor="#aaa"`：实际不可点击箭头更浅，视觉上容易被误认为可点击。

远端页面源码中存在后置普通脚本标记 `OPS-8093-LEGEND-ARROWS-WHITE-ACTIVE`，但该脚本没有改变已经渲染出的 ECharts option。原因判断为：后置脚本执行时机/作用域没有稳定覆盖 `lineOption/forecastOption` 或已创建的图表实例，属于“源码含补丁但运行未生效”。

本地修正已改为在原始 `lineOption()` 的滚动图例配置中直接写入颜色，避免依赖后置补丁时序：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html:L656](../高炉前端数据/frontend_dashboard_v3.server.html#L656)
- 兜底覆盖仍保留：[高炉前端数据/frontend_dashboard_v3.server.html:L1446](../高炉前端数据/frontend_dashboard_v3.server.html#L1446)

合格信号：

- `legend.type="scroll"`
- `legend.pageIconColor="#ffffff"`
- `legend.pageIconInactiveColor="rgba(118,128,145,.36)"`
- 浏览器运行时 ECharts option 与上述值一致。

2026-07-09 已在 220.12 上对 8093 目标 HTML 做定点修改，未运行大部署脚本，未重启 8768，也未改其它业务文件。修改对象：

- `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html`

远端执行结果：

- 备份文件：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_legend_arrows_20260709_161158`
- 修改前 SHA256：`BE74603844726E8D47344777C36F01655185E3BFD710F03759E745978D4915F2`
- 修改后 SHA256：`71C89F3E5FDD7120E981B77CAA3CA710A7BF1A7505A60C126AF5ACFCEA13CD40`
- 文件验证：`type:'scroll',pageIconColor:'#ffffff'` 出现 `1` 次，`pageIconInactiveColor:'rgba(118,128,145,.36)'` 出现 `2` 次。
- 远端本机 HTTP 验证：`http://127.0.0.1:8093/?t=legend-arrow-filepatch-20260709161158#trend` 返回 `200`，且响应 HTML 包含上述白/灰配置。

后续浏览器验收时，建议用新缓存参数打开趋势页并用浏览器/CDP 或 Playwright 复核运行时 ECharts option：

```powershell
curl.exe --max-time 45 -s -H 'Cache-Control: no-cache' "http://10.30.220.12:8093/?t=legend-white-active-v2-20260709#trend" | Select-String "pageIconColor|pageIconInactiveColor|OPS-8093-LEGEND-ARROWS-WHITE-ACTIVE"
```

## 2026-07-10 自动值守避让与建议 Markdown 渲染

用户反馈总览页的自动值守浮层遮挡核心信息，且值守建议中的 Markdown 标记以原始文本显示。根因有两点：原始 `#bf-auto-monitor` 是可拖动的固定浮层，宽屏下会保留在页面内容层之上；队列建议摘要直接使用 `esc(item.summary_text)`，只做了转义，没有走已有的 Markdown 渲染路径。

本次不删除自动值守，而是调整为按需查看的抽屉，保留值守能力且不再遮挡总览：

- 标记：`OPS-8093-AUTO-MONITOR-DOCK-MD-V9`
- 默认隐藏 `#bf-auto-monitor`，头部刷新区增加一个带提示的圆形入口 `#bf-auto-trigger`。
- 仅在用户点击入口后，从右侧展开 `420px` 宽的抽屉；关闭即恢复隐藏。默认首屏没有覆盖层，也不会产生横向滚动。
- 队列建议摘要使用受控的安全 Markdown 小解析器：先转义 HTML，再支持一级至三级标题、无序/有序列表、`**粗体**` 和 `` `行内代码` ``；不执行输入中的 HTML 或脚本。
- 为避免实时队列刷新重新绑定旧拖动逻辑，抽屉头部在每次应用时克隆替换，并由后置脚本稳定重放样式和入口。

实现与验证：

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html:L1967](../高炉前端数据/frontend_dashboard_v3.server.html#L1967)
- 远端补丁：[tools/patch_22012_8093_core_metric_groups.py:L214](../tools/patch_22012_8093_core_metric_groups.py#L214)
- 文件验证：[tools/verify_8093_v4_file_enabled.ps1:L65](../tools/verify_8093_v4_file_enabled.ps1#L65)
- 部署验证：[tools/remote_deploy_8093_core_groups.ps1:L110](../tools/remote_deploy_8093_core_groups.ps1#L110)
- 浏览器核验：[tools/verify_auto_monitor_dock_v9.py](../tools/verify_auto_monitor_dock_v9.py)

2026-07-10 17:04 已部署到 `10.30.220.12:8093`。本次仅重启 `BFV4PreviewProxy8093`，未停止 8768 数据桥接服务。

- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260710_170430`
- 文件 SHA256：`A5A62BBFA6166C8EC156F5B263D188EEAFC69219E2EDCC68D31B15AA7903AE37`
- HTTP SHA256：`6731BB6F6D681F4C5DD3830E6E2E4453B7A78D25F2AD069AA96015541CE9E5FA`
- 部署验证：`HasAutoMonitorDockMdV9=true`、`HasAutoMonitorMarkdownRenderer=true`、`BFV4PreviewProxy8093=Running`、8093 PID `5196`、8768 监听保持 `true`。

远端 Playwright 验收 URL：`http://10.30.220.12:8093/?t=auto-monitor-v9-remote-20260710c#overview`

- 初始状态：V9 标记和入口存在，`monitorDisplay=none`，无横向溢出。
- 展开状态：`display=flex`，抽屉矩形为 `x=934, y=104, width=420, height=574`，无横向溢出。
- Markdown 注入检查：`**关键结论**` 和两项列表渲染为 `<strong>` 与 `<ul><li>`；点击抽屉关闭按钮后恢复隐藏，无页面脚本错误。
- 截图：[关闭状态](../logs/8093_coregroups_playwright/overview_1366x768_auto_monitor_v9_closed.png)、[展开状态](../logs/8093_coregroups_playwright/overview_1366x768_auto_monitor_v9_open.png)
- 结果：[auto_monitor_v9_remote_result.json](../logs/8093_coregroups_playwright/auto_monitor_v9_remote_result.json)

## 2026-07-10 核心指标曲线间距微调

用户要求保持核心指标的文本字号、数值列宽度和曲线长度不变，仅收紧数值/状态区域与趋势线之间的视觉空隙。本次通过后置样式标记 `OPS-8093-CORE-SPARK-GAP-V10` 将 `.core-page-v7 .core-group-row .spark` 的额外 `margin-left` 从 `4px` 调整为 `0px`；未修改数值字体、数值固定列宽、状态文本或曲线尺寸。

- 本地页面：[高炉前端数据/frontend_dashboard_v3.server.html:L1947](../高炉前端数据/frontend_dashboard_v3.server.html#L1947)
- 远端补丁：[tools/patch_22012_8093_core_metric_groups.py:L199](../tools/patch_22012_8093_core_metric_groups.py#L199)
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_8093_core_metric_groups_20260710_172046`
- 文件 SHA256：`1448EC4A9A05FA8E58DA6E304BA820B9970FA0C83BEA483CAD3EA76E0D8B34E1`
- 发布状态：8093 已重启为 PID `14684`，8768 持续监听；文件和 HTTP 均通过 `FileHasCoreSparkGapV10=true`、`HttpHasCoreSparkGapV10=true`。
- 浏览器验收：`sparkMarginLeft=0px`、`valueFontSize=20px`、`valueWidth=128px`、无横向溢出、无脚本错误。
- 截图：[overview_2048x1152_core_spark_gap_v10.png](../logs/8093_coregroups_playwright/overview_2048x1152_core_spark_gap_v10.png)
- 结果：[core_spark_gap_v10_remote_result.json](../logs/8093_coregroups_playwright/core_spark_gap_v10_remote_result.json)

## 2026-07-14 总览高炉工艺标注浮层去项数与白色统一

用户截图指出总览页高炉七类工艺数据标注浮层中的 `n项` 角标不应显示，且“煤气顶压、料线、热制度、压差透气、送风供氧、喷煤、出铁口温度”七个工艺名、变量单位不应再用颜色区分。本次定位到活动浮层由 `FurnaceLayerFollowCard` 生成，并由 `FurnaceLayerCallouts=function BFModelFollowFurnaceLayerCallouts` 投射到 Three.js 高炉模型点位。

实现位置：

- 七类工艺定义与基础浮层：[高炉前端数据/frontend_dashboard_v3.server.html:L1539-L1543](../高炉前端数据/frontend_dashboard_v3.server.html#L1539-L1543)
- 点位锚定版浮层：[高炉前端数据/frontend_dashboard_v3.server.html:L1670-L1671](../高炉前端数据/frontend_dashboard_v3.server.html#L1670-L1671)
- 当前活动的跟随模型浮层：[高炉前端数据/frontend_dashboard_v3.server.html:L1716-L1717](../高炉前端数据/frontend_dashboard_v3.server.html#L1716-L1717)
- 白色与单位加粗兜底样式：`v3-annotation-cleanup`，位于 [高炉前端数据/frontend_dashboard_v3.server.html:L2441-L2454](../高炉前端数据/frontend_dashboard_v3.server.html#L2441-L2454)
- 远端热修补模板同步更新：[tools/patch_22012_8093_core_metric_groups.py:L89](../tools/patch_22012_8093_core_metric_groups.py#L89)、[L290](../tools/patch_22012_8093_core_metric_groups.py#L290)、[L339](../tools/patch_22012_8093_core_metric_groups.py#L339)

本次规则：

- `FurnaceLayerCard`、`FurnaceLayerPointCard`、`FurnaceLayerFollowCard` 均不输出 `<b>{group.items.length}项</b>`。
- 标注浮层中的工艺名、变量名、数值和单位统一为白色。
- 单位 `.furnace-layer-unit` 保持白色并加粗。
- 浮层内不再根据 `iqrStatus()` 给数值追加 `text-bad` / `text-warn`，避免红/黄颜色表达。
- 仅调整前端标注展示；七类工艺映射、变量 ID、实时数据来源、8768 数据桥接和诊断逻辑均不变。

验证：

```powershell
node tools\verify_8093_annotation_cleanup.js http://10.30.220.12:8093/ --static-only
```

结果：本地源页静态校验通过，活动跟随浮层渲染函数已检查。远端 HTTP 校验结果为：

```json
{"HttpHasCleanup":true,"HttpHasUnitBold":true,"FollowRendererFound":true,"FollowHasCountBadge":false,"FollowHasStatusColor":false}
```

补充浏览器验收：本机临时安装 `playwright-core` 到 `C:\tmp\codex_pw`，使用系统 Edge 访问 `http://10.30.220.12:8093/?annotation_cleanup=20260714#overview`，固定视口 `1366×768`。

- 浮层卡片数：`7`
- `n项` 角标数：`0`
- 七个工艺名颜色：全部 `rgb(255, 255, 255)`
- 28 个单位颜色：全部 `rgb(255, 255, 255)`
- 28 个单位字重：全部 `900`
- 数值 class：均为 `furnace-layer-value`，无 `text-bad` / `text-warn`
- 横向溢出：`false`
- 截图：[overview_1366x768_annotation_cleanup.png](../logs/8093_annotation_cleanup_20260714/overview_1366x768_annotation_cleanup.png)

已知非本次范围项：浏览器默认请求 `http://10.30.220.12:8093/favicon.ico` 返回 404；该 404 与本次高炉工艺标注浮层无关。若后续要求控制台 0 错误，可在 8093 远端 HTML 头部增加显式 favicon 链接，例如复用现有 `logo/冀南钢铁集团logo.png`。

## 2026-07-14 参数优化驾驶舱部署

目标是把已在 `front2` 验收过的“当前炉况—唯一决策—实时监测/风险—趋势/候选”参数优化布局同步到本机主源页与 220.12 的 8093 V4 预览页，而不覆盖远端已有的诊断、总览、问答及品牌头部修补。

- 本机源页已用 [sync_optimization_cockpit_to_8093.py](../tools/sync_optimization_cockpit_to_8093.py) 增量更新：`高炉前端数据/frontend_dashboard_v3.server.html`；最后一次本机回退副本为 `frontend_dashboard_v3.server.html.bak_optimization_cockpit_20260714_230440`。
- 远端先下载当前页并合并，再由 [remote_deploy_8093_optimization_cockpit.ps1](../tools/remote_deploy_8093_optimization_cockpit.ps1) 受控发布。该脚本只停止/启动 `BFV4PreviewProxy8093`，且在操作前后都确认 `8768` 仍在监听。
- 远端回退副本：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_optimization_cockpit_20260714_230530`。
- 最终发布前远端页面 SHA256：`F062A11A3D1FB2B131332B50826E9CA0062DAAB4094D2CBB544002F287CDC672`；最终发布后 SHA256：`3A296BDD34F46FDE6D8BAC93545CB4C60F81E387B344F0710D37B5135FDB30E5`。
- 发布后：`BFV4PreviewProxy8093=Running`、8093 监听、8768 监听、`http://10.30.220.12:8093/` 返回 HTTP 200；页面同时具备 `OptimizationCockpitLayout`、`ops-8093-optimization-cockpit-20260714`、`OptimizationTab=OptimizationCockpitLayout;`、`BFOptimizationWorkbenchV10` 和 `topbar branded-topbar` 标记。
- 首次远端 Chromium 验收发现 `1366×768` 下三列百分比宽度叠加间距导致右侧监测栏越出约 `24px`；已改为 `23fr/50fr/27fr` 比例轨道并再次受控发布。最终 [本机 8094 Chromium 代表视口报告](../logs/8093_optimization_cockpit_local_qa_after_fix/manifest_chromium_representative.json) 与 [远端 8093 Chromium 代表视口报告](../logs/8093_optimization_cockpit_remote_qa_after_fix/manifest_chromium_representative.json) 均为 4/4 通过，无横向溢出、核心面板缺失或脚本错误。

## 2026-07-15 完整建议引擎部署

- 目标目录：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW`。部署对象为参数优化 HTML、8768 的 `自动诊断服务/local_pg_ws_bridge.py`、`recommendation_adapter.py` 和独立 `调控结论生成引擎`；未修改 8092 主系统。
- 受控发布入口：[remote_deploy_8093_full_recommendation_engine.ps1](../tools/remote_deploy_8093_full_recommendation_engine.ps1)。发布前编译全部 Python 文件，停止并重启 `BFV4PreviewWs8768` 与 `BFV4PreviewProxy8093`，失败时自动恢复页面和桥接文件。
- 发布前 HTML SHA256：`3A296BDD34F46FDE6D8BAC93545CB4C60F81E387B344F0710D37B5135FDB30E5`；发布后：`8B5871DBA4A567DEB7253C8C666A9881628D1832D1DB52532E8068D910453C6E`。桥接文件发布前：`93EDB893751FEBBDE929B1672A0AD5CFEC30862382ECF3F458FDDBB7DFE7E9CD`；发布后：`FF6136F8328975BA29093DA31F28DC1DE0095135BA4FF0F4C21AE29DADE8F7E9`。
- 引擎回退目录：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\full_recommendation_engine_20260715_011058`。发布后两服务均 `Running`，8093/8768 均监听，HTTP 返回 200。
- 远端合同实测：[optimization_json_contract_remote_8768_full_engine_20260715.json](../logs/optimization_json_contract_remote_8768_full_engine_20260715.json) 为 `ok=true`；最新诊断 `cold`，`recommendation_status=ready`，引擎 `blast_furnace_recommendation_engine/v4-complete`，返回 6 条结构化动作。
- 首轮五页桌面矩阵 25 项中，路由、布局与横向溢出均通过；诊断页记录到素材同步前的 404。随后八张炉况 PNG 已按 [remote_sync_8093_furnace_assets.ps1](../tools/remote_sync_8093_furnace_assets.ps1) 同步，HTTP 8/8 为 `200 image/png`，素材回退目录为 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\furnace_conditions_20260715_012145`。

## 2026-07-15 正常顺行稳健聚合规则部署

- 部署对象：8093 V4 预览目录的 `炉况规则引擎`，以及 `\GL02AutoDiagnosis\RunOnce` 使用的 V3 `rule_engine` 实际落库目录。
- 规则变化：炉体三段波动由最大值改为 P75，7-16层圆周变异系数由最大值改为 P80；八项阈值更新为 `1.25/1.25/1.25/1.25/0.28/1.25/0.38/0.80`，正常权重保持 `18/18/18/12/12/14/10/8`。
- 主次炉况边界未变：主异常 `>=45`，次异常 `>=55` 且主次分差 `<10`；正常分仍不与已入选异常直接竞争。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\normal_rule_robust_20260715_012927`、`F:\高炉炼铁项目-real-sensor-v2_V3\backups\normal_rule_robust_20260715_012927`。
- 运行方式：Python规则不能文件级热重载，因此8093代理保持在线，只滚动重启 `BFV4PreviewWs8768`；新进程于 `01:29:29` 启动，8768监听正常，8093 HTTP `200`。
- 落库验证：计划任务于 `01:31:31` 完成，结果码 `0`；随后自然调度生成的 `01:35` 快照 `main_label=normal`、`raw_scores.normal=61.64`，部署版对同一特征复算为 `61.64`，覆盖率 `1.0`、缺失变量 `0`。
- 完整参数、SHA256与验证入口见 [正常顺行积分与主次炉况竞争规则汇总](正常顺行积分与主次炉况竞争规则汇总.md)。

## 2026-07-15 正常/异常主次竞争规则部署

- 规则范围：只修改 `engine/resolver.py`，不调整正常权重、稳定阈值或七类异常评分公式。
- 当前公式：`A<45` 时主正常、无次诊断；`45<=A<60` 且 `N>=A+3` 时主正常、次异常；其余情况最高异常为主，`A>=60` 不能被正常覆盖。
- 异常为主时，第二异常仍需 `>=55` 且与第一异常分差 `<10`；本次没有部署连续两个五分钟点确认。
- 30天 `8640` 点回放：主正常 `7595->8140`，异常主诊断 `1045->500`，切换次数 `369->294`，“主正常、次异常”`558` 点。
- 部署目标：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\炉况规则引擎` 与 `F:\高炉炼铁项目-real-sensor-v2_V3\rule_engine`。
- 回退目录：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\resolver_normal_abnormal_competition_20260715_020445`、`F:\高炉炼铁项目-real-sensor-v2_V3\backups\resolver_normal_abnormal_competition_20260715_020445`。
- 两处 `resolver.py` SHA256：`F90BF6CE6D19505422038E9B3ED2EB92E69320343DB957D2BCB092667CCD3BBE`。远端编译和 [边界验证脚本](../tools/verify_resolver_normal_abnormal_competition.py) 均通过。
- 运行处置：8093代理保持在线，只重启加载规则的 `BFV4PreviewWs8768`；重启后 8768 与 `BFV4PreviewProxy8093` 均为 `Running`，HTTP返回200，WebSocket init返回482个历史点。
- 落库验证：`\GL02AutoDiagnosis\RunOnce` 从 `02:06:06` 运行到 `02:08:08`，结果码0；新快照 `02:05` 为 `normal=36.12`、`cold=33.31`，正确输出主正常且次诊断为空。

## 2026-07-15 参数优化决策依据展示修正

- 移除现场页面中的“版本：v4-complete”；版本仍保留在 8768 JSON 和运维记录，不改变建议引擎契约。
- 决策依据不再固定使用同一组指标：正常顺行、低料线、边缘发展、中心过吹、管道、炉凉、炉热、崩滑料/悬料分别选择对应的四个实时证据指标。
- “为何这样决策”固定由诊断证据生成两段实际说明，不再引用当前选中的候选动作理由；点击候选动作不会改变上方诊断依据。生产页面现已隐藏两段的内部标签文字。
- 发布脚本：[remote_deploy_8093_decision_basis.ps1](../tools/remote_deploy_8093_decision_basis.ps1)。只重启 `BFV4PreviewProxy8093`，8768 全程保持监听。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_decision_basis_20260715_013517`；部署后 SHA256 `C4B621E54BFEFCAC4C14DA11E0FDC8B754DD8A8B967CC0FD881260615B2640F1`。
- [专项验证脚本](../tools/verify_8093_decision_basis.py) 验证建议状态 ready、版本不可见、4 张证据卡、两段实际说明完整、动作切换后依据不变、无横向溢出/文字裁切/页面或控制台错误。

## 2026-07-15 参数优化决策说明标签隐藏

- 仅从 8093 页面移除“规则判据：”和“处置逻辑：”两个可见标签；`basisEvidence` 与 `basisLogic` 的实际说明内容、分段换行和计算来源不变，8768 JSON 与建议引擎未修改。
- 发布脚本：[remote_deploy_8093_hide_decision_labels.ps1](../tools/remote_deploy_8093_hide_decision_labels.ps1)。只重启 `BFV4PreviewProxy8093`，8768 全程保持监听。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_hide_decision_labels_20260715_075811`；发布前 SHA256 `96DB653415DAA99F007F470F97F92E2D149BB935998F944C63D569885D245B85`，发布后 `DADC8E6711808E3AEA89965B6C023E55FD43EB285CB52FDCF63425CB7FED64B3`。
- 远端 Chromium 在 `1280×720`、`1366×768`、`1920×1080` 均验证通过：两个标签不可见，两段实际内容和 4 张证据卡保留，无横向溢出、裁切、页面或控制台错误。截图：[1366×768 效果](../logs/8093_decision_labels_hidden_remote_1366x768.png)。

## 2026-07-15 首页复用标准化建议结果

- 首页最终生效的 `BFOverviewRightThreeColumnV12` 和参数优化页统一调用 `bfRecommendationEngineView(diagnosis)`；首页只展示标准动作数组前三条，标题、阶段和理由均来自建议引擎，不再使用固定卡片标题。
- 建议状态为 waiting/failed 时：首页分别显示等待和不可用状态，不用前端静态建议冒充实时结果。点击任一摘要仍进入参数优化页查看完整候选与现场边界。
- 发布脚本：[remote_deploy_8093_overview_recommendations.ps1](../tools/remote_deploy_8093_overview_recommendations.ps1)。仅重启 `BFV4PreviewProxy8093`，8768 全程保持监听。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_overview_recommendations_20260715_015834`；发布前 SHA256 `4F8F129F8AEFE1B228C5067EC96C890A6550D999BDF72532220BAABF9AC94AEB`，发布后 `44723260A0382F5B1EDB35913EDBB81EEFFC42F504B2EC7450ABB1AAEECDB4B8`。
- [一致性验证脚本](../tools/verify_8093_overview_optimization_consistency.py) 在远端 `1280×720`、`1366×768`、`1920×1080` 均返回 `ok=true`；当前正常顺行时两页前三条依次为“保持当前制度稳定”“不建议频繁小调风、氧、煤、料制”“持续监测风压、顶压、透气性和顶温离散度”，无横向溢出、卡片裁切、HTTP/页面/控制台错误。

## 2026-07-15 首页趋势跳转防遮挡修正

- 根因：原“进入趋势分析”按钮以绝对定位叠在趋势面板上，ECharts 图例或曲线内容延伸到右上区域时可能被按钮遮挡。
- 修正：`Panel` 增加可选标题栏操作槽 `headerAction`，趋势按钮进入标题栏正常文档流，不再占用或覆盖图表内容；跳转仍复用底部导航按钮，保持当前 WebSocket 和页面状态。
- 发布脚本：[remote_deploy_8093_overview_trend_jump.ps1](../tools/remote_deploy_8093_overview_trend_jump.ps1)。只重启 `BFV4PreviewProxy8093`，8768 全程保持监听。
- 远端备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_overview_trend_jump_20260715_021413`；发布前 SHA256 `44723260A0382F5B1EDB35913EDBB81EEFFC42F504B2EC7450ABB1AAEECDB4B8`，发布后 `96DB653415DAA99F007F470F97F92E2D149BB935998F944C63D569885D245B85`。
- [专项验证](../tools/verify_8093_overview_trend_jump.py) 在远端 `1280×720`、`1366×768`、`1920×1080` 均为 `ok=true`：按钮完全位于标题栏、与图表区域不相交、点击后 `#trend` 同文档切换、无横向溢出、HTTP/页面/控制台错误。

## 2026-07-15 首页应用内快速跳转

- 延迟根因：原首页炉况诊断和优化建议入口在写入 hash 后继续调用 `window.location.reload()`，导致 React/Babel 页面、WebSocket 和 ECharts 整体重新初始化；底部正式导航本身已经支持无刷新切换。
- 修正：`overviewDecisionNavigateV11()` 按 `NAVS` 找到对应底部导航按钮并调用其现有 React 点击处理器，降级路径只更新 hash；参数优化工作台入口同步复用该函数。首页新增“进入趋势分析”按钮，最终版本位于面板标题栏，避免覆盖图例、曲线和时间线。
- 数据边界：没有修改 API、数据库、8768 WebSocket 合同或建议引擎；站内切换保留当前文档、实时连接和已加载状态。
- 首次受控发布只重启 `BFV4PreviewProxy8093`，8768 全程保持监听；回退文件为 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\高炉前端数据\frontend_dashboard_v3.server.html.bak_fast_navigation_20260715_015523`。后续首页建议共享与趋势标题栏修正继续保留无刷新导航，当前发布链最终 SHA256 以趋势防遮挡记录的 `96DB653415DAA99F007F470F97F92E2D149BB935998F944C63D569885D245B85` 为准。
- 远端 Chromium 专项点击中，诊断、优化建议、趋势三个入口均保持同一 document，常见耗时约 `68–319ms`。同源 HTML 的 [Chromium 9 个固定视口](../logs/8093_fast_navigation_local_qa_20260715/manifest_chromium_all.json)、[Firefox 4 个代表视口](../logs/8093_fast_navigation_local_qa_20260715/manifest_firefox_representative.json)、[WebKit 4 个代表视口](../logs/8093_fast_navigation_local_qa_20260715/manifest_webkit_representative.json) 全部通过；专项验证同时检查目标路由、核心按钮、横向溢出、页面错误和控制台错误。

## 2026-07-15 MCP 口语调用失败核查

- 故障标记：`ERR-8093-MCP-COLLOQUIAL-002`。
- 用户现场问题“南探尺和北探尺现在分别是多少？”返回炉况快照兜底，没有查询南北探尺。
- 真实 8093 复测确认：自然语言问题为 `mcp_prefetch.used=false`、`mcp_tool_trace=[]`；显式查询 `L_south`、`L_north` 均调用 `get_latest_gl02_value` 并返回最新采样，证明数据库点位可用，故障位于自然语言路由层。
- 同类失败覆盖 A-D 上升管煤气压力、理论燃烧温度、冷风管道压力、富氧流量/富氧率、喷煤设定、1/2号出铁口温度、三高度静压力、称量罐重量、最近日报和历史问答检索。
- 精确变量冲突：`P_blast_cold` 被短别名 `P_blast` 截获；`P_top_gas_A` 被短别名 `P_top` 截获。
- 时间意图冲突：未命中预取变量时，问题中的“最近/近期”会触发炉况上下文短路，使南北探尺趋势图、A-D 压力趋势图和最近日报不进入工具轮。
- 数据源边界：`Hopper_weight`、`H2_top` 显式工具查询当前无最新采样；CO/CO2/H2 口语查询虽有工具轨迹，但不能返回实时数值，不能与路由失败混为一类。
- 基准回归：既有 8 条 MCP 口语基准中 7 条按路由与数据事实通过；“最近一份日报”虽然 HTTP 成功，但没有工具调用，已从“成功”更正为失败。
- 完整失败矩阵和复测 Prompt 见 [MCP 可执行功能及口语调用模板](../PT/MCP可执行功能及口语调用模板.md#9-2026-07-15-修复前未成功调用-prompt-核查)。

## 2026-07-15 MCP 口语调用准确率修复部署

- 修复标记：`ERR-8093-MCP-COLLOQUIAL-002`，状态为已修复。
- 问答代理新增完整分点别名、标准变量精确匹配、A-D/双探尺/双出铁口/三高度分组解析和父子变量抑制，避免 `P_blast_cold -> P_blast`、`P_top_gas_A -> P_top`。
- 报表与历史问答改为直接工具意图，绕过传感器最新值预取；含“最近”的已知变量趋势请求不再被炉况上下文短路。
- MCP 图表目录必须位于前端静态根目录，URL 由实际相对目录生成；代理会把模型写出的旧图表目录替换为工具返回的权威 `/data/mcp_charts/...` URL。
- 本地验证：相关 Python 文件 `py_compile` 通过；`test_qa_latency_optimizations.py` 共 20 项通过。
- 真实 8093 最新值验证通过：南北探尺、A-D 上升管压力、冷风管道压力、富氧流量/富氧率、喷煤设定/实际值、双出铁口温度、三高度静压力、理论燃烧温度。
- 真实 8093 工具验证通过：南北探尺趋势图、A-D 压力趋势图、最近日报、历史问答；最终图表 URL `/data/mcp_charts/gl02_trend_20260715_080357_d4b0aab6.png` 返回 `200 image/png`，大小 `200380` 字节。
- 数据边界：`Hopper_weight`、`CO_top`、`CO2_top`、`H2_top` 路由已正确，但当前无最新采样。
- 发布脚本：[remote_deploy_8093_mcp_colloquial_fix.ps1](../tools/remote_deploy_8093_mcp_colloquial_fix.ps1)。部署过程只滚动重启 `BFV4PreviewProxy8093`，`BFV4PreviewWs8768` 全程保持监听；MCP 为代理按需启动的 stdio 子进程，没有独立 Windows 服务。
- 最终远端备份：`ollama_proxy_server.py.bak_mcp_colloquial_fix_20260715_080258`、`bf_data_mcp_server.py.bak_mcp_colloquial_fix_20260715_080258`。
- 最终 SHA256：代理 `FE54B2A2214E30221573D8DD5F12368B8611EA95EFEF759693044769160FDDD8`；MCP `A673BBF51A262F59C36AC7C6B4992EC9487DA8AD96CEAE9453946229B5091346`。
- 发布后状态：`BFV4PreviewProxy8093=Running`、8093 监听、8768 监听、HTTP 200。

## 2026-07-15 MCP 数据绘图扩展部署

- 需求标记：`REQ-8093-MCP-CHART-003`；首轮提前作答故障：`ERR-8093-MCP-CHART-EARLY-ANSWER-003`。
- `plot_gl02_trends` 保持旧参数兼容并新增 `auto/line/area/step/scatter/dual_axis/small_multiples`、移动平均、极值/最新值、参考线和工业/亮色/深色主题。
- 新增 `plot_gl02_analysis`，支持相关散点、相关矩阵、直方分布和箱线图；相关图返回同分钟对齐点数、Pearson 系数和线性拟合/矩阵派生信息。
- 首轮现网 6 条仅 3 条真正产图：模型在变量检索后提前回答，或把标准变量猜成 `T_hot_wind/P_wind/V_wind`、`P_diff_total/I_permeability`。修复后 `qa_mcp_chart_plan()` 直接根据口语选择标准变量和绘图工具，`qa_mcp_tool_loop_async()` 在 round 0 执行确定性绘图，不允许“正在生成”假完成。
- 本地验证：`test_mcp_chart_expansion.py` 6/6；`test_qa_latency_optimizations.py` 22/22；相关 Python 文件 `py_compile` 通过。远端 Python 3.11.9、Matplotlib 3.10.9。
- 真实 8093 验收：双轴、三分面、相关散点、相关矩阵、分布直方、箱线图 6/6 自动通过；报告：[8093_mcp_chart_expansion_remote_after_deterministic_20260715.json](../logs/8093_mcp_chart_expansion_remote_after_deterministic_20260715.json)。六张 PNG HTTP 均为 `200 image/png`，大小 `220251/227534/94587/72050/57069/49940` 字节。
- 视觉检查发现首版相关矩阵标题左侧裁切；默认标题已改用本地短时间格式并加宽画布，最终图片 `/data/mcp_charts/gl02_correlation_heatmap_20260715_151938_62e33f48.png` 为 `200 image/png`，专项报告：[8093_mcp_heatmap_title_retest_20260715.json](../logs/8093_mcp_heatmap_title_retest_20260715.json)。
- 绘图扩展后再次复测用户指定四条最新值问题，4/4 通过，数据时间为 `15:23-15:25`；报告：[8093_mcp_four_prompt_retest_after_chart_expansion_20260715.json](../logs/8093_mcp_four_prompt_retest_after_chart_expansion_20260715.json)。
- 发布脚本：[remote_deploy_8093_mcp_chart_expansion.ps1](../tools/remote_deploy_8093_mcp_chart_expansion.ps1)。发布只滚动重启 `BFV4PreviewProxy8093`；每轮发布前确认 8768 正在监听，`BFV4PreviewWs8768` 全程未重启。
- 最终备份：`ollama_proxy_server.py.bak_mcp_chart_expansion_20260715_151843`、`bf_data_mcp_server.py.bak_mcp_chart_expansion_20260715_151843`。最终 SHA-256：代理 `F2184B97C8F1FF90C9C16973DF4C32AC649932E452864720A80EAB945F74940F`，MCP `0977A7CA100FC75C6F8A29296AE29F7E9CEC90E749C355BF626689AE7ECDA753`。
- 最终状态：`BFV4PreviewProxy8093=Running`，8093/8768 均监听，HTTP 200。

## 2026-07-15 炉体温度热力趋势矩阵部署

- 需求：`REQ-8093-MCP-BODY-MATRIX-004`；故障：`ERR-8093-MCP-BODY-MATRIX-GATE-004`。
- 新增 `plot_gl02_body_temperature_matrix`：默认 7–16 层、A–F，支持 A–H；每格底色为当前温度、折线为最近时间窗、文字为当前值/采样时刻/升降趋势。
- 口语路由在工具门禁之前识别专用矩阵，避免无单变量请求被误判为普通炉况问答；事件结果采用紧凑单元格摘要，完整统计写 JSON 旁车。
- 本地 `py_compile` 通过，绘图与问答联合回归 32/32；真实原句工具验收 1/1，报告：[8093_body_temperature_matrix_prompt_visual_final_20260715.json](../logs/8093_body_temperature_matrix_prompt_visual_final_20260715.json)。
- 最终图片 `/data/mcp_charts/gl02_body_temperature_matrix_20260715_171338_c8e8e1a5.png` 返回 `200 image/png`，大小 `552160` 字节；JSON 旁车返回 200，60/60 点有效、0 缺测。
- 视觉检查后把色标移动到独立底栏，第 16 层不再被遮挡；本地验收副本：[最终矩阵图](../logs/gl02_body_temperature_matrix_20260715_171338_c8e8e1a5.png)。
- 发布脚本：[remote_deploy_8093_body_temperature_matrix.ps1](../tools/remote_deploy_8093_body_temperature_matrix.ps1)；最终备份时间戳 `20260715_171249`。
- 最终 SHA-256：代理 `A64D1862E0F3B816B45FFDBC5259632148C74D31CD349FC4794F21F4580CD527`；MCP `833F1BA9A9A00AEDD6F3F896DF30C2C2E64B953B428D0D514D0976DCA0E43E52`。
- 发布边界：只重启 `BFV4PreviewProxy8093`；`BFV4PreviewWs8768` 全程未重启，8093/8768 最终均监听，HTTP 200。

## 2026-07-16 通用单点/多点传感器查询与绘图部署

- 需求：`REQ-8093-MCP-GENERIC-SENSOR-005`；故障闭环：`ERR-8093-MCP-LAYER-SENSOR-GROUP-005`。
- MCP 新增 `query_gl02_sensors`，统一支持多传感器 `latest/history/statistics`、同窗参数和逐项部分失败。
- 8093 口语层新增运行期变量目录匹配，并把“7层炉温”展开 `T_body_L7_A-H`；不再把整层误当单一传感器。
- 多传感器绘图在单位缺失但数量级差异达到 20 倍时自动使用双轴/分面。
- 本地验证 38/38；真实口语 3/3，报告：[8093_generic_sensor_access_20260716.json](../logs/8093_generic_sensor_access_20260716.json)。最终绘图专项：[8093_generic_sensor_chart_final_20260716.json](../logs/8093_generic_sensor_chart_final_20260716.json)。
- 最终图片 `/data/mcp_charts/gl02_small_multiples_20260716_114257_ff84d6c1.png` 返回 `200 image/png`，大小 `243418` 字节；视觉检查三分面无曲线压平。
- 最终备份时间戳 `20260716_114218`；代理 SHA-256 `C2A9EEE2945546E92A05249F3CA29D4D529644DE8F4C6CFE7AD31FF7089DC35C`，MCP SHA-256 `D1F675B42370B02A969F8950988847A3045DC3DE27436A53B3854814D36F5BE2`。
- 最终状态：`BFV4PreviewProxy8093=Running`，8093/8768 均监听，HTTP 200；8768 未重启。

## 2026-07-16 炉身静压力 A–F 与 PostgreSQL 同步部署

- 需求：`REQ-8093-MCP-STATIC-PRESSURE-AF-20260716`。
- 新增18点扩展目录，三个高度各A–F六点；原三个平均代理保留。
- MCP/口语/绘图部署最终 SHA256：后端 `96EF3D1193364F97EECA5B6395CE6004014B5FE4701E7612D1FCB9DF037A93C5`；MCP `682F2233C8C726259C0AF038A10BE9006B40CD21FBECA277A5EC32FF6D3BD2FD`；helper `63FB2E25346AC1C46B65C2A5B33ECB511DB4A6B7618BA9E4E3A94226679D604E`；扩展目录 `9CF54085B22B3111E9F244E31FC762FC002606845AFA25D831B0E742365C68EC`。
- 最终8093备份时间戳 `20260716_125741`；服务 `Running`，8093/8768监听，HTTP 200。
- 同步目录备份：`点位清单.tsv.bak_static_pressure_af_20260716_125608`；最终目录SHA256 `47D1D9972B8981621A1C484BDA0550CAAC79953500FDC4B128C84C9E7A3F6734`。
- 同步验收：133个物理点全部成功、0错误；分钟聚合写入1518行；PostgreSQL新增点注册/启用/最近数据均18/18，最新时间 `2026-07-16 12:55:00`。
- 现网口语验收2/2：20.35米A–F最新值正确返回；23.49米A/C/F最近一小时趋势图成功返回 `/data/mcp_charts/gl02_line_20260716_125944_6420b1bf.png`。
- 发布只滚动重启 `BFV4PreviewProxy8093`；8768与PostgreSQL未重启。完整说明见 [专项文档](8093_MCP炉身静压力AF与PostgreSQL同步.md)。

## 2026-07-26 11434 单 27B 策略对 8093 的边界

- `BFOllama11434` 的实际 `OLLAMA_MAX_LOADED_MODELS` 已改为 `1`，两个 30.5B
  manifest 已隔离，11434 当前只驻留
  `chiqiong-blast-furnace:latest`。
- 8093 本次没有重启，操作前后监听 PID 均为 `14172`；因此没有把 8094 修复
  扩大成 8093 发布。
- 8093 当前环境原本已经固定 27B，所以不会因旧前端模型名加载 30.5B；共享
  后端文件已具备“只选择 `/api/ps` 中已驻留批准模型”的策略，待 8093 下次
  正常滚动重启时生效。
- 单驻留 11434 不适合再同时常驻 embedding。8094 已切到关键词知识检索；
  若 8093 后续仍需 pgvector 查询 embedding，应将 `nomic-embed-text` 迁到
  独立服务端口后再接入，不能恢复 30.5B 或把 11434 驻留数改回 2 来掩盖问题。
- 详情、验证和回滚位置见
  [Ollama 单模型修复说明](22012_Ollama_27B_30B自动拉起链路_20260726.md#2026-07-26-27b-单模型修复)。

## 2026-07-26 炉顶压力半小时趋势口语修复

- 故障提示词：`最近半小时炉顶压力如何？`。修复前只回答当前值，并误称
  没有连续半小时数据；数据库实际具有 `P_top` 分钟历史。
- 修复内容：数据意图识别增加“如何/情况如何/近况”，并为 `P_top` 增加核心
  传感器目录兜底；该问题现在调用 MCP `get_statistics`，默认窗口为 30 分钟。
- 受控部署时间：`2026-07-26 15:11:17`。备份后更新 8093/8094 共享 backend
  与 MCP 文件，只重启 8093 服务和 8094 计划任务。
- 部署前后受保护服务 PID：8768=`15668`、8770=`12956`，均保持不变。
  部署后 8093 PID=`17520`、8094 PID=`16252`。
- 现网精确回归：两端均 HTTP 200，识别变量 `P_top`、窗口 30 分钟、
  预取类型 `statistics`，回答包含 31 个分钟点汇总形成的范围、均值、起止值
  与趋势。
- 发布入口：[deploy_22012_top_pressure_mcp_fix.py](../tools/deploy_22012_top_pressure_mcp_fix.py)；
  脚本包含远端预编译、备份、失败回滚和 8768/8770 PID 守卫。

## 2026-07-26 MCP 受控自主工具编排发布

- 发布内容：模型多步分析路由、运行时 MCP Schema 校验、工具调用次数与参数规模
  上限、流式问答多轮工具闭环。
- 首次现网检查发现流式入口在目录检索第一轮后提前生成答案；修复
  [流式多轮开关](../高炉前端数据/智能助手/backend/ollama_proxy_server.py#L6412)
  后重新发布并通过。
- 最终发布时间：`2026-07-26 15:49:41`。8093 PID=`16020`、8094 PID=`2176`。
- 受保护服务：8768 PID=`15668`、8770 PID=`12956`，发布前后均未变化。
- 现网验收：8093、8094 都由模型自主执行两次目录检索和一次相关散点图工具，
  返回半小时真实数据相关系数 `-0.33` 与 `/data/mcp_charts/...png`。
- 规划与验收：
  [PT规划](../PT/MCP受控自主工具编排规划与通用流程.md)、
  [验收JSON](../logs/8093_8094_mcp_agent_orchestration_acceptance_20260726.json)。

## 2026-07-26 MCP自主编排低延迟发布与回退

- 性能问题：初版自主编排对相关性问题执行两次目录检索和一次分析绘图，并在
  工具间重复27B推理，8093/8094总耗时约38.57/45.22秒。
- 优化：常见相关性问题改为一个复合分析工具；27B规划使用温度0、360 tokens、
  最近6条精简消息且排除完整炉况/RAG；增加30秒缓存、2轮/4次限制和45秒预算。
- 最终发布时间：`2026-07-26 16:25:47`。8093 PID=`13368`，
  8094 PID=`3056`；8768 PID=`15668`、8770 PID=`12956`均未变化。
- 优化后：工具数`3→1`，8093样本15.43/25.94秒，8094样本16.32秒；
  相关系数和PNG均正常。固定P_top半小时统计仍自动通过。
- 性能升级前本地基线：
  [manifest.json](../backups/mcp_route_baseline_20260726_1552/manifest.json)。
  先运行`python tools/restore_mcp_route_baseline.py --verify`核验；确认需要回退时
  才运行`--restore`，完成本地测试后再执行受控部署。远端每次发布同时保留
  带时间戳`.bak_*`备份。
- 验收：
  [低延迟验收JSON](../logs/8093_8094_mcp_latency_optimization_acceptance_20260726.json)。

## 2026-07-26 统一业务对象目录发布

- 发布时间：`2026-07-26 17:34:39`。部署新增catalog目录、统一加载器和三个
  MCP目录工具，并更新8093/8094共享问答提示。
- 运行进程：8093 PID=`3304`、8094 PID=`16616`；受保护8768 PID=`15668`、
  8770 PID=`12956`，部署前后未变化。
- 现网验收：
  - 8093调用`search_business_objects("铁水硅含量")`，命中
    `hot_metal_chemistry`和`query_hot_metal_chemistry_by_heat`；
  - 8094调用`search_business_objects("散点图")`，命中
    `correlation_chart`和`plot_gl02_analysis`；
  - 两端HTTP 200、各一次工具调用、自动验收通过。
- 回滚：远端旧backend/MCP/policy保留时间戳备份；新增目录文件首次部署没有旧
  文件，部署脚本失败时会删除新增文件并恢复原程序。
- 证据：
  [验收JSON](../logs/8093_8094_business_object_catalog_acceptance_20260726.json)。

## 2026-07-26 结构化MCP追问与确定性批量查询发布

- 最终发布时间：`2026-07-26 18:21:19`。
- 发布文件：`ollama_proxy_server.py`、`mcp_conversation_context.py`、
  `mcp_tool_policy.py`、`bf_data_mcp_server.py`、统一目录加载器及catalog JSON。
- 最终端口PID：8093=`16636`、8094=`13924`、8768=`12672`、
  8770=`12956`；最终发布前后受保护8768/8770 PID不变。
- 远端三个核心文件SHA-256与本地一致，远端`py_compile`退出码0；诊断记录：
  [部署后诊断](../logs/22012_8093_phase3_postdeploy_diagnostic_20260726.json)。
- 关键四问改为一次批量`query_gl02_sensors`，4/4自动通过，中位总耗时约
  `1.12s`，回答保留正式中文名和单位。
- 多轮7/7：变量、时间和图表类型均按真实`tool_start.arguments`验收，PNG
  全部`200 image/png`。
- 纯MCP直连4/4：不经过Prompt、27B、问答路由或RAG；该模式只用于工具能力和
  延迟基线，不能替代生产口语E2E。
- 发布中一次`Start-Service`与自动值守竞争导致失败，脚本完成回滚；之后启动
  命令允许瞬态竞争，但仍以端口就绪和受保护PID为硬性通过条件。
- 独立部署间隔内8768曾被外部自动值守更换PID；不是最终部署动作造成。判断
  “8768未重启”时必须限定到单次部署前后，不得跨自动值守窗口笼统表述。
## 2026-07-27：8095 Billboard 点位聚焦与炉壳防穿透隔离发布

- 本轮只更新 8095 隔离预览：
  `http://10.30.220.12:8095/`。8094 的五个页面、入口、相机实现和问答服务均未
  修改。
- 页面入口为
  `高炉前端数据/frontend_dashboard_v3.8095_preview.server.html`，只在该入口
  加载 `高炉前端数据/assets/bf3d-billboard-focus-guard-8095.js`。
- 点击真实 Billboard 后，OrbitControls 的 target 从炉心切换到该点位；相机沿
  表面法线停在局部观察距离。滚轮 Dolly 前用 Raycaster 向炉壳求交，距离小于
  `1.2m` 时停止。退出点位聚焦后恢复原 target、相机和以炉心为中心的全景旋转。
- 远端真实交互验收点击 `T_body_L9_H`（炉腰温·L9H），初始聚焦距离约
  `4.4069m`，连续推进在 `1.2m` 触发 `collisionBlocked=true`，退出后恢复约
  `74.04m` 全景。
- 验收报告：
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\logs\acceptance\8095_focus_guard_20260727_r3\8095_focus_guard_acceptance.json`；
  七项断言全部通过。该结果是 Chromium 专项，不等于全站跨引擎矩阵。

## 2026-08-01：8094 炉壳表面相机中心与滚轮防穿透

- 用户已明确授权只修改 8094 总览 3D 相机行为；8093 主页面保持不变。
- 8094 页面加载独立运行时
  `高炉前端数据/assets/bf3d-surface-camera-guard-8094.js`，没有复用 8095 的
  Billboard 点击聚焦运行时。
- 新运行时从当前相机向炉心发射射线，以
  `IMG2THREEJS_FITTED_GL02_SHELL` 的首个真实交点作为 OrbitControls target，
  并只向壳外偏移 `0.06m`。滚轮不再以模型包围球中心为目标，平移关闭。
- 每次向内滚轮先向炉壳 Raycast；相机同一事件内至少保留 `1.2m` 炉壳法向间距。
  “全景”按钮重新计算表面 target，不会恢复为炉心 target。
- 远端 Chromium 真实验收保留 133 个 Billboard；连续推进后
  `collisionBlocked=true`，炉壳法向间距为 `1.200m`，相机到 target 约
  `1.235m`；规定 9 个 Chromium 视口全部无横向溢出、守卫就绪。
- 8093 主 HTML SHA-256 部署前后均为
  `4F0D80F45218565ABA5F8A0EB28C30882E10067745F88690B288039366C3CCEE`。
  8094 页面备份位于
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\8094_surface_camera_guard_20260801\frontend_dashboard_v3.8094_preview.server.html.20260801_021740.bak`。
- 机器报告与截图位于
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\logs\acceptance\8094_surface_camera_guard_20260801_r1`。
  本次未完成 Firefox、WebKit 和现场 Edge，不得表述为完整跨浏览器通过。

## 2026-08-01：8093 同步 8094 高炉本体并移除炉内仿真面板

- 远端 8093/8094 共用的
  `高炉前端数据/models/gl02_blast_furnace.glb` 已经是
  `GL02_FURNACE_BODY_R1.glb`，大小 `8,229,120` bytes，SHA-256 为
  `150DF18B68F0410B9A168F80C2DF34EFDA86384FC51D37226F81C2ACD244ED53`；
  本次没有重复复制或覆盖 GLB。
- 8093 页面通过 `tools/patch_8093_furnace_body_no_simulation.py` 补齐 21 个静压
  兼容点和两处 Three.js viewer 合同，加载 133 点 Billboard 适配器与端口作用域
  为 8093 的炉壳表面相机入口。
- 8093 没有加载 `bf3d-internal-simulation.js` 或
  `bf3d-internal-simulation.css`，并用 `#bf3d-8093-no-internal-simulation-panel`
  作为防御性样式；真实页面 `.bf3d-sim-panel` 数量为 0，
  `window.__BF3D_INTERNAL_SIMULATION__` 不存在。
- 首轮浏览器验收只出现 `/favicon.ico` 404；补充内联空 favicon 后重新执行完整
  矩阵，浏览器错误降为 0。该修正不产生外部资源请求。
- 真实 Chrome 最终结果：模型标识 `GL02_FURNACE_BODY_R1.glb`、133 点、
  `bf3d.surface-camera-shell-guard.8093.v1`、连续滚轮碰撞和 `1.2m` 炉壳安全
  间距全部通过；总览、炉况诊断、参数优化建议、趋势分析、智能问答在 9 个规定
  Chromium 视口共 45/45 通过，无页面横向溢出且正式头部可见。
- 最终 8093 HTML SHA-256：
  `5C536EF11B83A73BCA5B7363C348463E69F9881B860BA8E1AA1F963E6C6C6146`。
  完整回滚基线：
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\8093_furnace_body_no_simulation_20260801\frontend_dashboard_v3.server.html.20260801_023920.bak`；
  favicon 修正前备份为同目录 `frontend_dashboard_v3.server.html.20260801_024419.bak`。
- 验收 JSON 和截图：
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\logs\acceptance\8093_furnace_body_no_simulation_20260801_r1`。
  临时计划任务和 `F:\bf8093_sync_accept` 已删除；正式 8093 服务没有重启。
- 本轮没有执行 Firefox、WebKit 和现场 Edge，不能表述为完整跨浏览器验收。

## 2026-08-01：8093 炉壳近裁剪视觉穿透修复

- 现象不是相机坐标越过炉壳，而是
  `bf3d-furnace-body-billboard-adapter.js` 的全模型包围球适配把
  `camera.near` 计算到约 `26.67m`；防穿透守卫随后把相机停在炉壳外
  `1.2m`，但没有同步恢复投影矩阵，导致近处炉壳被裁掉、后方梯道和环形构件
  仍可见，视觉上像进入炉内。
- `bf3d-surface-camera-guard-8094.js` 新增端口条件
  `PORT_SCOPE === "8093" ? 0.05 : null`。8093 在表面 target、滚轮推进和
  80ms 周期守卫中锁定 `camera.near=0.05m` 并调用
  `camera.updateProjectionMatrix()`；8094 默认分支没有改动。
- 8093 入口缓存版本更新为 `20260801-8093-near-plane-r4`。远端实际文件与
  HTTP 返回均通过探针：入口 SHA-256
  `01231424EBA892B79B029AC73B4C011914CC72BD52A6D3C1509A638632D462F9`，
  共享守卫 SHA-256
  `0C331DE1DCEBB3DADAE5C031F29686A00A715197732115005F162A8DD23AB929`。
- 8093 主页面的外层相机入口也从 `20260801-8093-sync-r1` 更新为
  `20260801-8093-near-plane-r4`，避免已有浏览器继续命中旧模块缓存；主页面
  SHA-256 为
  `8DF482B78B9ED548BDC080D68A35293964B34C0973F79D1FF458C2D32401B9D6`，
  HTTP 200 且返回内容包含新版本标记。
- 回滚目录为
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\8093_near_plane_fix_20260801\20260801_050810`，
  内含变更前的两个相机资源文件。
- 主页面缓存版本变更的独立回滚目录为
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\8093_near_plane_fix_20260801\20260801_053530`。
- 220.12 真实 Chrome 核心回归：133 点完整；10 次标准 `WheelEvent` 后
  `collisionBlocked=true`；炉壳法向间距 `1.200m`；相机到 target
  `1.235270675m`；`cameraNear=0.05m`、`safeNearPlane=0.05m`；仿真面板 0；
  浏览器错误 0；10 项核心断言全部通过。证据在
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\logs\acceptance\8093_near_plane_fix_20260801_r8`。
- 本轮改动不涉及布局，继续引用同日
  `8093_furnace_body_no_simulation_20260801_r1` 的 45/45 Chromium 视口基线。
  服务器无界面 Chrome 的整页截屏、canvas 导出和尺寸切换均超过用户规定的
  单次 5 秒等待上限，因此本轮没有新视觉截图；需要用户强制刷新 8093 后进行
  现场视觉复核。Firefox、WebKit、现场 Edge 仍待补。
- 验收创建的 `F:\bf8093_near_plane_accept_20260801*` 临时配置目录已删除，
  相关临时 Chrome 进程剩余数量为 0；8093 正式服务没有重启。

## 2026-07-27：8891 炉次多源仪表盘独立升级

- 8891 继续使用独立计划任务
  `\BlastFurnaceServices\StandaloneHeatDashboard8891` 和独立目录
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\standalone_heat_dashboard_8891`。
- 页面接入 31 个白名单数据集，支持任意 `[start,end)` 时间窗、炉次号、检索、
  变量、分页、CSV/XLSX 导出和 pSpace 聚合；不接受浏览器任意 SQL/URL/tag。
- 正式铁水化验映射固定为 `heatno=meltno` 且 `batchno=batchno` 双键精确连接；
  试样号推断仅保留历史审计兼容用途。
- 生产凭据只位于 ACL 受限且 Git 忽略的 `PT/external_sources.local.env`。
  Vastbase 双账号、PostgreSQL 和 pSpace 已实读；IMES Web 现有资料没有可验证
  密码，页面显示 `authorization_required`，没有伪造接通。
- 专项说明：
  [22012 8891 炉次多源数据仪表盘](22012_8891炉次多源数据仪表盘_20260727.md)。
- 远端 Chromium 九视口实测中，首轮发现 390/375 手机表单横向撑宽；修复
  `heat_data_explorer.js` 的网格最小宽度后 9/9 通过。机器报告和截图在
  `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\logs\acceptance\8891_multisource_20260727`。
- 专项验收结束后，临时计划任务
  `\BlastFurnaceServices\CodexVerify8891MultiSource` 及两个临时 runner 已删除；
  正式 8891 任务保持 Running，监听 `0.0.0.0:8891`。

## 2026-08-01：8093 全炉旋转与点位动态聚焦双模式

- 新运行时为
  [bf3d-surface-camera-guard-8093.js](../高炉前端数据/assets/bf3d-surface-camera-guard-8093.js)，
  schema `bf3d.camera.dual-mode.8093.v2`；它是 8093 独立资源，不引用 8094。
- `overview`：`OrbitControls.target` 为炉体中心；`minDistance` 使用能够包住整个炉体
  的整体包围半径；`minAzimuthAngle=-Infinity`、`maxAzimuthAngle=Infinity`，允许持续
  360° 旋转。运行时公开 `overviewAzimuthTravelDegrees` 供验收连续方位行程。
- `focus`：点击真实 Billboard 后寻找其背后的炉壳交点；相机每次旋转都从当前
  方位向炉壳重新 Raycast，更新表面点和外法线。滚轮进入前保持 `1.2m` 安全间距，
  `camera.near=0.05m`。退出按钮、Esc 和全景复位恢复炉心模式。
- 页面资源版本由
  [patch_8093_furnace_body_no_simulation.py](../tools/patch_8093_furnace_body_no_simulation.py)
  固定为 `20260801-8093-dual-camera-r5`。
- 远端部署必须使用
  [remote_deploy_8093_dual_camera_modes.py](../tools/remote_deploy_8093_dual_camera_modes.py)：
  它先备份 8093 页面与相机资源，再原子替换，并校验 8094 页面和共享运行时哈希未变。
- 截至本记录，220.12 SSH 在用户规定的单次不超过 5 秒窗口内出现协议头超时，
  SMB 管理共享认证返回访问拒绝；因此本轮没有覆盖远端文件，8093 仍为历史
  `near-plane-r4/v1`，8094 未变化。不得把本机专项结果写成远端已上线。
- 通道恢复后的顺序：上传 r5 运行时和原子部署器 → 执行部署器 → 检查 8093 HTTP
  包含 r5/v2 标记 → 运行浏览器验收器验证全炉累计方位、动态表面更新、滚轮防穿透、
  退出复位和无仿真面板 → 再校验 8094 哈希保持不变。

## 2026-08-01：8093 121 点与仅全炉旋转模式（已部署）

- 本节取代上面的“双模式”目标：8093 已取消 Billboard 点位聚焦，只保留炉心全景 360° 旋转与整体包围半径防穿透。Billboard 悬停和实时数值仍保留。
- 三维场景显示 121 点：80 炉体温度、18 静压力、2 出铁口温度、21 设备/管线/炉顶实测点。12 个计算、设定、汇总或兼容点仅从三维场景移除；左侧 28 变量面板完全不改。
- 8093 专用资源：[点位筛选器](../高炉前端数据/assets/bf3d-physical-point-filter-8093.js) `bf3d.measured-point-filter.8093.v2`；[全景相机](../高炉前端数据/assets/bf3d-surface-camera-guard-8093.js) `bf3d.camera.overview-only.8093.v3`。
- 远端部署器：[remote_deploy_8093_physical_points_overview.py](../tools/remote_deploy_8093_physical_points_overview.py)。部署根目录 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW`，备份 `backups\8093_measured121_overview_20260801\20260801_202127`。
- 保护结果：8094 页面、共享 `bf3d-furnace-body-billboard-adapter.js`、8094 相机运行时的部署前后 SHA-256 一致。8093 HTTP 已返回筛选版本 `20260801-measured121-r2` 和相机版本 `20260801-overview-only-r6`。
- 回退：先停止 8093 服务写入，再从上述备份目录恢复 `frontend_dashboard_v3.server.html`、`assets/bf3d-physical-point-filter-8093.js` 与 `assets/bf3d-surface-camera-guard-8093.js`；不得恢复或覆盖 8094 文件。

## 2026-08-02：8093 七类外围工艺汇总浮层放大（已部署）

- 调整对象仅为总览高炉四周的 7 张 `.furnace-layer-callouts.follow-model` 工艺汇总卡；不是 121 个炉体 Billboard，也不改左侧 28 变量。
- 8093 专用 CSS：[bf3d-furnace-summary-readability-8093.css](../高炉前端数据/assets/bf3d-furnace-summary-readability-8093.css)，版本 `20260802-expanded7-r2`。`.layered-cad-stage` 前缀保证覆盖权重高于运行时追加的旧 116px 规则；桌面卡宽 `208–242px`，紧凑桌面 `198px`，低高度 `190px`；取消省略号，单位在所有断点保持显示。
- 守卫部署入口：[remote_guarded_deploy_8093_furnace_summary_readability.ps1](../tools/remote_guarded_deploy_8093_furnace_summary_readability.ps1)。它只停启 `BFV4PreviewProxy8093`，要求 8768 在停机、写入、恢复全过程保持监听。
- 原子部署器：[remote_deploy_8093_furnace_summary_readability.py](../tools/remote_deploy_8093_furnace_summary_readability.py)。自动备份到 `backups\8093_furnace_summary_readability_20260802\<时间戳>`，并保护 8094 页面、共享 adapter、8094 相机资源哈希。
- 验证：本地相关测试 `11 passed`；守卫暂停/恢复、8768 不变、8093/8768 监听均为 true；线上 HTML/CSS HTTP 200，页面已引用缓存破坏版本。最终页面 SHA-256 `F308993A...24FC1`，CSS SHA-256 `BDA77396...EE48`，备份 `backups\8093_furnace_summary_readability_20260802\20260802_210417`。Chrome 页面重载本轮因重资源等待超时，新的 DOM 尺寸和截图矩阵待补。

## 2026-08-02：初始构图相机更新的守卫操作

- 固定入口为 [remote_guarded_deploy_8093_initial_camera_framing.ps1](../tools/remote_guarded_deploy_8093_initial_camera_framing.ps1)。它只停止/启动 `BFV4PreviewProxy8093`，不得停止 `BFV4PreviewWs8768`。
- 远端写入前必须上传 `bf3d-surface-camera-guard-8093.js` 和 `remote_deploy_8093_initial_camera_framing.py` 到用户临时目录；部署器只允许改 8093 页面相机标签和 8093 相机文件。
- 本轮实测：部署前服务 Running；守卫暂停、恢复均成功；8768 始终监听；8093 恢复后 HTTP 200。备份在 `backups\8093_initial_camera_framing_20260802\20260802_213047`。
- 回滚时同样先停 8093 守卫，只从该备份恢复页面与相机两个文件，再恢复守卫并核查 8768 未变；不得回滚模型、点位筛选、tooltip、汇总卡 CSS 或任何 8094 文件。

## 2026-08-04：8093 底部空白只读排查与 8094-only 修复

- 根因：页面末尾 `ops-cad-ghost-bands-fix` 为 `.cad-furnace-viewer` 设置 `bottom:5%`，紧凑视口为 `bottom:6%`，导致 3D 画布底部与 `.furnace-stage-3d` 不重合，出现截图中的底部空白带；不是 GLB 炉底缺失。
- 8093 处置：只读 HTTP/DOM/源码检查，未暂停 `BFV4PreviewProxy8093`，未修改 8093 页面和专用资源，未停止 `BFV4PreviewWs8768`。
- 8094 部署：使用 [8094-only 部署器](../tools/remote_deploy_8094_cad_bottom_band.ps1)，只追加 `bottom:0` 的幂等最终覆盖样式；备份在 `backups\8094_cad_bottom_band_20260804\20260804_124719`。
- 验证：8094 HTTP 200、`V3AutoPreviewProxy8094=Running`；8093/8094/8768/8770 PID 均保持；8093 返回内容前后一致。完整记录见[交接说明](handoffs/2026-08-04-8094-cad-bottom-band-fix.md)。
