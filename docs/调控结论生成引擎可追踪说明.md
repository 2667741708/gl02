# 调控结论生成引擎可追踪说明

本文档记录完整建议引擎从需求契约到代码、8767 payload、前端展示和验证脚本的追踪关系。后续修改动作模板、严重度、安全门禁、8767 字段或前端优化建议页时，必须同步更新本文、`docs/automation_traceability.md` 和对应测试。

## 需求 ID

`REQ-OPT-FULL-ENGINE-20260715`、`REQ-THREE-RULES-RECOMMENDATION-ALIGNMENT-20260804`

目标：恢复完整调控结论生成模块，并把动作真相源从八类静态模板升级为《三规二制》的上部、下部、负荷、碱度四类策略；每条动作返回四态和完整审计字段。前端只消费后端结构化结果，不再自行拼造固定分数或静态建议。

## 当前结论

已落地的独立代码目录名为 `调控结论生成引擎`，不是 `suggestionengine`。如果后续需要英文别名，应只作为文档别名或环境变量说明，不另建第二套漂移实现。

## 程序位置

| 层级 | 文件 | 职责 |
|---|---|---|
| 引擎入口 | [core.py](../调控结论生成引擎/recommendation/core.py) | `RecommendationEngine.generate()`，整合策略评估、冲突、顺序、安全门禁和兼容输出 |
| 四类策略目录 | [three_rules_two_systems.yaml](../调控结论生成引擎/policy/three_rules_two_systems.yaml) | 保存原文章节、动作、幅度、输入、观察窗口和审批要求 |
| 动作合同 | [action_contract.py](../调控结论生成引擎/recommendation/action_contract.py) | 固定四态与全部审计字段，执行只读/状态一致性校验 |
| 策略评估 | [policy_evaluator.py](../调控结论生成引擎/recommendation/policy_evaluator.py) | 同时评估主次炉况、四类调剂和碱度触发 |
| 冲突解析 | [conflict_resolver.py](../调控结论生成引擎/recommendation/conflict_resolver.py) | 处理相反方向动作与安全优先级 |
| 顺序规划 | [sequence_planner.py](../调控结论生成引擎/recommendation/sequence_planner.py) | 固化两条热制度动作链并保留跳过步骤 |
| 八类目标模板 | [action_templates.py](../调控结论生成引擎/recommendation/action_templates.py) | 只保留八类炉况目标、观察项和旧接口兼容信息，不再是真相动作源 |
| 严重度分级 | [severity_mapper.py](../调控结论生成引擎/recommendation/severity_mapper.py) | 将主诊断分数映射为 `watch/warn/high/critical` 等严重度 |
| 主次组合摘要 | [combination.py](../调控结论生成引擎/recommendation/combination.py) | 对任意主次炉况生成组合标识，不再只有两个组合 `if` |
| 安全门禁 | [safety_gate.py](../调控结论生成引擎/recommendation/safety_gate.py) | 高压、悬料、顶温、风口、TRT 安全约束和强制动作 |
| 输出格式 | [formatter.py](../调控结论生成引擎/recommendation/formatter.py) | 保留完整 `actions[]`，派生旧版三组动作数组 |
| CLI | [main.py](../调控结论生成引擎/main.py) | 本地输入诊断 JSON 输出 recommendation JSON |
| README | [README.md](../调控结论生成引擎/README.md) | 引擎用途、运行示例和只读安全边界 |

## 8767 接入

8767 WebSocket 桥接通过 [recommendation_adapter.py](../自动诊断服务/recommendation_adapter.py) 适配数据库诊断快照和当前值：

- 默认引擎目录：`PROJECT_ROOT / "调控结论生成引擎"`。
- 可选覆盖变量：`BF_RECOMMENDATION_ENGINE_DIR`。
- 引擎版本：`v5-three-rules-two-systems`。
- 策略合同：`three_rules_two_systems.v1`。
- 输出元数据：`engine_meta.name=blast_furnace_recommendation_engine`、`engine_meta.source=diagnosis_snapshot`、`engine_meta.read_only=true`。

[local_pg_ws_bridge.py](../自动诊断服务/local_pg_ws_bridge.py) 在生成诊断 payload 时调用 `generate_recommendation(payload, current_values)`，成功时写入：

```json
{
  "recommendation_status": {
    "state": "ready",
    "engine": "blast_furnace_recommendation_engine",
    "version": "v5-three-rules-two-systems"
  },
  "recommendation": {
    "schema_version": "three_rules_two_systems.v1",
    "goal": "...",
    "actions": [
      {
        "id": "LOW-TBLAST-UP-06",
        "scheme": "lower",
        "status": "eligible",
        "source_refs": ["5.1.8.4", "5.3.4.1.1", "5.3.4.1.6"],
        "trigger_evidence": [],
        "preconditions": [],
        "blocking_reasons": [],
        "delta": {"min": 10, "max": 20, "unit": "℃"},
        "sequence": {"rank": 10, "position": 1},
        "missing_inputs": [],
        "observation_window": {"min_minutes": 15, "max_minutes": 60},
        "approval": {"required": true, "role": "值班工长"},
        "read_only": true
      }
    ],
    "immediate_actions": [],
    "followup_actions": [],
    "forbidden_actions": [],
    "observe_items": [],
    "safety_gate_passed": true,
    "safety_warnings": [],
    "engine_meta": {
      "read_only": true
    }
  }
}
```

失败时 8767 不伪造建议，而是移除 `recommendation` 并写入 `recommendation_status.state=failed` 和 `error_type`，供前端显示错误或降级状态。

## 前端绑定边界

[frontend_dashboard_v3.server.html](../高炉前端数据/frontend_dashboard_v3.server.html) 的优化建议页应读取 `diagnosis.recommendation` 与 `recommendation_status`。前端允许做展示排序、中文格式化、置信度归一化和空/错态提示，但不得自行发明动作分数、风险贡献或替代后端标准建议。

前端契约信号：

- 文件包含 `REQ-OPT-FULL-ENGINE-20260715`。
- 优化建议页绑定 `OptimizationTab=OptimizationEngineCockpitLayout;`。
- 置信度通过 `bfRecommendationConfidence()` 归一化。
- 组件内不得再使用旧 `a.score` 作为动作分数，也不得显示“风险贡献”。
- 当前炉况卡同时显示 `diagnosis_ts` 与传感器最新时间；诊断按5分钟档位落库，传感器按1分钟更新，两者不能混作同一更新时间。

## 安全边界

引擎只生成只读建议，不连接、不调用、不暴露任何生产控制写接口。所有强动作必须由现场制度和值班长/高炉长确认。

固定安全门禁包括；门禁动作现在使用唯一策略 ID，也出现在完整 `actions[]` 中：

- 高压运行：禁止直接放风坐料或休风，必要时先改常压。
- 悬料征兆：先解除悬料再考虑休风，并复核探尺、风压、压差和透气性。
- 顶温超限：顶温超过 350 摄氏度时安全门禁不通过，优先控顶温和减风。
- 风口异常：严禁强行恢复风量，优先检查风口防灌渣。
- 管道或悬料伴随 TRT 运行：先解列 TRT。

## 验证命令

```powershell
$OutputEncoding=[Console]::OutputEncoding=[Text.UTF8Encoding]::new($false)
python tools\verify_8093_recommendation_engine_contract.py
python tests\test_three_rules_recommendation_engine.py
```

验证覆盖：

- 八类炉况都能生成结构化动作。
- 任意主/次炉况通过通用合并规则生效，不再依赖两个组合特判。
- 高压、顶温等安全门禁会置前强制动作。
- 每条动作固定返回四态、来源、证据、前置、阻断、幅度、顺序、缺失数据、观察窗口和审批。
- 8767 payload 包含 `recommendation_status.state=ready` 和 `engine_meta.version=v5-three-rules-two-systems`。
- 前端使用完整建议引擎组件，不再使用旧动作分数契约。

## 修改清单要求

后续变更必须同步检查：

1. 修改动作或门禁：更新 `调控结论生成引擎/recommendation/*`、本文、PT 说明和验证脚本。
2. 修改 8767 字段：更新 `recommendation_adapter.py`、`local_pg_ws_bridge.py`、前端读取逻辑、测试和 API/追踪文档。
3. 修改前端展示：保留后端 recommendation 为唯一建议来源，补充浏览器验收截图或说明未跑浏览器的原因。
4. 修改安全门禁：必须确认只读边界不变，不能新增生产控制写入。

## 当前部署边界

2026-08-05 已在本地补齐八炉况 `recommendation_bundle`、完整动作详情展开和后台模型只读复核。Chromium 九视口及炉况切换通过隔离夹具验收；Firefox/WebKit 与现场 Edge 仍未运行。代码没有部署或重启 220.12 的 8768/8093，远端现网页面仍以实际已部署版本为准。
