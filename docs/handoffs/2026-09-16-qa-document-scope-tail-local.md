# 2026-09-16 原文章节末页遗漏：本机候选复现与修复

- 状态：local_candidate_verified_not_deployed；本轮远端当前状态未能核实。
- 事项：BUG-QA-SELECTED-SCOPE-TAIL-20260916；关联QAOPT-K03/K06、REQ-QA-FULL-ISSUE-INVENTORY-20260916。
- 权威来源：现有V10–V14交接、V15未提交候选、本机合成反例与pytest。未读取或公开生产原文。
- 边界：只继续本机候选。保留进入本轮前已有修改，不上传服务器、不发问答、不停启服务。

## 恢复到的任务状态

本机权威交接已到V14；旧自动跟进Prompt中的V2仅是历史采集基线，不允许以旧V2覆盖/回滚当前版本。
本机记录的V14生产提交为06db3d6db6371af381816cc1523cc294c0981a18，此次未通过远端探测重新确认。
原1259条结果加1条未知发送已归档，未知发送不得重放。后续知识来源已审计为803有效、30个oracle冲突，区别于最初818唯一执行题/15重复来源口径，详见V10–V14交接。

本轮Reliable SSH两次只读进度/哈希查询、一次身份探测和一次路由身份探测均在工具层30秒超时。
没有据此断言服务器宕机、生产故障或版本回退；没有尝试直连SSH、Paramiko读取或重新发送问答。
必须恢复Reliable SSH身份确认后，才能继续实时哈希、只读候选及生产验收。

## 新反例

独立权威原文含第一部分和末尾第二部分；章节索引有两部分，原子索引另保留第二部分。
删除章节索引的第二部分，但保留原子索引。原有全索引覆盖门仍通过，因为缺页内容在另一个索引中存在；
页面渲染只读取第一部分，却返回completed/verified_original_page。

这证明“全文在所有索引中能找到”不能证明“本次选择的章节索引足以输出完整答案”。
修复前已有36项相关测试通过，新增反例仍能触发，不能用旧测试全绿代替该完整性检查。

## 本机修复

- 在权威原文成员关系与全局覆盖检查后，对所选岗位/规程执行跨索引一致性检查。
- 原子/主题索引中属于同一所选范围的原文行，必须由本次章节片段覆盖；其他岗位/规程不扩大用户请求范围。
- 缺行返回dependency_blocked / selected_scope_missing_original_lines，不标完整。
- 同时拒绝全空白权威文本和全空白片段。
- 原文成员关系、作用域元数据真实性、原子条款自身边界仍是不同门槛；本次不宣称全部知识语义/全文完整性已证明。

## 验证

- python -X utf8 -m pytest tests/test_qa_document_integrity.py tests/test_qa_document_knowledge.py -q
- 结果：38 passed。
- 新反例修复后：删除前completed；删除后dependency_blocked；缺尾却标完整=false。
- 补充回归：其他规程的原子条款不会污染所选规程；全空白文本不能通过。
- 原始反例仅使用合成文本，远端写入0，问答POST 0。

## 后续门槛

1. Reliable SSH恢复后先核对真实生产版本，不能直接使用历史V2或仅凭V14本机记录。
2. 对实际索引运行只读候选检查，特别核对合法非连续聚合/标题是否触发新的保守阻断。
3. 补原子条款边界、角色/规程元数据与权威原文独立对应的验收，不能把交叉索引一致性当完整证明。
4. 通过既有803有效题覆盖及定向原文终态检查后，再按受控8093流程决定候选发布。
5. 33项整体任务继续执行；不将本地38项通过写成线上通过。
