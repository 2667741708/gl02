# 模型固定测试窗口与V24完善计划

- 状态：方案已冻结；模型/计划任务操作尚未获本轮独立授权，未执行。
- 最后核对：2026-09-17。
- 需求：OPS-QA-FIXED-MODEL-TEST-WINDOW-20260917、REQ-QA-EVIDENCE-PRESERVATION-20260917。
- 权威来源：[V23部署与8题全文核验](2026-09-17-qa-v23-single-window-routing.md)、Reliable SSH任务动作/模型标签/驻留探测、冻结计划和claim。

## 1. 已证明的问题

批准Qwen可以驻留；“驻留波动”指内存中的实际权重暂时为空、标签和驻留digest不匹配，或版本在请求前后不同。不是禁止Qwen，也不是单纯HTTP不可达。本轮第8题后只能确认身份漂移或无法核实，不能断言当时一定从哪份权重切到了哪份。

实际模型选择恢复任务`\BlastFurnaceServices\BFOllamaModelSelectionRecovery`调用`F:/Ollama/model-switch/manage_ollama_model_switch.ps1 -Action Repair -CatalogPath F:/Ollama/model-switch/model_catalog.json`，控制模型互斥锁`Global\BFOllamaModelSwitch`。Repair的首选/回退、latest标签和预热形成独立控制来源，可能干扰测评固定权重。其脚本内部还临时管理健康检查任务，所以不能未经审查直接调用Repair/Activate，也不能强杀运行中的Repair。

## 2. 需要独立授权的最小操作

仅在一次最多120分钟的测评窗口中：

1. Reliable SSH只读确认任务定义/启用/运行状态、当前批准权重、latest与驻留digest、11434 PID、全部保护端口、健康检查状态，保存脱敏指纹和受控私有原状态。
2. 只临时禁用`BFOllamaModelSelectionRecovery`的后续触发；不强行停止运行中的Repair。等待其自行完成并确认健康检查状态恢复到窗口前状态，最长10分钟，超时恢复原启用状态并退出。
3. 使用已经存在的批准Qwen `chiqiongblastfuenace:0`，digest `9111be230d48e53a385a28930fb8cf6972767c83e93c0db51f6b331a534e30fb`。如latest已经指向它，不重复别名操作；否则仅将latest绑定该现存权重，保存原标签绑定用于恢复。不下载或更换权重，不修改11434服务环境。
4. 允许一次受控驻留预热；确认11434同一PID、单模型、批准digest与标签一致。预热不是回归题，不覆盖或重发任何已有问题。
5. 根据全部durable claim和结果，只生成399道明确未发送原失败题的新续跑计划，保持原case_id/问题/标准，逐题单POST。已发8题及首次未知题禁止自动重放；任何版本漂移、取消或传输未知均停止并留证据。
6. 窗口由独立远端受控执行器运行，超时/失败/正常结束都在`finally`恢复原任务启用状态和原标签绑定，验证原状态；不创建新的计划任务。若恢复验证失败，停止测试并通知，禁止继续写入。
7. 验收8093/8094/8768/8770/5432/11434 PID保持原值；数据库、11434服务、健康检查任务定义/启用状态、模型文件及其他任务不作主动修改。

实施前还须核对现有模型管理器的恢复接口并审查受控窗口脚本。若无法在这些边界内安全保留/恢复任务与标签，则停止，不以别的模型管理动作扩大授权。

## 3. 399题收集后V24逐项修复

|问题|实现接缝与措施|逐项验收|
|---|---|---|
|明确历史小时漏查询|`qa_task_plan.py`增加服务端时区的相对日期/中文时钟解析；用类型化start/end和跨日校验，原理/引用/禁工具仍排除|“昨晚八点到九点”等原题进入精确历史窗口；日期确实歧义才澄清；不取当前快照代替|
|预取成功值在后续失败后丢失|代理现有预取、`qa_verified_facts.py`及证据策略形成统一只追加证据账本，成功预取登记variable/source/time/unit/quality；后续同请求复用，不重复规划最新值|最新温度原题最终含已取得值和真实时间；后续工具失败仍保留，不能只返回点位目录|
|稳定性问题只有数据堆叠|固定计算器先生成标准差/极差/首末/回归/最近窗口及适用性；模型只解释已登记事实，无正式阈值时明确描述性判断和风险判断的差别|数值复算一致，回答“观察到的波动/是否足以判定”，缺阈值不冒充正式稳定等级；不取消可信守卫|
|多点图请求对象混淆|点位目录解析高度与A-F方位，明确三个高度的全部方位组；复用受控图表/序列能力，图表不可用则给有来源的数据及具体限制|原题对象完整、窗口正确；不得把A-F叫六个高度；不把禁代码误当禁查询/禁图表|
|仍提出提供代码|`qa_evidence_policy.py`以及最终能力校验同时覆盖生成/执行/提供示例；拒绝代码子任务不影响查询/统计/图表子任务|所有角色和降级回合均不提出代码；普通数据分析和界面图表继续允许|
|未知单位与比值误用|权威目录补登记单位与量纲来源；计算器不得靠变量名把猜测单位冒充已核实|每个数值绑定单位来源；温标/非正均值/近零均值不以CV作稳定等级；原始测量不平移/取绝对值|
|基线质量缺口|将coverage/有效时段/截断/缺失原因保存到私有采集结果，公开只放审计状态与哈希|可区分真实基线不足和程序取数失败，缺证据不判正常；数据足够则完成对比|
|知识标准冲突|知识文档版本/岗位/章节/条款/表格范围与oracle分开审查|30冲突单列；经权威确认再更新标准；原文覆盖不等于语义正确|

V24必须从当前生产字节构建，写集和最小读集冻结、独立审查、8093受控部署后，只对修复覆盖的明确失败/部分题做新版原题对比。当前V23的已发题不原地重试。

## 4. 最终交付指标

逐题发布首次失败→V23/V24通过、部分、失败、依赖阻断；绑定程序/Prompt/模型digest和结果哈希。完整有用回答率、正确完整率、传输率分别统计。按知识/实时数据/历史/图表/用户给定数据/无工具解释分层；未发送、未审核、标准冲突不算通过。模型不同的对比明确为分层观察，不能称固定模型的因果改进。

828题Luna实际阅读全文审核仍独立进行；拒绝默认passed初筛的370通过/50.20%作为准确率。该集合的真实新增失败要单独扩展回归和分母，不能悄悄并入本次405基线。
