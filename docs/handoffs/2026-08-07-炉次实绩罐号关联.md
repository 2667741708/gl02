# 炉次实绩罐号关联补充

需求标识：`REQ-8093-HEAT-PERFORMANCE-QUALITY-QUERY-20260807`

`db_dashboard/heat_service.py` 的炉次同步现在复用 IMES 已核验的精确口径：

```text
hot-metal sample batchno -> public.v_qpes_mat_final.batchno -> thankno
```

查询使用每个 `batchno` 最新 `publishtime` 的 `thankno`，不会把试样序号或出铁段序号冒充罐号。映射失败时仍保留化验结果，并由 8093 页面明确显示“罐号未提供”。

这使“炉次实际”可以在同一炉次展开查看多条样本的真实 C/Si/Mn/P/S 和对应铁水罐号；出铁称量段仍单独展示，避免把称量段和化验样本错误一一对应。

本机验收：`pytest -q tests/test_heat_performance_quality.py tests/test_heat_performance_quality_query.py tests/test_heat_tank_mapping_contract.py tests/test_heat_service.py`，结果 `17 passed`。
