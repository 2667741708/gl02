# 正式书名、完整章节请求及子句规程作用域

状态：本机378项与独立静态复审通过，V34-r1私有候选冻结；生产未应用。
最后核对：2026-09-17。需求：REQ-QA-FORMAL-DOCUMENT-SCOPE-20260917。
关联：QAOPT-K01/K02/K03/K04/K06。权威来源：当前代码、隔离原生PG反例及合并回归、私有实际原源读取保真验证。
适用边界：确定性文档身份与选择范围；不代表所有口语形式、岗位/规程语义或真实助手准确率已经通过。

## 实际问题与修复

1. **引号书名字串误绑**：旧入口把包含“三规二制”的任何《书名》映射为正式原书，其他公司的书或讲义可能借用本书。现在精确注册三规二制、冀钢炼铁三规二制及高炉事故处理；未知引号书名在任何DB查询前返回document_reference_unresolved，不借用正式资料。
2. **多个章节只取首个**：旧re.search只匹配第一个“第几章”；首章有文本即可completed，另一个请求范围遗漏。现在完整解析独立章节、并列列表及正向区间，按核验目录选择全部；未知编号、倒置区间或上限越界明确澄清，不省略后宣布完成。
3. **编号覆盖岗位冲突**：旧逻辑让章节编号覆盖已提岗位。现在编号前后紧邻的岗位必须与其目录相容；冲突返回chapter_reference_conflict。独立子句中另一岗位与已选章节可以合并，避免把合理多范围问题误拒。
4. **原子条款只认岗位文本**：明确章节编号也能提供唯一岗位给原子条款匹配。保留完整原文/来源/哈希和歧义门禁，不借其他岗位同句作答。这里只修复章节→岗位绑定，其他原子规程限定与复杂口语关联仍须继续持出验证。
5. **全局规程筛选删范围**：多个子句分别要求某章原文和另一岗位某规程时，旧全局过滤能删掉前者。现在明确分句按声明岗位分别应用规程筛选；不存在的已请求岗位/规程组合保留可用原文，但总体partial，coverage.missing_chapters明确列出缺项。

实现位于[章节选择](../../高炉前端数据/智能助手/backend/qa_document_knowledge.py:133)、[子句规程](../../高炉前端数据/智能助手/backend/qa_document_knowledge.py:189)、[缺范围门](../../高炉前端数据/智能助手/backend/qa_document_knowledge.py:210)。公开入口第463行继续使用V33相同的一条MVCC SELECT；固定源投影、manifest、applied release与全量after核验不变。

## 实测证据及分母

- 新[范围回归](../../tests/test_qa_document_scope_resolution.py)初19项在V33实际16失败/3通过；追加的3个规程筛选反例在中间候选实际3失败，分别记录，不拼成未经同版本执行的准确率。
- 第一次修复后5例暴露测试适配器属性误用，单列为夹具错误；更正后仍由已有ask独立断言一条固定SELECT，没有删产品断言或放宽期望。
- 最终22项全部通过；合并356既有源/读取/只读/事务/入口/文档/综合回归，实际378 passed，28.86秒。使用隔离原生PostgreSQL16.13和真实public.vector，独立gpt-5.6-luna low静态复审PASS。
- 更新后的真实固定r2私有原源5587索引、11范围组、14次确定性读取再次通过，15.17秒；全部预期section短条款/表格跨页保留，生产投影常量未替换。私有report绑定当前读取器SHA，旧r1读取report保留不覆盖。
- 以上不调用助手模型，不是822原失败/partial题的生产重问；模型调用/生产请求/写入均0。原文、问题、答案及临时PG数据均不上传。

hash、命令、计数与边界见[脱敏机器证据](../../tests/qa_regression/document_scope_resolution_20260917.json)。来源范围、传输完成和答案语义仍分别核验。

## 冻结与问题状态

V34-r1私有目录.codex_runtime/qa-routing-v34/candidate-r1，10个Python文件和manifest。
manifest SHA256 72aed0fded83c53797e01197b3031c817c7cb6b817182d8cc4597a76dc7aae91。
读取器SHA256 f631e5b374542e537adb2e592493c22c75b8f332add448b33bba24de8348f0f9；V33其余9文件逐字节保留，AST解析/读取模块导入通过。V32/V33及历史报告均保留；本机冻结不等于生产已密封，远端read_set仍待刷新。

K04原production_verified仅证明此前未知标题子集，新字串书名边界在V33本机复现。台账保留旧state/evidence并重新deployed_partial_verified；这不伪装成新生产失败或改变首次题集统计。其余问题状态不变，本轮0销项。

## 仍待完成

本轮Reliable SSH身份与显式备用路线均再次超时。0生产命令/上传/停启/数据库写入/模型POST，不能据此宣称服务器故障或当前驻留健康；此前V26状态为历史快照。

恢复后先只读复核r3进度/进程、生产版本/hash、固定模型与源基线、实例/config/read_set。仍固定chiqiongblastfuenace:latest及e4ad74c41d68de1c8004419d8141a2b2df2275fa08f0dcf326ca0e63fb6d8124；不切备用底座、不接受同名权重变化。代码执行及代码示例继续禁用，普通推理与数据分析继续保留。

固定管理器/恢复任务安装和知识库迁移/发布/回滚仍有各自授权边界，既有问询未获回复，不自动重试。取得授权且身份/依赖重核通过后，复用私有entry包和受控发布闭环，再守卫更新8093。其他服务/数据库/11434 PID保持独立范围。

生产更新后串行复问822原失败/partial题，每题一次，未知发送TPL-10C8C8FAF2C694EF禁重放；按原ID/原标准统计传输、语义与成功回答率。30源/标准冲突、89oracle阻断、topic完整性及复杂岗位/规程关联继续单列。本轮822题0复问，不能报告优化后准确率。

[V33交接](2026-09-17-qa-original-source-reader.md)为前阶段快照；当前范围选择以本交接及新机器证据为准。生产完整验收未完成，继续保持修复目标。
