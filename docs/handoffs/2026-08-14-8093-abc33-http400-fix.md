# 8093 ABC33 固定首问 HTTP 400 修复交接

- 状态：`accepted_in_production`
- 最后核对：2026-08-14
- 需求：`BUG-ABC33-QA-HTTP400-20260814`
- 适用边界：`10.30.220.12:8093` 的 ABC33 规则弹窗首次智能解释。

## 实施

前端固定首问改为与后端 `ABC_RULE_INITIAL_QUESTION` 完全一致，并在非 2xx 时展示服务端
`error/error_code/message`，仍然禁止自动重发。生产只替换以下两个目标：

1. `高炉前端数据/assets/bf-abc33-assistant-dialog.js`
2. `高炉前端数据/frontend_dashboard_v3.server.html`

HTML 不是从脏工作树覆盖，而是下载当时最新生产版本并只更新缓存版本参数。第一次封装因另一个
已验收上线改变生产 HTML 基线而在停服前被拒绝；随后从 Git `6a37903` 的最新生产 HTML 重新派生，
保留 `BUG-8093-8768-CONTRACT-LOADING-20260814` 的全部内容。

## 生产证据

- 执行 ID：`abc33-http400-20260814-r2`
- 清单 SHA-256：`0109B240557737FA1427C6CF7A5B06B17C3FBEFBA5EAA182238F0EDEB69F0000`
- JS SHA-256：`CEAEA43DE919AD14AED392E457142792487706FA74FD91CF5CC43703A8D5E39C`
- HTML SHA-256：`25DF40E5FACBA9CBF9DC37FCDA293F1C42970C5CAA9894F9F0A2757A91FCAD4E`
- 8093 PID：`240 → 15392`
- 备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\abc33_http400_20260814_181228`
- 真实问答请求：恰好 `1` 次；SSE 为
  `preparing → prepared → delta → final → done`；答案 `679` 字。
- 8094/8768/8770/5432/11434 PID 在部署与真实验收前后均未变化。
- 远端 Git：提交 `d24543a`，标签 `prod-8093-abc33-http400-20260814-r2`。

## 220.12 连接配置

本机 SSH 明文已从 SSH 配置注释移除，仓库和 Fleet JSON 只保存 LocalAppData 受保护文件引用，
不保存密码值。持久 Paramiko 会话复用同一认证 transport，保活 30 秒；新增的 220.12 Reliable
SSH MCP 已完成主机身份校验，连接池大小 `1`，SSH 保活 `30s`，只读心跳 `60s`，自动重放关闭。
本次下载、预暂存、部署、验收和 Git 记录均复用已认证连接。

第二次幂等验证发现旧迁移脚本会把脱敏注释误写成密码；脚本已改为拒绝脱敏占位符。用户随后授权
精确 Windows 凭据目标和 Reliable SSH 受保护源：Windows 条目确认目标但不导出秘密，恢复器因此
从 ACL 已验证的固定 220.12 Reliable SSH 密码文件恢复 LocalAppData 文件，全程未输出密码、长度或
哈希。两个独立 `pwsh -File` 配置进程均返回 `ok=true/migrated=false`；停止旧 broker 后全新
Paramiko 认证成功，双 `.ps1` 请求 `1→2`、重连 `0`，耗时 `1643/1589ms`。Reliable SSH MCP
再次确认 `WIN-54B94HVHKBA / 10.30.220.12 / administrator`，连接池重连 `0`、自动重放关闭。

## 当前生产复核（2026-08-14）

- Reliable SSH 再次确认主机身份，连接池重连 `0`、自动重放关闭。
- 当前生产 HEAD 为 `d153ed5cd5e4865f91aab669c6c8dc23c1bc5a38`；修复提交
  `d24543a2dcc18111a5c9f827726a845d39445e71` 是其祖先，标签仍精确指向该修复提交。
- 两个修复目标的远端 Git 状态为空；实际 HTTP HTML 仍引用
  `abc33-initial-question-20260814-r1`，实际 JS SHA-256 仍为
  `CEAEA43DE919AD14AED392E457142792487706FA74FD91CF5CC43703A8D5E39C`。
- 再次发送恰好一次真实固定首问：evaluation `1984`，SSE 为
  `preparing → prepared → delta → final → done`，答案 `720` 字，没有 HTTP 400。
- 8093 PID 在复核前后均为 `15392`；8094/8768/8770/5432/11434 PID 均未变化。未上传、未重启、未写生产文件。
