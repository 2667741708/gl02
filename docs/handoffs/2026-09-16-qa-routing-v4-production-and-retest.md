# QA V4 生产更新与代表题复测

- 状态：已部署；复测完成，残留问题继续修复。
- 最后核对：2026-09-16。
- 需求：REQ-QA-FULL-ISSUE-INVENTORY-20260916。
- 权威来源：受控部署结果、生产版本引用和脱敏审阅。
- 边界：220.12:8093 六个明确目标；其他服务、模型配置及数据库结构未改。

## 部署验收

执行 `qa-routing-v4-20260916-r1`，8093 PID `9768 → 15520`。
守卫恢复、页面200、API及安装哈希验收通过，未触发回滚。
8094/8768/8770/5432/11434 PID 前后不变，8892前后无监听。
Python3.11生产编译通过。Git可记录性519行语义变化，无换行迁移。
生产提交 `f79b13ccf79d3b522c5a7a253989c9915df74aa4`，CAS一次成功。
不可变引用 `refs/prod-8093/20260916/qa-routing-v4-20260916-r1`。
无关索引及工作树改动保留。

受控执行耗时34.603秒，其中远端命令34.227秒；版本记录2.956秒。
两次均复用同一认证连接。这些耗时不包含此前构建、测试及全部暂存，不能作为完整部署提速比较。

## 复测与残留

六题各发送一次，全部明确SSE done；三题通过、一题部分通过、两题失败。
两次就绪阻断均发生在下一题claim创建之前，只续测已证明未发送的题。
就绪GET可有限重查；问答POST自动重试始终为0。

详细脱敏判定：
[routing_v4_production_review_20260916.json](../../tests/qa_regression/routing_v4_production_review_20260916.json)。

1. R03：CO/CO2/H2及南北探尺通过；A-D观测问句仍未进入工具路由。
2. R04：30日基线通过；双窗口实际完成查询但结果层级适配错误。
3. R05：最新日报已读正文；行内摘要识别遗漏。
4. O01：就绪状态仍间歇失败，不能把HTTP200视为模型就绪。
5. E01：预取MCP动态导入缺少同目录依赖，成功工具结果仍需独立保全。

## 后续修复

V5针对上述三项真实结构缺陷补回归，并修复历史检索结果投影及owner限定。
代码执行和代码示例生成继续禁用。未验证事项继续保留开放状态。

## 可复现入口

`python tools/run_qa_failed_retest_once.py --help`：显式execute、新输出目录、不可覆盖claim。
`pwsh -NoLogo -NoProfile -File tools/verify_qa_routing_release.ps1`：发布脚本语法及受控范围验证。
`python tools/build_qa_remediation_execution_ledger.py`：重建33项执行台账。
