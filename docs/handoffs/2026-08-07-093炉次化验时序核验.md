# 2026-08-07 093 炉次化验时序核验

## 数据事实

在 220.12 PostgreSQL 中核对 `2#20260807-093`：

- 开口：`03:43:00`
- 出铁结束：`06:10:00`
- `tappingTime=147.00`，与开口到结束的 147 分钟一致
- 已对齐 3 条化验结果，批次号分别为 `22608-093-001`、`22608-093-002`、`22608-093-003`
- 结果时间分别为 `05:27:00`、`05:46:26`、`06:30:02`
- 结果之间间隔约 19 分 26 秒、43 分 36 秒

## 判断边界

第一、第二条结果在出铁结束前已经形成，第三条结果在出铁结束后约 20 分钟形成。因此 093 不是“等整炉出铁结束后才一次性化验”，而是同一炉次的多个样本/批次结果陆续发布，最后一条可能在结束后补齐。

当前对齐记录的 `sample_ts`、`tank_no` 为空，只有 `result_ts`、`batchno` 和 `sample_no`。所以可以确认“同炉次内分批出结果”，但仅凭现有镜像不能严格证明每一条是否一一对应某一罐铁水；要确认每罐关系，需要 IMES 原始取样表或铁罐号字段。

## 页面解释

生产实绩页面按炉次主记录展示，093 的开口时间仍是 03:43；这不代表化验结果停在 03:43。093 的质量结果已经在 05:27、05:46、06:30 分批进入，后续 094 是否显示取决于 IMES 是否先写入其开口/结束字段。
