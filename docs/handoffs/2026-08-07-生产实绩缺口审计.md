# 2026-08-07 生产实绩 04:00-05:00 缺口审计

## 结论

07:09 左右页面仍显示 `2#20260807-093`（开口 03:43），主要原因不是 8093 传输通道中断，而是 IMES 输出表中后续炉次 `094`、`095` 当时只有炉次编号，`openTime`、`closeTime`、`tappingTime` 仍为空，不能被判定为已开始/已完成的生产实绩。页面按已形成的炉次记录展示，因此不会凭空生成 04:00-05:00 的已完成炉次。

## 220.12 只读核查证据

核查时间约为 2026-08-07 07:14（Asia/Shanghai）：

- `bf_imes.imes_bf2_output_list_cond_data` 中 `2#20260807-093`：开口 `03:43`，出铁结束 `06:10`，`updateTimes=06:16:08`。
- 同表 `2#20260807-094`、`2#20260807-095`：`openTime/closeTime/tappingTime` 为空；`094` 的 `updateTimes=06:51:50`，说明编号已出现但业务记录尚未完成。
- `bf_assistant.heat_performance_quality_summary` 中 `093`：`first_sample_ts=05:27:00`，`last_sample_ts=06:30:02`，`source_updated_at=06:30:02`，`aggregated_at=07:13:26`，样本数 3，当前为 partial 状态。
- `bf_imes.fetch_runs` 的 `bf2_output_list_cond_data` 在 06:29、06:34、06:39、06:44、06:49、06:55、07:00、07:05、07:09、07:14 均有成功运行，典型间隔约 5 分钟，未见 4 小时级断采。

## 延迟分段

以 `093` 为例：

| 环节 | 时间 | 与上一环节的间隔 |
|---|---:|---:|
| 开口 | 03:43 | — |
| 出铁结束 | 06:10 | 2 小时 27 分（工艺过程，不是传输延迟） |
| 首个样本 | 05:27 | 样本采集早于出铁结束，属于取样流程 |
| 最近样本/源更新时间 | 06:30:02 | 出铁结束后约 20 分钟 |
| 页面后端聚合观测 | 07:13:26 | 最近样本后约 43 分钟；聚合字段会在同步时刷新 |

因此，截图中 07:09 对比开口 03:43 的约 3 小时 26 分，主要是当前炉次从开口到页面时刻的炉次年龄，不等于数据库传输延迟。IMES 镜像约 5 分钟同步，8093 页面已改为约 5 秒自动刷新；但源表尚未填写 `openTime` 的炉次不能提前显示为已完成实绩。

## 复验口径

浏览器建议使用带缓存参数的新地址或强制刷新：

`http://10.30.220.12:8093/frontend_dashboard_v3.server.html?cache=heat-realtime-20260807`

若现场确认 `094/095` 已实际开口而 IMES 仍为空，应继续追查 IMES `listCondData.do` 的业务写入/接口口径；此时问题不在 8093 前端轮询。
