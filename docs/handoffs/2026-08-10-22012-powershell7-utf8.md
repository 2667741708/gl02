# 2026-08-10 220.12 PowerShell 7 UTF-8运行时交接

## 结论

220.12已安装PowerShell 7.6.4 Core，本机远程跳板默认改为短`pwsh.exe -File`执行。中文输出、中文文件回环、带`[CmdletBinding()] param()`的独立payload均通过。安装过程没有重启或修改生产服务/计划任务。

## 安装前

- Windows Server 2016 Standard，64位。
- Windows PowerShell 5.1.14393.206。
- PowerShell 7不存在；WinGet不存在。
- 8093/8094/8768/8770/5432监听PID为`4044/4156/8524/3732/12372`。
- 多个生产任务显式调用`powershell.exe`，不能全量替换。

## 安装与安全门

- 本机从WinGet官方源下载PowerShell 7.6.4 x64 MSI。
- SHA-256：`D11942DF52FD12470169797ABFA4781D9480EFDC81000BA4FA55A5B921ED8DD0`。
- Authenticode：Microsoft Corporation，`Valid`。
- `msiexec`退出码0；不需要重启。
- 安装后程序：`C:\Program Files\PowerShell\7\pwsh.exe`。
- 安装完成后远端MSI与临时验证脚本已从Administrator Temp精确删除。

## 验收

- `PSEdition=Core`，`PSVersion=7.6.4`。
- Console Input/Output及`$OutputEncoding`均为UTF-8。
- 中文回环：`冀南钢铁 PowerShell 7 UTF-8：硅含量预测`。
- 关键脚本语法通过：V20严格整点、V20可调分钟、8094 runner、NSSM服务管理、健康检查、炉次质量同步。
- 真实跳板冒烟输出：`冀南钢铁：远端中文执行正常`。
- 五个受保护端口安装前后PID完全一致，无服务短窗。
- 本机合同测试：`7 passed`。

## 远程执行合同

`tools/remote_22012_exec.py`默认：

1. SFTP暂存UTF-8 wrapper；
2. `--script`时另存独立payload，保留参数块首行；
3. 通过短命令调用`C:\Program Files\PowerShell\7\pwsh.exe -File`；
4. 完成后删除随机临时脚本；
5. 不再默认使用5.1或`EncodedCommand`。

## 未迁移边界

服务器现有计划任务Action仍保持原样。只有完成单任务真实运行、备份XML、切换Action、最近结果0、端口保护和回滚验证后，才能逐项切换到PowerShell 7。此次安装结果不能等价于“所有历史脚本已经完成运行级兼容验证”。
