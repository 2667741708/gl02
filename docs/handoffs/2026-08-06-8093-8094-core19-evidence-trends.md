# 8093/8094 炉况评分弹窗19项核心证据与趋势交接

追踪编号：`REQ-8093-8094-DIAGNOSIS-CORE-19-TRENDS-20260806`

## 完成内容

- 保留所选炉况的规则主证据并优先展示。
- 新增固定19项核心变量折叠区，展开后可看当前值、5分钟变化、30天基线、数据时间与来源。
- 点击任一变量打开依赖无关的SVG最近60分钟曲线；缺失值不补零。
- `T_top`直接值缺失时由四点顶温有效值均值派生并标记来源。
- 新增只读轻量接口 `GET /api/diagnosis-core-evidence`；19项证据不再等待模型、建议或知识库。详细智能分析仍只按用户当前选择的一个炉况异步生成。
- 查看证据和曲线不写库；人工评分与可选建议继续写既有不可覆盖事件表。

固定ID顺序为：`P_top_gas_A`、`P_top_gas_B`、`P_top_gas_C`、`P_top_gas_D`、`T_top_A`、`T_top_B`、`T_top_C`、`T_top_D`、`P_top`、`T_top`、`Q_blast`、`P_blast_cold`、`P_blast`、`T_blast`、`PI`、`DP_total`、`DP_upper`、`DP_lower`、`GasUtil`。

## 验证

- Python/前端合同：`44 passed`。
- 本地浏览器：Chromium 9视口、Firefox 4视口、WebKit 4视口，共 `17/17`。
- 8093生产：2026-08-06 11:20完成守卫闭环；HTTP 200、核心ID 19/19、曲线 19/19、8768 PID不变、未回滚。备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\logs\deploy_backups\diagnosis_ai_analysis_20260806_112034`。
- 8093远端主payload与本机生成文件SHA-256一致：`CF2F09A8149C0FF34792444CA1D01D7E332EE2CEBCBC7C6EB66CB2DBE0507919`；前端模块为 `507ABDEBC9647F34E420EF616BB4BFFC2AADB406DDE6CB607E1BF75AC0473524`。

## 异常与恢复

首次r7部署仍等待完整模型分析，后续资源请求遇到短暂不可连接，守卫脚本按设计自动回滚；复核8093、8768、8094、8770均恢复。修正后以轻量接口19/19作为硬验收，完整模型分析作为运行时异步能力，第二次部署成功。

8094继续部署时本机到220.12的SSH先后出现10060、10053，随后端口22探测超时；远端脚本未启动，因此8094当时仍保持原运行版本。VPN恢复后执行 `remote_guarded_enable_8094_diagnosis_ai.ps1`，再用下列命令复核两页：

```powershell
python tools\verify_diagnosis_core19_remote.py --lightweight `
  --base-url http://10.30.220.12:8093 `
  --base-url http://10.30.220.12:8094
```
