# QA V7生产验收与V8知识原文修复

- 状态：V7已部署、复测；V8本机开发中。
- 最后核对：2026-09-16。
- 需求：`REQ-QA-FULL-ISSUE-INVENTORY-20260916`。
- 权威来源：受控部署结果、生产Git版本、真实单次SSE结果和[脱敏语义评审](../../tests/qa_regression/routing_v7_production_review_20260916.json)。
- 边界：仅8093；代码生成/执行继续关闭；833知识题单列，不以非空计通过。

## V7部署

执行`qa-routing-v7-20260916-r1`仅替换`qa_history_projection.py`。
生产提交`f46841edd06502c53fa85df88255d381ee850dfb`，版本引用`refs/prod-8093/20260916/qa-routing-v7-20260916-r1`。
8093 PID `12208 → 11544`；守卫暂停后在finally恢复，HTTP200、目标SHA256及受保护PID检查通过，未回滚。
目标SHA256：`56f82b925c6877909eddd3f91847cbdf1de3784de60d025c366ab154e7942a1a`。
8094/8768/8770/5432/11434在本次切换前后PID均相同；Ollama PID在V6与V7之间发生外部变化，未归因于本次部署。
常驻连接复用；受控激活33364.584ms，版本记录2614.662ms，CAS1次，无关索引保留。

V7修复两个`NOT LIKE`模式的绑定参数。业务SQL继续使用`?`，由真实`PgCompatConnection`转换为psycopg参数；测试覆盖实际转换函数、psycopg解析器与SQLite排除行为，107项聚焦回归通过，独立复审PASS。

## 五题真实复测

5次POST、5次SSE完成、0次自动重放。逐答案评审4通过/1部分，无失败。
三条历史题解决SQL失败及生成摘要递归；旧答案明确为未重新核实的历史摘录。
压力题继续输出可追踪的单点事实，缺单位明确提示。
无工具原因题不再截断，但其压力控制系统因果表述仍需限定，保留为partial。
这一评审不代表全部知识题或全部工艺知识正确率。

## V8新增发现与开发

1. 真实只读知识目录含28份文档、5521片段；三规二制原文202884字，独立`rag_hierarchical_chunk`表不存在。
2. 已有`rag_chunk`包含`three_rules_atomic/topic/section`及章节、规程、分片编号；复用这些数据，不新增DDL或重新导入生产数据。
3. 当前RAG每文档最多保留两个片段，不能保证长章节和表格完整。
4. 原无实时/代码分支提前返回，丢弃已检索知识证据；V8以允许来源构造Prompt，保留知识并去除新主题的旧现场断言。
5. 原文执行器只接受核验的书名、来源类型与哈希；缺页、错岗位、未知书名、异常表格和版本漂移分别降级，不补写制度。
6. 长章节按整片分页，页数及尚未展示范围显式返回；分页partial不计全文通过。
7. `bf_foreman_ops_v1`未出现在本次真实知识目录，诊断专用知识依赖需另核对，不能用基础机理文档冒充已导入64主题。

验证入口：`tools/audit_qa_knowledge_manifest_readonly.py --root <生产根> --candidate-source <候选模块>`只读核验来源/原文，不调用模型或写数据库；本次三项候选检查成功：工长安全原文、28章目录、事故原文。最终部署仍需密封、独立复审及真实问答复测。
