# ABC33入口、详情和缺数分数热修复回执（2026-08-08）

## 结果

- 8094顶部“炉况建议切换”区域增加显眼的“查看A/B/C 33项炉况”入口，不再挤压底部导航。
- A/B/C总览采用双栏规则卡；窄屏自动改单栏。
- “查看复核详情”改为真正的详情抽屉，显示主复核参数、展开参数、缺失数据、人工复核、处置顺序、观察窗口、审批要求、工艺依据和原文章节，不再调用浏览器 `alert`。
- `needs_data` 不再显示100分，统一显示“不可计算”；公开合同为 `score=null`、`score_available=false`。

## 生产验证

- 8094主页HTTP 200，并引用 `assets/abc-furnace-rules-production.js?v=abc33-20260808-r2`。
- 新资源HTTP 200、13748字节，包含新入口、详情抽屉和缺数分数门禁，不包含旧 `alert(JSON.stringify(...))`。
- 最新ABC批次ID 26，时间 `2026-08-08 10:40:00+08:00`，33项完整。
- A1公开结果：`score=null`、`status=needs_data`、`score_available=false`。
- A1详情接口HTTP 200，包含5项主复核参数和3步处置顺序。

## 部署与边界

- 远端备份：`F:\\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\\backups\\abc33_8094\\20260808_103955`。
- 8768重启的SSH输出读取超时，但端口8768可达，随后已生成新格式批次26，证明新引擎已加载。
- 8094服务重启尝试被远端SSH通道关闭；静态资源与API均已实际生效。本次没有修改或重启8093、8770、11434。
- 内置浏览器对该超大单页的自动化DOM读取持续超时；本次以HTTP资源、API合同和本机14项自动测试完成验收，尚未宣称完成全浏览器/全视口矩阵。
