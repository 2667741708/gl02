# QA V6生产验收与V7历史SQL修复

- 状态：V6生产已验收；V7单文件候选，尚未部署。最后核对：2026-09-16。
- 需求：REQ-QA-FULL-ISSUE-INVENTORY-20260916。
- 权威结果：[V6语义审查](../../tests/qa_regression/routing_v6_production_review_20260916.json)。

## 生产与并发更新保留

V6首次停服前被HEAD漂移阻断。只读证明热趋势提交`e2beafeaa676371984a090d11b5edf8f65f5a487`仅修改热趋势JS/Python两个文件，与QA五写集和十二依赖不重叠。刷新预检、证据与密封后才执行，不覆盖并发热趋势更新。

受控执行`qa-routing-v6-20260916-r1`成功，8093 PID188→12208，守卫finally恢复、HTTP200、五哈希通过，无回滚。保护8094/8768/8770/5432/11434 PID仍16576/6440/844/12372/6732。激活33959.307ms，版本记录2873.37ms；CAS一次成功，生产提交`dd34e235f75d9f474923c4094c47e035e8328229`，版本ref`refs/prod-8093/20260916/qa-routing-v6-20260916-r1`。

八题各一次POST，七题done，另一次确定上游连接错误。语义3通过/1部分/3失败/1依赖阻断；单点事实、窗口比较、报表摘要通过。四温度点存在单位空缺、C点全零和不足每分钟样本，代码已明确缺项而未编造结论。修复数据目录/采集有效性属于后续独立数据事项，未修改生产库或传感器配置。

## V7必要修复

历史SQL新排除模式使用字面`%`，本机SQLite测试无法识别psycopg参数解析差异。真实生产三题关闭范围，未泄漏其他owner内容，但不计通过。将两个排除模式改为绑定参数，顺序`owner,id,prefix1,prefix2,keyword,assistant_flag`；不删除历史记录、不改变owner截止门或通配符转义。

新增回归使用真实`assistant_pg._qmark_to_psycopg`实现与真实psycopg占位符解析器，覆盖SQLite误通过的驱动错误。聚焦107项回归通过。V7构建器绑定V6历史模块准确哈希，仅部署`qa_history_projection.py`一个目标，所有相关依赖保留。

## 未验证

上游Ollama连接关闭/驻留变化尚无跨进程模型租约修复，8094或Ollama配置未获得本轮修改授权，保持原配置。无工具一次压缩补答本机合同通过，生产仍需成功生成才可核对。长期33项台账继续进行，828知识语义待审不算通过。
