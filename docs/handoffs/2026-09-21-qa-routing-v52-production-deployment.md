# V52 路由候选 8093 生产部署交接

- 状态：已完成生产部署、生产 Git 记录和部署后只读健康复核。
- 最后核对：2026-09-21。
- 需求：`REQ-QA-RETEST-ANSWER-ROUTING-20260921`。
- 发布标识：`qa-routing-v52-20260921-r1`。
- 适用范围：`10.30.220.12:8093` 的智能助手路由模块；不包含 8094、8768、8770、数据库、Ollama 服务或模型权重变更。

## 1. 冻结候选

- 候选包含 16 个模块；发布 manifest SHA-256 为 `803267bfaee348dd9a685708f33a94eb78f5d36d79a177add7c5c9d327ea5fbb`。
- 预检核对 61 个只读依赖，全部与远端基线一致。
- 16 个目标文件通过编译、哈希、UTF-8 无 BOM、LF 和临时索引记录性检查。
- 固定模型身份为 `chiqiongblastfuenace:latest`，digest 为 `e4ad74c41d68de1c8004419d8141a2b2df2275fa08f0dcf326ca0e63fb6d8124`，模型族 `qwen35`，参数规模 `26.9B`。

## 2. 受控部署结果

|项目|结果|
|---|---|
|部署结果|成功；`rollback_applied=false`|
|8093 守卫|已暂停并恢复；仅操作 `BFV4PreviewProxy8093`|
|8093 监听 PID|`6636 → 12248`|
|HTTP / 模型就绪|首次检查即通过，`proxy_ok=true`、`ollama_ok=true`、`model_ok=true`|
|安装哈希|16/16 与冻结候选一致|
|备份目录|`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\qa-routing-v52-20260921-r1-20260921-220008`|
|原问题重发|0|
|模型操作|0；未预热、卸载、切换、回退或复制权重|

受保护监听在部署前后保持不变：8094=`16576`、8768=`6796`、8770=`844`、5432=`12372`、11434=`12656`；8892 前后均无监听。

## 3. 生产版本记录

- 生产分支：`production-8093`。
- 父提交：`4ba11ff2bc19607cbe040b5f57474c0aa2afdc62`。
- V52 生产提交：`19892017d93028fbf1cd6dcd0ed8799fca778fa0`。
- 任务引用：`refs/heads/codex/8093/qa-routing-v52-20260921-r1`。
- 不可变引用：`refs/prod-8093/20260921/qa-routing-v52-20260921-r1`。
- 16 个目标路径在记录后均为 clean；远端原有无关工作树和索引内容得到保留。

## 4. 部署后只读复核

1. `/api/ollama/status` 返回 `proxy_ok=true`、`ollama_ok=true`、`model_ok=true`。
2. Ollama `/api/tags` 仍提供锁定名称和 digest；`/api/ps` 仅有该模型驻留，数量为 1。
3. 8093 静态页面返回 HTTP 200，响应体 233358 字节。
4. 匿名 `/api/qa/bootstrap` 返回 HTTP 200、`ok=true`、`access_mode=guest_shared`。
5. 错误日志尾部中的访客 bootstrap `NameError` 位于部署重启前；部署后重新请求成功，新增日志区间为空，因此当前未复现。

## 5. 验证和边界

- V52 QA 选择回归：2116 通过、0 失败、1003 条按筛选条件未执行。
- MCP gold schema：14/14 通过。
- 部署路径和发布就绪测试：36 通过。
- 本次部署确认的是候选完整性、服务可用性、模型身份和受保护服务隔离。它不证明 822 条旧答案在 V52 下的语义准确率。
- 原失败题应在独立复测阶段按登记顺序单次发送；发生传输不确定时不得自动重放，并按 V26/V52 配对口径报告。
