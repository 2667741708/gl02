# 已确认对象的多轮追问执行

状态：V49-r6本机冻结候选，完整回归、原生只读合成合同和有界独立审查通过；未部署、未完成原题语义复测。
最后核对：2026-09-17。需求：REQ-QA-OWNED-FOLLOWUP-EXECUTION-20260917，关联QAOPT-R01/R07/R08/R10/O01/T05。
权威：冻结V48/V49、实际准备/JSON/SSE/异步工具循环、Reliable SSH原生Python3.11.9 RAM探针及gpt-5.6-luna有界只读审查。范围：8093对象追问和来源授权；不授权模型、恢复任务、数据库或其他服务修改。

## 实际缺陷和修复

此前已保存owner对象和时间窗，但准备及异步时间窗预检仍以当前短问题提取变量。“这个最近30分钟的趋势如何？”可能不执行已确认对象的查询；“继续/画出来”可能丢失服务端上下文。缺少有效对象时还可能落入普通模型答复。“它现在是多少？”的原任务计划甚至可能预取未确认对象；纯代码示例请求也可能携带现场查询权限。

[resolve_owned_followup:L582](../../高炉前端数据/智能助手/backend/qa_task_plan.py#L582)仅处理当前实际指令的明确数据追问。对象必须来自服务端owner会话加载，经现有TTL、来源消息归属、对象目录、来源映射及继承标记校验；用户拼接的私有状态文本不能建立继承授权。有效时形成精确对象的`live_readonly_data`任务，当前30分钟替换旧120分钟，“现在”清除旧窗口，继续/画图可保留适用窗口。所有查询重新取数，不使用旧值证明当前事实。

无对象、过期、跨会话或未知对象时返回确定性澄清，并明确关闭prefetch/MCP/知识检索与来源域。JSON/SSE同请求完成澄清，不调用模型或数据工具。澄清完成不是原数据问题回答正确。仅用户数据、原文、聊天历史、报表、禁工具、规则解释和主题重置不获得隐式现场授权。纯代码请求关闭预取，且不将其中的对象和窗口写入可继承查询上下文；独立允许的查询+代码复合任务仍按已有禁代码子任务策略处理。

[候选构建器:L19](../../tools/build_qa_owned_followup_candidate.py#L19)将同一TaskPlan贯穿准备、隐藏提示词、同步/异步MCP；时间窗预检使用已校验对象，JSON/SSE出口在普通模型回合前处理确定性澄清。原始用户问题保持；没有增加客户端传入的继承权限字段。引用屏蔽和现有owner SQL保持。

## 冻结与验证

V49-r6 manifest SHA256 `73da30da0e2aebcfec7fafd8453598af11c4337a5ef538f29d3ae79854d70a42`。16文件中14个逐字节继承V48；planner只新增1纯函数，移除后全AST一致；proxy只改7个函数，归一化后剩余全AST一致。V47继承的3个ABC共享函数、导入和2个生产依赖pin保持；固定模型模块字节不变，无数据库迁移、模型配置或新API字段。

[机器证据](../../tests/qa_regression/owned_followup_execution_20260917.json)保存34模块**905通过、126.89秒**的完整复现命令、14金标schema和独立审查PASS。实际服务器Python3.11.9 RAM加载16候选文件及60生产依赖，10问答、9共享ABC、6普通准备、15对象追问共**40合成合同通过**。有效对象的5场景各执行1次模拟prefetch；6缺失/过期/跨会话场景及给定数据/纯代码场景均0预取；JSON/SSE澄清0模型请求。模型、数据库、权限会话、MCP预取和线程等外部边界明确模拟；实际取数、最终模型答案、真实并发未验证。

同探针运行冻结V48作为修前观察：新状态字段缺失导致新合同不满足，不将其统计为15个原题错误。实际计数分别证明4个追问没有预取、三个缺失latest场景错误预取、纯代码错误预取、两个澄清出口落入模拟模型。旧25合同仍通过。原生过程0实际副作用尝试、生产写、真实模型调用及原题POST。

初轮34模块899通过/3失败来自AST隔离夹具未提供模块原有re依赖，补齐夹具后完整通过；额外6个缺失对象反例在权限收敛前失败，修复后通过。保留全部私有失败记录，不将其写成生产问题或成功率。

```powershell
python -B -X utf8 tools/build_qa_owned_followup_candidate.py --revision rN
python -B -X utf8 -m pytest -q tests/test_qa_owned_followup_execution.py tests/test_qa_sensor_context_source_gate.py tests/test_qa_ordinary_context_source_gate.py tests/test_qa_full_candidate_probe_evidence.py --basetemp=.codex_runtime/qa-owned-followup-reproduce-new
python -B -X utf8 tools/evaluate_mcp_gold_tasks.py --validate
```

rN与basetemp必须使用未存在的新编号/目录；不得覆盖既有候选。完整34模块命令以机器证据为准。-O不能绕过候选构建断言。真实线上测试继续保持一题一次POST、未知发送只读恢复、不自动重放。

## 固定底座与生产门

唯一允许名称`chiqiongblastfuenace:latest`，digest `e4ad74c41d68de1c8004419d8141a2b2df2275fa08f0dcf326ca0e63fb6d8124`。禁止切换、备用回退、同名更换权重或另立基线；身份不符停止新的模型调用与发送。

两个独立UTC快照必须分开：12:58:09 tags→ps→tags匹配e4；12:58:40两次tags与全部ps记录均为同名`9111be230d48e53a385a28930fb8cf6972767c83e93c0db51f6b331a534e30fb`。瞬时匹配不能证明永久锁定，也不据此确定变更执行者。主任务0加载/卸载/cp/换模/任务变更。禁切换管理器仍未安装，原独立授权请求维持；不重复申请，不自行扩张8093权限。详情见[固定底座策略](2026-09-17-qa-single-base-model-policy.md)。

生产HEAD仍cdd390…，代理及2共享依赖哈希保持既有值；r3为953/953完成，初始PID存活只读检查为0。没有完整受保护PID前后配对，不宣称所有服务PID不变。全部33项状态、首次1233有效题及822失败/部分范围不变，0销项。

真实发布还需固定身份持续符合、禁切换管理器/任务及知识DB各自授权与验收、完整依赖/共享功能/EOL/recordability/密封清单和8093守卫流程。满足门后才单次复问822原失败/部分题，独立审核完整最终答案并报告实际成功率和正确率。
