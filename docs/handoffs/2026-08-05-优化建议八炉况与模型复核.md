# 优化建议八炉况与模型复核交接

追踪编号：`REQ-OPT-MULTI-CONDITION-LLM-REVIEW-20260805`

## 已完成

- v5 建议适配器一次输出当前有效方案与八炉况独立只读预案。
- 8767 诊断快照新增 `recommendation_bundle`，旧 `recommendation` 保持兼容。
- 优化建议页新增八炉况矩阵、完整动作审计、所选炉况驱动的原驾驶舱和后台模型复核面板。
- 模型接口完成白名单、严格 JSON 校验、TTL 缓存和失败隔离；没有数据库写入或生产控制能力。
- 本地 Python/静态/Babel 回归和 Chromium 九视口完成。

## 尚未完成

- 未部署或重启 220.12 的 8093/8768；远端必须另走受守卫发布流程。
- 未在 Firefox、WebKit 和现场主用 Edge 完成验收。
- 真实 8767 数据源仍需确认 `recommendation_bundle.conditions=8`，真实模型需验证结构化返回、超时和缓存命中。
- 布料、负荷、最佳风量、炉次化验和人工事件的真实字段缺失时仍应保持 `needs_data/manual_confirm`，不得以模型补数。

## 发布前门禁

1. 影子回放确认 `active_plan` 与当前 v5 单方案逐字一致。
2. 真实模型失败、超时、停止时规则建议仍完整可用。
3. Firefox/WebKit/Edge 按项目固定视口矩阵通过，无横向溢出、裁切或控制台业务错误。
4. 现场工艺人员复核四类调剂顺序、幅度、门禁和审批角色。
5. 生产部署必须验证 8093/8768 健康、备份、回滚与受保护服务不变。
