# 8093 炉次实绩实时刷新交接记录

追踪编号：`OPS-8093-HEAT-REALTIME-20260807`

## 结论

- 220.12 的 `HeatPerformanceQualitySync` 计划任务原来启用但最近结果为 `1`，根因是 Windows PowerShell 5.1 读取含中文路径且无 UTF-8 BOM 的 runner 失败。
- 已备份原 runner 到 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\standalone_heat_dashboard_8891\logs\deploy_backups\heat_sync_runner_pre_encoding_20260806_234515`，并将生产 runner 改为带 UTF-8 BOM 的版本。
- 已手动触发一次 `\BlastFurnaceServices\HeatPerformanceQualitySync`；日志显示增量同步 `synced=44`、`ok=true`，最新炉次为 `2#20260806-088`。
- 8093 专用前端资源已备份到 `F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\logs\deploy_backups\8093_heat_realtime_20260807_000121`，并通过“停止 8093 → 复制 → 启动 8093”闭环上线。
- 8093 资源现包含 `REFRESH_MS = 5000`、`setTimeout(load, REFRESH_MS)` 以及 220.12 数据源/源时间展示；服务已恢复运行，8093、8768、8094 监听未改变。

## 当前数据口径

`HeatPerformanceQualitySync` 仍按 5 分钟运行一次，前端每 5 秒轮询 8093 的只读 `/api/heat-performance-quality`。5 秒是页面刷新周期，不会伪造数据库频率；数据库摘要由同步任务按 5 分钟吸收 IMES 迟到数据。

## 英文路径尝试

已建立 `C:\ProgramData\BFV4\HeatSync\root` ASCII junction 和 `C:\ProgramData\BFV4\HeatSync\run.ps1` 兼容入口，但 Windows 计划任务的 `/TR` 修改返回“参数错误”，因此没有切换现有任务定义。当前继续使用已经带 BOM、验证成功的原 runner，避免改变任务触发器和权限。

## 本机源代码状态

本机仓库的既有文件尚未同步本次远端资源改动：Codex Windows `apply_patch` 沙箱持续报 `apply deny-read ACLs`，无法安全更新既有 HTML/JS/Python 文件。远端生产部署不应被视为本机源码发布；下次环境恢复后应把 8093 轮询逻辑、8092 回退接口和测试补回本机源代码，并更新 `AGENTS.md`、`docs/program_index.md`、`docs/test_reference.md`。

## 验收命令

```powershell
schtasks.exe /Query /TN "\BlastFurnaceServices\HeatPerformanceQualitySync" /FO LIST /V
```

预期：`Scheduled Task State=Enabled`、`Last Result=0`、`Repeat: Every=5 Minute(s)`。

远端资源验收：检查 8093 资源包含 `REFRESH_MS = 5000` 和 `setTimeout(load, REFRESH_MS)`，并确认 8093 服务为 Running、8093/8768/8094 端口监听。
