# ABC33数据接线核查与影子安全门回执（2026-08-08）

## 结论

220.12传感器和旧8类诊断数据正常；ABC33全0来自8768适配错误，不是数据库无数据。数据接线与稀疏分钟取值已修复，但逐条对照原始运营核验文档后确认当前33项公式目录仍未完全一致，因此已恢复影子安全门，禁止未标定分数和假报警进入生产页面。

## 真实批次变化

- 批次34：覆盖率0、数据年龄空、33项置信度0。
- 批次44：质量字段修复后20项置信度大于0，最高60%。
- 批次46：各变量最近有效值修复后31项置信度大于0，B5/C9达到75%；同时暴露物理原值误用通用阈值的假高分风险。
- 批次56：影子安全门生效，33项完整、31项有非零置信度，`needs_data=31`、`blocked=2`、公开分数0、告警0。

## 生产变更

- 8768：合并实时当前值和旧诊断派生特征；质量字段从诊断JSON读取；每个变量只允许在5分钟内向前取自己的最新有效样本。
- 8094：页面加载竞态修复为数据/宿主任一先到都必定渲染，并始终显示加载/错误状态；版本 `abc33-20260808-r5-shadow-gate`。
- API：同一诊断时间存在多个配置版本时按 `evaluation_ts DESC, id DESC` 选择最新批次。
- 发布门：`release_control.mode=shadow_validation`、`scores_visible=false`、`alerts_enabled=false`。

## 运行验证

- 8768 WebSocket初始化成功，最新历史481点、旧8类25个历史诊断。
- 8094 HTTP 200，页面和ABC资源均包含R5/R4标记。
- 最终受控重启中8093、8094、8770、11434在每个重启闭环内保持PID不变；只替换8768进程。
- Chrome扩展在自动验收时报告浏览器连接不可用，因此本轮没有新增远端Chrome DOM截图；HTTP、WebSocket、API和用户回传页面作为运行证据，不能冒充完整跨浏览器矩阵。

## 下一发布门

逐条把原文A9/B13/C11公式改为标准化风险因子：`z60_X`、`z15std_X`、`slope30_X`、Top/Static范围、DPHigh、PIBad、AirAcceptBad、BurdenStall/Slip、CoolingRisk、HeatProxy和DrainProxy；完成30天回放、异常样本校准和24小时影子运行后，才允许把配置切到 `published`。

