# 8093 Billboard 标签醒目度调整（2026-08-02）

## 目标

- 需求编号：`REQ-BF3D-8093-BILLBOARD-EMPHASIS-20260802`。
- 将 8093 炉体的 121 个可见物理/设备测点 Billboard 适度扩大，使中文语义和实时值更醒目。
- 不改变点位清单、实时数据绑定、命中对象、左侧 28 变量、相机旋转、防穿透和无点位聚焦口径。

## 实现

- 运行时：[bf3d-tooltip-stable-hover-8093.js](../高炉前端数据/assets/bf3d-tooltip-stable-hover-8093.js)。
- 固定比例：`BILLBOARD_SCALE_MULTIPLIER=1.22`。
- 初次安装时放大现有 Sprite；同时包装每个 Sprite 的 `scale.set()`，共享 adapter 在实时值变化后刷新纹理和尺寸时仍自动乘以 1.22。
- 只修改 8093 专用资源；共享 adapter 与 8094 页面不修改。

## 部署与回滚

- 通过 [守卫停—改—启脚本](../tools/remote_guarded_redeploy_8093_tooltip.ps1)部署，8768 全程保持运行。
- 当前资源 SHA-256：`E74DFF787522585BA80C44323C744CA9D1B6BE6CD849107EDCE83F35ED0B52F7`。
- 回退目录：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\8093_stable_tooltip_hover_20260802\20260802_200808`。

## 验收

- JavaScript/Python 语法通过，6 项合同测试通过。
- 服务：`BFV4PreviewProxy8093`、`BFV4PreviewWs8768` 均为运行状态；8093、8768 均监听；8093 HTTP 200。
- Chrome 现场页面：`hostStatus=loaded`、`billboardCount=121`、`pointPolicy=measured-121-8093`、`emphasisScale=1.22`、`emphasisMode=moderate-8093`；目视可读性比原尺寸更高。
- 独立 Chromium 冷启动仍可能因 8093 共享静态资源偶发 `ERR_EMPTY_RESPONSE` 未进入 adapter；这属于静态资源链路问题，不作为本比例实现通过的替代证据。
