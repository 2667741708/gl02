# 8093 初始构图与远端/本机一致性审计（2026-08-02）

需求编号：`REQ-BF3D-8093-INITIAL-FRAME-CLOSER-20260802`

## 结论

- 截图底部不是 GLB 缺面或炉体模型没有到底。炉缸底部、立柱和地面网格均已加载；“空白”主要来自相机整体包围球适配时额外保留的 8% 构图余量。
- 8093 初始全景及“全景”复位已改为 `OVERVIEW_FIT_MARGIN=1.0`、`OVERVIEW_ORBIT_MARGIN=1.12`，相对旧 `1.08` 线性适配约放大 8%。
- 防穿透边界没有放宽：`controls.minDistance` 仍等于 `fullOrbitRadius`，近裁剪面仍为 `0.05m`，方位角仍允许 360°。
- 远端受控 GLB 与资产库 `GL02_FURNACE_BODY_R1.glb` 一致；本机 Web 目录中的同名 alias 是另一历史模型，不能拿它判断 8093 资产一致性。

## 守卫部署

- 部署入口：[remote_guarded_deploy_8093_initial_camera_framing.ps1](../tools/remote_guarded_deploy_8093_initial_camera_framing.ps1)。
- 原子部署器：[remote_deploy_8093_initial_camera_framing.py](../tools/remote_deploy_8093_initial_camera_framing.py)。
- 备份：`F:\高炉炼铁项目-real-sensor-v2_V4_8093_PREVIEW\backups\8093_initial_camera_framing_20260802\20260802_213047`。
- 守卫结果：`guard_paused=true`、`guard_restored=true`、`ws8768_unchanged=true`、8093/8768 均监听、HTTP 200。
- 页面部署前哈希为 `F308993A...24FC1`，与上一轮已知线上哈希相同，说明守卫没有在两轮之间回写旧页面。
- 守卫恢复后的新相机哈希为 `1C87B11B...C6F0`，与本机新相机文件一致，说明恢复守卫没有覆盖新资产。

## 远端/本机逐项审计

审计入口：[audit_8093_remote_local_parity.py](../tools/audit_8093_remote_local_parity.py)。

| 对象 | 结果 | SHA-256 摘要 |
|---|---|---|
| 8093 相机运行时 | 一致 | `1C87B11B...C6F0` |
| 121 点筛选器 | 一致 | `EDD9DE67...CD0` |
| 悬浮稳定运行时 | 一致 | `E74DFF78...52F7` |
| 7 张汇总卡 CSS | 一致 | `BDA77396...EE48` |
| 共享 Billboard adapter | 一致 | `8E5FB80F...4E60` |
| 受控 GLB master | 一致 | `150DF18B...ED53` |
| 本机 Web 同名 alias | 不一致，属于历史别名 | 本机 `808960F1...2AF6`；远端 `150DF18B...ED53` |
| 整页 HTML | 不逐字节一致 | 本机 `2AA8D443...E80`；远端 `A6A7D2D6...E3A` |

整页 HTML 不一致是已知部署结构：远端页面累计注入 8093 专用 adapter、点位筛选、相机和 tooltip 标签，而本机主 HTML 没有这些注入。运行时资产本身均与本机源码一致。因此正确口径是“受控模型和 8093 专用运行时代码一致”，不是“整页文件逐字节一致”。

## 验证

- `python -m unittest tests.test_8093_initial_camera_framing tests.test_8093_physical_points_overview`：`7 tests / OK`。
- Python 部署器和审计脚本通过 `py_compile`。
- 浏览器扩展对该重 WebGL 页面刷新、DOM 读取和截图均在 15～30 秒窗口超时，因此本轮没有把新的浏览器截图写成已完成证据；HTTP、文件哈希和运行时静态合同已完成。现场请强制刷新 8093 后目视确认初始构图。
